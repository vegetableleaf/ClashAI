# Shell scripts are run by Git Bash on Windows; CRLF makes bash read the  as part of every
# command ("set: -: invalid option", "cd: pipeline/..: No such file"). Keep them LF on checkout.
*.sh text eol=lf
