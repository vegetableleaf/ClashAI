**GAUNTLET loop 56** — does the sim itself reward the corner Tesla?
**Did:** forced-cell Tesla probe on `c2r_best` in the sim (play.py's greedy rule, gate tau 0.25, no search): 4 arms × 24 matches × 2 seeds, only the Tesla cell forced — the policy's own cell / corner 234 / left-lane 274 / centre 314. Read per-Tesla damage (upper bound), kills, lifetime, and the placement history across the checkpoint line.
**Found:**
- **No, the sim does not reward the corner — and it does not punish it either.** (measured) Damage per Tesla, pooled over 48 matches/arm: own 954, corner 1040, lane 690, centre 1157. Per seed, corner vs centre: 1014 vs 1225 and 1070 vs 1067 (95% CIs ±170, overlapping both times). Kills per Tesla 1.75 corner vs 1.64 centre. The left-lane cell is the worst of the four on both seeds (731/644, CIs disjoint) — so "move it into the lane" would not have been the fix either. Match results across arms: 14/16/10/10 wins of 48, noise.
- **Where the corner came from.** (measured) gatec2 at 5k matches had Tesla spread over 14 cells; at 10k it was 234 in 23/29 and X-Bow 234 in 9/9. c2r resumed that checkpoint (its log shows the cell head already saturated, raw logit 81, rescaled by the guard) and kept it: 13/30 → 27/31 → 63%. Every other arm locks on one cell too, just a different one (aggro1 347, gate05 327), and Skeletons-in-front-of-the-king (423) shows up in every checkpoint I have.
- **The bots go right.** (measured) Enemy river crossings left/right: right 55-64% in all 8 arm-seeds. A corner Tesla (reach 5.5) covers only the left bridge; it still scored as well as a centre one because the scripted bots never make it pay.
- **Why 234 and not any other flat cell (untested):** an X-Bow at 234 reaches the enemy left princess tower (10.4 tiles ≤ 11.5 reach) from outside its range — a real lane-bow spot — and all cards' placement maps come out of ONE conv over a shared trunk, so the Tesla/Tornado/Ice Wizard maps could have been dragged along with the X-Bow's. Settled by the X-Bow lock-rate probe at 234 and a per-card map correlation read.
- **Retraction:** L55's "the sim rewards the corner" — no, the landscape is flat there.
**Means:** the placement problem is not the reward paying for a bad cell, it is a sim that cannot tell a bad cell from a good one, plus a coupled placement head that sticks wherever it first drifts. So the exploration arm I queued would explore a flat landscape and learn nothing — I am parking it. The fix needs information the sim lacks: (i) a human placement prior per card from the 268-replay corpus as a KL term on the cell head (one training change, readable with the placement probe), or (ii) an opponent that punishes out-of-position buildings (bigger job). Oscillation stays a (b): no exploration left + gate updated from 5-20 plays per batch.
**Next:** read the human Tesla/X-Bow cell distribution out of the replay corpus (cheap, ~10 min) — it says how far 234 is from what players do and whether a prior is even well-defined for those cards — then spec the prior arm. Oracle step 2 stays queued behind this.
**Cost:** ~14 min wall, 1 core. Box idle (Nucleo only). Commit + HANDOFF §5cs.26.
