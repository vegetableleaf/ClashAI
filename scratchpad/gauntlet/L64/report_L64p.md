**GAUNTLET loop L64p/q** — S2: icebow crawl done, a column-shift bug of mine caught and repaired, corpus_v5 drive + hogeq expansion running, renderer rebuilt
**Did:** drained the icebow backlog (one account, fixed backoff), launched the full corpus_v5 engine drive, started the hogeq roster expansion (cap 50 → 300 players), rebuilt the daily-clip renderer per your request.
**Found:**
- (a) **Icebow crawl end state:** 2,076 discovered / 2,073 fetched; 2,019 with usable placement rows; 953 already in corpus_v4 → **845 never-driven replays** queued for corpus_v5.
- (a) **Bug, mine, retracted-and-repaired:** the L64n sharded fetcher wrote 13-column play rows into the 12-column `plays_ext.csv` (attr_i does not exist in that file). Every new row was one column out, so the first drive failed on **793/793 tags**. Values were intact, only mislabelled. Repaired with `.bak_L64p` backups first: 63,925 rows remapped into `plays_ext_i1.csv` (asserted attr_s ∈ {blue,red}, attr_i ∈ {0,1}), 801 battles rows with a duplicated trailing "plays" trimmed. The fetcher now reads the live CSV header instead of hard-coding one. Post-repair probe: 3/5 ok, 2 failures in the pre-existing classes → drive launched.
- (a) **Drive so far:** 164/845, 127 ok, 33 failed — **all 33 are the same card**, evo Elite Barbarians (26000043), which the sandbox has no evolution form for (known since corpus_v3: 52/296 on hogeq). ~20% of pro replays are lost to one missing card; corpus_v5 will be biased against E-barb opponents. Engine-side fix is outside this loop.
- (a) **Hogeq expansion:** discovery found **953 battles across 79 players** (was 50 by our own cap); 355 to fetch. Stage 2 spent its first 20 min at 1 replay/min (23 × 429, old drop-on-429 code) then recovered to ~7/min — 40/355 at 23 min. Left running; dropped tags get picked up on the next pass.
- (a) **Renderer** (0587961): name labels, hp bars, solid attack-range rings, dotted aggro rings, target lines, labelled projectiles with landing lines, tower ranges, elixir bars, hand + gate bar + play log with refusal reasons. Only the recorded frames changed — the model input is byte-identical. Trap found: the sandbox's own range tables decompress but hold no values, so ranges come from the wiki stats table + the clashrl sight table.
**Means:** S2's third scaling point is on its way (~×1.8 icebow, not a doubling — say so when it lands). The label-side bug cost one drive attempt and nothing else.
**Next:** let both crawls finish → corpus_v5/icebow = 953 + ok new replays → dataset → 3 seeds `v5lat` on the v3 val set vs 18.17 / 19.84. Then hogeq's turn.
**Cost:** drive ~10 s/replay, ETA ~2 h; hogeq fetch ~45 min if the rate holds. Both running now.
