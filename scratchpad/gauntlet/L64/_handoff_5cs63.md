### §5cs.63 -- L64d (2026-09-06 05:3x-05:5x UTC): **S1 icebow seed 1 agrees with seed 0 (val tile 18.34 / half 16.15 vs 18.12 / 15.36); the ENGINE PLAY HARNESS for the new model exists (`pipeline/engine_play.py`, real engine + L62 ghost opponents, 2-match smoke: 1W-1L, 8.8 plays/min, 45/47 plays accepted, deterministic on re-run).**

**A. Seed 1 (a), `L64/s1/final_icebow_s1.json`.** Best epoch 14 again: val tile 18.34% / half 16.15%, card 59.5%, joint 10.7%, emb cosine 0.205, train-subset tile 22.4%. Matched grid on the same 3,796 plays: 1-tile 17.89 (old 6.69), old's 432 grid **13.28 vs old 13.70** (clean 14.29 vs 13.13), miss distance mean 4.08 / median 2.37 tiles, within 2 tiles 44.3%. Two seeds: tile 18.12 / 18.34, half 15.36 / 16.15, 432-grid 12.54 / 13.28. Band not yet closed (seed 2 running).

**B. Harness (a), subagent record `L64/engine_play.md`, code `pipeline/engine_play.py` (342 lines), tests `pipeline/tests/test_engine_play.py` (3 offline: board<->engine round trip both sides < 1e-6, cell centre maps back to its own cell; OK).** World = `L62/engine_env.py::EngineMatchEnv` subclassed to hand back the RAW `observe()` dict (not the old FakeEngine tensor); obs path = `compact_raw` (strip kind/effects/projectiles, i.e. the same reduction the training rows had, §5cs.61) -> `from_engine` raw branch -> `to_tokens` + `_past` over our own accepted plays. Parity check at the first decision tick: `from_engine(compact_raw(state)) == from_engine(list_frame(state))` True in both matches. Decision rule: every 10 ticks (0.5 s), play iff sigmoid(gate) > tau (0.5; `--gate sample` available), argmax hand-masked card, argmax cell for that card, cell centre -> engine xy -> `eng.act`. Per-decision jsonl per match under `L64/engine_play/`, CPU by default (GPU left to the trainer).
Smoke, seed 0, port 37031, `s1_icebow_s0.pt`: m0 `099P9CL8L2QJ` WIN 2-1 at 240.9 s, 473 decisions, 38 plays / 38 accepted, 9.46 plays/min, p_gate mean 0.30, ghosts 22/22; m1 `02GY9R09LU8J` LOSS 0-3 at 78.1 s, 9 plays / 7 accepted / 2 refused (elixir), ghosts 5/16 delivered. 15.9 s wall per match; re-run byte-identical. Pool human icebow side averages 10.93 plays/min over 477 entries (a) -- same order as the harness's 8.84, which is 2 matches and NOT a policy measurement.
What it does NOT establish: any winrate (n=2), anything under `--gate sample`, and it has not been run with hogeq.
**TRAPS (a):** (1) engine `result_code 13` = not enough elixir in this build (docs say 1050); `L62/engine_env.py`'s ghost retry keys only on 1050, so in the 500-match loop a ghost short of elixir is counted refused, not retried -- add 13 before that run. (2) m1's 0-3 in 78 s is the ghost pool entry, not the harness: replaying that tag with NO plays from us also takes all three towers between t=50 and 70 s -- (b) a pathological pool entry (level/deck mismatch or a mined battle that was not a real game); the 500-match run needs a per-tag "no-plays" control so such entries are visible and a do-nothing baseline exists. (3) `close()` appends to the untracked `L62/deal_cache.json` (benign).

**C. Next.** Seed 2 icebow (~06:05 UTC) closes the icebow band; hogeq x3 (~06:45). Then, box permitting, the first real engine read: 3 checkpoints x N ghost matches with the no-plays control, both gate rules.
