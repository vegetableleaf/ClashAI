### §5cs.75 -- L64p (2026-09-06 17:4x-18:5x UTC): **icebow crawl COMPLETE (2,073 of 2,076 discovered battles fetched, single account); my own crawl_par writer had SHIFTED every new plays row by one column, which is why the drive rejected all 793 new tags -- repaired with backups, values were intact; corpus_v5 drive (845 tags) and the hogeq roster expansion (50 -> 300) launched; the daily-video renderer shipped (c9aa461).**

**A. Crawl end state (a).** `battles_raw.json` 2,076, `replays_done.json` 2,073, `battles.csv` 2,074 rows. The last drain ran with the fixed backoff (`--rl-sleep 15 --rl-passes 2`), one account, per the owner's ruling ("we'll stick with one crawl account"). Of the 2,073 fetched, **2,019 tags have usable placement rows**; 953 are already in corpus_v4; **845 have never been driven through the engine** -> `scratchpad/gauntlet/L64/v5_tags_icebow.json`. The remaining ~221 were attempted in earlier drives and failed (engine card support, §5cs.72 D's "card has no native evolution form" class).

**RETRACTION of §5cs.72 E's "+457 unfetched icebow battles" (c).** Broken down against `replays_done` and the corpus manifests: 16 never fetched, 52 fetched but never driven, 226 driven-and-failed. The 457 counted the failures as backlog. The real never-driven backlog today is the 845 above, almost all of it from this window's fetches.

**B. The column-shift bug (a) -- mine, caught by the drive, repaired.** The first full-corpus drive on the new tags failed on every one of 793 tags with `KeyError: '1'` / `'0'` on `attr_s`. Cause: `crawl_par.py` (L64n) hard-coded a 13-field play row with `attr_i` in the middle and appended to `plays_ext.csv`, whose header is **12 columns and has no attr_i at all** (that column only exists in `plays_ext_i1.csv`, LAST). Every new row landed one column out: `attr_s` held attr_i ("0"/"1"), `attr_t` held attr_s, and attr_t spilled into a 13th unnamed column. Second bug in the same writer: `pipeline.BATTLE_FIELDS` already ends with "plays"; I appended it again, so battles rows carried a duplicated trailing value. The site markup was NOT the cause -- a fresh fetch of one replay parsed byte-identically to the stored row.
- Repair `scratchpad/gauntlet/L64/_repair_csv_L64p.py`: `.bak_L64p` copies of all three CSVs first (860 KB / 11.3 MB / 3.8 MB, retained in `icebow/data/royaleapi/crawl2/`); moved **63,925 shifted rows** out of `plays_ext.csv` into `plays_ext_i1.csv` remapped to that file's own order (asserted attr_s in {blue, red}, attr_i in {0, 1} after the remap); trimmed the duplicate from **801 battles rows** (asserted the two trailing values were equal before dropping). Values were never wrong, only mislabelled.
- `crawl_par.py` now reads the live header of `plays_ext_i1.csv` (`play_fields()`), writes only there, asserts the header is unchanged at merge, and passes `pipeline.BATTLE_FIELDS` unmodified.
- Probe after the repair (`drive_v5_probe.out`, 5 tags): **3 ok, 2 failed on known classes** (1 "card has no native evolution form", 1 side-inference SystemExit), 5 rotated tags, accept 0.98 on the ok replays. That is the pre-bug failure profile, so the drive was launched.

**C. Launched (both running at write time).**
- **corpus_v5 drive**: `replay_batch.py --crawl icebow --plays-file plays_ext_i1.csv --tags v5_tags_icebow.json --record-every 20 --record-plays --determinism-every 10 --port 37031` -> `scratchpad/gauntlet/ext/corpus_v5_new/icebow`, log `drive_v5_icebow.out` (ends `DRIVE_V5_EXIT`). At 29/845: 26 ok, 3 failed (all "no native evolution form"), 8.5-15 s per replay -> ~2.3 h. **Projection (b):** at the probe's ~60-90% ok rate, corpus_v5/icebow = 953 + ~500-750 = ~1,450-1,700 replays, i.e. between x1.5 and x1.8 over v4, NOT a full doubling. The third scaling point will need that stated when it is read.
- **hogeq roster expansion**: `crawl_deck.py --deck hogeq expand --expand-to 300`, log `hogeq_expand.out` (ends `HOGEQ_EXPAND_EXIT`). Owner's taxonomy warning tested BEFORE launch: the hogeq seed in the crawler config already maps to RoyaleAPI's slugs and all 600 existing hogeq battles match them (a). At 54/115 players walked, 700 battles kept. **Trap carried:** crawl_deck.py's stage 2 still has the drop-on-429 flat-120 s behaviour that L64n measured at 62-63% of wall clock; it is NOT the crawl_par path. If stage 2 crawls, stop after discovery and point crawl_par at hogeq (its OUT is hard-coded to icebow's crawl2 -- needs a `--deck` option first).

**D. Daily video renderer (a, shipped c9aa461).** `engine_play.py --record-every N` keeps every Nth engine observation plus a marker at every model play; `pipeline/match_video.py` renders that to a 1080x1920 H.264 MP4 (towers = the entities with `name: None`, dots scaled by hp, both elixir bars from `elixir_exact`, the model's hand, a flash at each placement); `pipeline/daily_clip.sh <deck> <ckpt> "<label>"` is the one-command version, fixed seed 0 so day-to-day clips share the opening. Two bugs found by looking at stills, not by tests: the mirror was backwards (side 1 sits at HIGH engine y) and a Pekka was drawn as a tower (max_hp is not a tower witness). It draws the engine's observation, not the game's art -- said so in the HUD.

**What this does NOT establish.** Nothing about model quality moved this loop. The corpus_v5 size is a projection until the drive exits. The hogeq expansion's yield is unknown until stage 2 finishes.

**Traps.** (1) A CSV writer given a field list it did not read from the file it appends to WILL desync silently -- `csv.DictWriter` neither checks the header nor complains about extra fields; the failure surfaced two steps later as a KeyError in an unrelated tool. Read the header, assert it at merge. (2) `pipeline.BATTLE_FIELDS` already ends with "plays". (3) Retained `.bak_L64p` files are the only way to audit the repair; do not delete them until corpus_v5 is graded.

