### §5cs.68 -- L64i (2026-09-06 08:3x-09:4x UTC): **hogeq engine read on 100 paired ghost entries: S1 checkpoints 53-47 / 19-1-80 / 44-56 vs no-plays 0-100 and rate-matched random 18-82 -- two of three checkpoints clearly beat random, one equals it; the outlier plays 17.4 accepted cards per match vs 26.8 / 24.9 with the SAME p_gate median (0.36), i.e. the tau 0.5 threshold decides again (p_gate p90 sits at 0.48-0.49 for all three). Rotation of the re-fetched i=1 half passes its first fidelity test: engine-vs-real winner agreement 64.1% (41/64) vs 67.2% on corpus_v3 hogeq; blue tesla lane preference agrees (left fraction 0.224 vs 0.279; a mirror would give 0.776).**

**A. hogeq controls (a), `L64/engine_hogeq/`, scores `score_thr.txt`, `score_ck1.txt`, `score_ck2.txt`, `score_rnd.txt`, pool = 241 hogeq ghost entries (§5cs.66), 100 entries seed 0, paired by match index.** No-plays: 0-100, mean 107.3 s (icebow's pool: 91.3 s), 2.87 crowns against. Random p 0.13 (15.25 plays/min offered, 59.7% accepted): **18-82**, +40.8 +/- 4.7 s, survived longer 90/100, crowns against 2.41. (Icebow's same control: 3-97 -- hogeq's ghosts are the softer pool, or random hogs reach a tower; not separated.)
**B. Checkpoints (a), threshold tau 0.5, same 100 entries.** s0: **53-47**, 188.4 s, +81.1 +/- 6.1 s, survived longer 92, crowns against 1.40, 9.73 plays/min, 87.6% accepted, p_gate mean 0.339. s1: **19-1-80**, 175.0 s, +67.7 +/- 4.7, survived longer 94, crowns against 2.06, 6.94 plays/min, 85.7% accepted, p_gate mean 0.346. s2: **44-56**, 190.5 s, +83.2 +/- 4.6, survived longer 93, crowns against 1.46, 9.11 plays/min, 86.2%, p_gate 0.344. Band: 38.7 wins mean, sd 17.6 -- NOT closed the way icebow's was (75/71/71, §5cs.66); val tile order (21.30 / 20.58 / 21.08) does not predict it (s2 < s0 on val, s2 ~ s0 on the engine; s1 lowest on both but 2.6x further off on the engine).
**C. What the outlier does differently (a), `ckpt_diff.py` / `ckpt_diff.txt`, per-decision logs.** Accepted plays per match 26.8 / **17.4** / 24.9. p_gate median 0.352 / 0.361 / 0.356, p90 0.493 / 0.484 / 0.491, fraction of decisions above tau **0.083 / 0.059 / 0.077** -- the whole difference is in the tail above 0.5, which for hogeq is thin for every seed. Mean elixir at decision 8.0 / **8.73** / 8.2 (s1 banks and wastes at the 10 cap); hog plays 293 / **174** / 253, earthquake 179 / **52** / 120, tesla 252 / 172 / 231. Same story as §5cs.49 (L62j): with p_gate p90 under tau, a calibration wobble of a few hundredths moves the play rate by a third and the result by 25-35 wins. (b) If that is the cause, the same checkpoint under `--gate sample` (plays proportional to p_gate) should play ~25/match and land near s0/s2 -- RUNNING on 37031 (`smp_ck1`, task bnh3nz4c4, ~30 min). If it does not, the placement policy of s1 is genuinely worse and val tile hides it.
**D. Rotation fidelity for the re-fetched half (a), `L64/xy_why/refetch/drive_compare_hogeq.txt`, `handedness_hogeq.txt`.** `replay_drive.py` gained `PLAYS_FILE` / `set_plays_file()` and rotates rows with `attr_i == "1"` by (18000 - x, 32000 - y) on load (`ROTATED` counter, `replay_batch.py --plays-file`, aggregate reports `rotated_rows/tags`). First 80 re-fetched hogeq tags driven through the engine (`corpus_v3_i1/hogeq`, 37032): 64 ok rows, accept rate 97.6% (v3: 98.7%), **winner agreement 41/64 = 64.1% vs v3 162/241 = 67.2%**, exact-crowns 43.8% vs 56.4% (n=64, 2 sd low -- (b) watch on the full set), terminal_vs_last_play median -101 vs +120 (36/64 negative vs 88/241 -- (b) the rotated matches end earlier than the real ones more often; unexplained, re-read on the full set). Handedness on 207 rotated replays: blue tesla left-fraction 0.224 (n 1,084) vs 0.279 (n 1,410) in the i=0 half, z -3.1 but the same side; a vertical mirror would put it at 0.776 -- rotation is the right transform (a); 7 of 89 (side, card) pairs differ at |z| > 3, more than chance, so the two halves are not identical samples (b -- player mix, or a residual frame issue; the icebow x-bow will be a second witness).
**E. Re-fetch state (a).** hogeq 180+/299 at 09:32 UTC (rate ~4/min under RateLimited, 15 sleeps); icebow's first attempt died at connect on the 60-s Cloudflare check (`AuthError` inside `pages.renew()`), driver hardened to fall through to the saved token; it re-runs after hogeq (chained task ended with the hogeq run -- relaunch needed). hogeq i=1 drive chained to the re-fetch's DONE line (task bq5hzc1gl).
What this does NOT establish: why s1 crosses tau less (C's test is running); anything about icebow's i=1 half; whether the exact-crowns deficit in D is noise.
**TRAPS (a):** (1) hogeq's gate tail is thin: p90 of p_gate < tau for all three seeds, so `threshold` reads are seed-fragile on this deck; quote plays-per-match with every hogeq engine number. (2) `replay_batch.py` output dir must exist before `> out.txt` redirection (twice now).
**F. Next.** Read `smp_ck1`; if it restores the play rate and the result, the engine instrument for hogeq becomes `sample` (or tau re-calibrated per deck from the gate prior -- one change, later). Then corpus v4: drive the icebow i=1 half when its re-fetch lands, build `corpus_v4/<deck>` = v3 + i1, S1 re-run 3 seeds/deck.
