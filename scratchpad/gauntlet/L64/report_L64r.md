**GAUNTLET loop L64r** — S2: hogeq roster expanded and fully fetched; the CSV shift bug recurred from a second writer
**Did:** finished the hogeq expansion (crawl_deck + the requeueing fetcher for what it dropped), repaired and root-caused a second instance of the column-shift bug, and applied your three renderer requests.
**Found:**
- (a) **Hogeq: 953 battles across 79 players, ALL fetched.** 307 new via crawl_deck (63 min, 4.9/min, 65 × 429), the 48 it dropped via crawl_par (7.9 min, 6.1/min). Final: battles.csv 953 × 29 cols, plays_ext_i1.csv 58,002 × 13 cols, attr_s ∈ {blue,red}, attr_i ∈ {0,1}, 944 tags with plays. Hogeq's engine corpus is still 296 tags, so its undriven backlog is now ~648.
- (a) **The column shift recurred — from a different writer.** crawl_deck.py builds its field list from the parser's keys, and the parser emits attr_i, so it also wrote 13 fields into the 12-column plays_ext.csv: **26,720 rows** shifted. Repaired (.bak_L64q backups, domains asserted) and fixed at source. **Trap for the ledger:** `csv.DictWriter` never checks its fieldnames against the file it appends to — any append must read that file's live header. Two independent writers made the identical mistake.
- (a) **"Knight (hero)" is correct, not a naming bug.** Engine matches play the *pro's* deck, forms included; the icebow pool is 433 evo-knight / 40 hero-knight / 4 base and seed 0 landed on a hero game. Form never reaches the model: engine_key folds hero/evo/base to one `knight` token, unmapped=[] . New `--own-forms` keeps only pool replays matching our forms (433/477); the daily clip uses it.
- (a) **Bridges were wrong, river was not.** Bridges drew 1 tile, should be 3 — fixed (columns 2-4, 13-15; troop centres in the river band occupy exactly those). River stays 2 rows: the sandbox docs say rows 15..16 are river (15+2+15=32) and no ground troop off a bridge ever occupies y 15.5-16.5. The in-game river looks thinner because the banks are painted on land tiles.
- (a) **Towers now 3×3 / 4×4** with a tile-alignment check: princess centres are tile centres (±1.5 lands on boundaries), the king centre is a tile corner (±2.0 does too).
- (a) **`*.sh` CRLF trap:** autocrlf gave daily_clip.sh Windows endings and bash read the `\r` as part of every command. Fixed repo-wide (`.gitattributes`, `*.sh text eol=lf`) — this would have hit any script we hand over.
**Means:** the hogeq half of S2 is now data-ready and is the bigger untouched corpus of the two. Nothing about model quality moved this loop.
**Next:** icebow corpus_v5 drive (452/845, 363 ok / 84 fail) → dataset → 3 seeds v5lat vs 18.17 / 19.84. Then drive hogeq's 648 new tags.
**Cost:** ~70 min of crawling, no training. Running: the icebow drive, ~65 min left.
