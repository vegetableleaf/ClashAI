### §5cs.60 -- L64a (2026-09-06 04:4x-05:1x UTC): **OVERNIGHT TRIGGER FIXED (timer tasks), CLEANUP LOOP DONE (524 MB / 3,297 files out of scratchpad, zip-backed), and the S1 DATASET BUILDER shipped -- icebow 21,687 pro-play rows + 56,590 wait rows from 493 replays (old corpus: 9,444 placements / 211), hogeq 9,797 + 23,421 from 241.**

**A. Trigger (a).** `ScheduleWakeup`/`CronCreate` never fire while the session idles (3 nights); a background task's exit re-invokes the loop (60 s timer test fired within seconds; 8-min timer fired; batch exit fired). Standing rule now in `.claude/commands/gauntlet.md` §4 and §7 here: end every loop on `sleep N; echo WAKE` (N <= 570 s, chain longer), keep batches as background tasks.

**B. Cleanup (a), ruling 6, record `L64/cleanup.md` + `cleanup_manifest.csv` (5,075 rows).** scratchpad 1.1 GB -> 557 MB, files 5,071 -> 1,789; deleted 3,297 (1,435 tracked, 1,862 untracked), all inside `scratchpad/`, all first zipped to `C:\Users\benpe\ClashBot_archive\scratch_2026-09-06.zip` (360 MB, 3,297 entries, sizes verified, testzip clean). Kept: corpus_v3, batch_v2, L62-L64, every md/py/sh/ps1/yaml/npy of L1-L61, all 101 `.pt`, engine_view json/py/log. Deleted by judgement: engine_view mp4/png renders (112 MB; HANDOFF line ~1660 cites a png now only in the zip), `ext/replay_*_run1.json`, `ext/dump` binaries, `ext/batch` (v1). `*/data/`, `research/`, `.venv` untouched; `data/` staged count 0 before and after. Stray 0-byte `icebow/HANDOFF.md` removed. `.gitignore` proposal for scratch bulk in cleanup.md §6 (not applied).
TRAP (a): the agent staged its deletions with `git rm --cached` while the lead committed L64a -- `git commit` takes the whole index, so the 1,435 deletions rode inside `9adc967` under an unrelated message. Rule: subagents never stage; only the lead touches the index.

**C. S1 dataset (a), `pipeline/dataset.py`, tests `pipeline/tests/test_dataset.py` (24 OK with the contract tests).** Rows in MY board frame via `from_engine`/`to_tokens`: PLAY rows = the full engine state before every ACCEPTED pro play of a deck-matching side (deck matched on all 8 base keys of `final_decks`; 2 icebow mirror matches give both sides), target = deck slot + continuous (x, y); WAIT rows = every 2nd compact frame (40 ticks = 2 s) with no accepted play in the next 20 ticks, hand/next reconstructed EXACTLY (the engine hand changes only on an accepted play, so between accepted plays k, k+1 the hand is `hand_before(k+1)`), targets = the slot played next + seconds until it. Every row: past-actions channel (last 3 plays: slot, x, y, seconds ago) and the REAL RoyaleAPI crowns (mine, theirs). Split by replay tag (crc32 % 100 < 15 -> val). Output `<deck>/data/pipeline/s1_dataset.npz` (variable-length unit rows + offsets; icebow 5.8 MB, hogeq 2.3 MB; build 18 s / 6 s).
Numbers: icebow 78,277 rows = 21,687 play + 56,590 wait (val 13,761), units/row 4.4, played card in the recorded hand 100%, wait rows with a fully known hand 100%, 12,163 frames dropped as within 1 s of a play, 256 plays skipped (rejected or unrecorded); hogeq 33,218 = 9,797 + 23,421 (val 6,133), 644 skipped. Crawl names map through `crawl_slot` (`the-log`/`x-bow`/`ice-wizard` -> vocab keys; first smoke lost 84/207 plays to this).

**D. Not established.** Whether 2-s wait sampling is the right negative rate for the gate (b; the trainer can reweight). Whether the 9.1% tray-tap defect of the OLD labeller has any analogue here: none by construction (engine-accepted plays only), but the 256/644 skipped plays are pro plays the engine refused, i.e. the engine's state had already drifted there (a, count only).

**E. Next (L64b).** The S1 model + trainer (`pipeline/model_v3.py`, `pipeline/train_s1.py`): entity tokens + coordinate patch tokens, full-res half-tile cell head (36x64) as a query-key head with no upsampling, hand-masked card head, state-conditioned gate, wait-for-card head, categorical crown-diff value head; mirror aug; board-blind histogram baseline on the SAME val rows first (the old 13.65/15.44 pair was on the old rows -- a different instrument until the old model is re-scored on the new rows).
