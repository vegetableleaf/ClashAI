### §5cs.72 -- L64m (2026-09-06 13:5x-14:2x UTC): **the v4-lattice checkpoint reads INSIDE the v3 band on the hogeq engine instrument (82 wins vs 85/79/84, one seed, a null screen -- the instrument sits at 80-85% and cannot see a +1.6 pp placement gain); the icebow i=1 half is driven and graded (460/560 ok, winner agreement 74.6% vs 78.5% for the i=0 half -- the same 2-4 pp fidelity deficit hogeq showed, second witness for §5cs.69 D); corpus_v4/icebow = 953 replays / 147,842 rows (v3: 493 / 78,277), play share 27.4% vs 27.7%; the icebow lattice chain (v3-lattice x3 -> v4-lattice x3 -> floor place eval) is running.**

**A. Engine screen (a), `engine_hogeq/smp_v4lat0/summary_hogeq_s0.json`, `score_smp_v4lat0.txt`.** `s1_hogeq_v4lat_s0` (§5cs.71, lattice grid, placements land ON the lattice) under `--gate sample`, seed 0, the same 100 entries as the §5cs.69 band: **82 W / 18 L** vs v3 floor 85 / 79 / 84 (mean 82.7, sd 3.2); survival +55.8 s +/- 6.2 vs control (v3: +57.0 / +38.6 / +51.0); crowns against 0.79 (v3 0.82 / 0.97 / 0.95); crowns for +1.99 vs control (v3 +2.4 / +2.4 / +2.36); plays per match 48.2 (accepted 31.2, ghost-ok 20.8) vs v3 s0 56.3 (32.9, 21.9). Read: no separation, and none was expected -- the instrument scores a whole match against the replayed pro's opening, and at 80-85% wins it has no headroom for a 1.6 pp placement change. (b) Crowns-for at 1.99 vs 2.36-2.4 is the only number outside the v3 spread; one seed, not a finding; a 3-checkpoint band would settle it and is parked (the engine instrument is not the S2 grade, pro agreement is).
**B. Icebow i=1 half (a), `xy_why/refetch/fidelity_icebow_full.txt`, `handedness_icebow_full.txt`, `drive_icebow_rec.out`.** Refetch 563 done / 560 usable / 3 rejected (02CY8L9JVCY0, 02VY8LGY90UV, 09PP9RQJGYPQ); 47,642 positioned rows, all i=1; 51 RateLimited + 4 AuthError tags skipped (resumable, `refetch_i1.py --deck icebow`). Drive with v3's flags on 37031: 460 ok / 560 (82.1%), rotated 51,991 rows over 560 tags. Fidelity vs the i=0 corpus_v3 half: **winner agreement 343/460 = 74.6% vs 387/493 = 78.5%; accept 0.9897 vs 0.9922; exact crowns 0.722 vs 0.769; terminal-before-last-play 123/460 = 26.7% vs 113/493 = 22.9%**; expected side-1 wins 314 vs engine 267 (v3 338 vs 283). Same direction and size as hogeq (65.3 vs 67.2): the rotated half is a little less faithful on both decks, and the residue is still unexplained (b: candidates are the y-offset of the rotation and the truncated tail of the crawl; neither tested here). Handedness: 113 cards, 5 with |z| > 3 on left-fraction, sum |dLeft| rotation 7.04 vs mirror 8.12 -- rotation is the better hypothesis on icebow too (a).
**C. corpus_v4/icebow and the chain (a).** 493 + 460 = 953 replays, 0 name collisions, `frames` present in the i1r replays (checked before the copy -- §5cs.70 E trap). `icebow/data/pipeline/s1_dataset_v4.npz`: 147,842 rows (40,449 play / 107,393 wait), play share 0.274 vs v3 0.277, val_rows 23,527, unmapped []. Chain `s1_v4/run_icebow_lat.sh` (task launched 14:04, `run_icebow_lat.out`): v3 `--grid lattice` x3 -> eval on v3 val -> v4 lattice x3 -> eval -> v3 floor checkpoints through the patched evaluate (so icebow gets `place_1t`/`place_dist` for all three bands). Icebow floor reference 18.22 +/- 0.11 (tile, floor instrument -- NOT comparable to lattice top-1; compare via place_1t / place_dist and lattice-vs-lattice).
**What this does NOT establish:** anything about icebow scaling yet; whether the engine instrument could see a larger S1 gain (it saturates near 85 on hogeq; a harder instrument -- later entries, or a pro-agreement grade on engine states -- is the S3 question, not S2's).
**TRAPS (a):** (1) An instrument at 80-85% of its ceiling is not a discriminator for a 1-2 pp change; check the headroom before spending 40 min of engine time. (2) `score_smp_ck1.txt` carries two JSON lines (an earlier threshold run and the sample run); take the last.
**E. Next.** Read the icebow bands as they land (`eval_summary_icebow_*.txt` via `summ_eval.py`), then decide the x10 corpus step (the +457 unfetched icebow battles and the RateLimited skips are the cheap next data; beyond that the crawl has to widen).
