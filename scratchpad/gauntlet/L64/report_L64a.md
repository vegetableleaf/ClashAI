**GAUNTLET loop L64a** — Square One: S0 close-out, cleanup, S1 dataset
**Did:** Cleanup loop (owner ruling 6) via a subagent with manifest + zip backup; wrote and tested the S1 dataset builder `pipeline/dataset.py`; built both decks' datasets.
**Found:** (a) scratchpad 1.1 GB → 557 MB, 3,297 files deleted (all zipped first to `ClashBot_archive/scratch_2026-09-06.zip`, 360 MB, verified); `data/`, `research/`, venvs untouched. (a) Dataset: icebow **21,687 pro-play rows + 56,590 wait rows** from 493 replays (old corpus: 9,444 placements / 211 replays); hogeq 9,797 + 23,421 from 241. Played card is in the recorded hand on 100% of rows; wait rows carry an exactly reconstructed hand (the engine's hand only changes on an accepted play). Past-actions channel and real crowns attached for the value head. 24 tests OK.
**Means:** S0 is closed; S1 has its data. Trap recorded: the cleanup agent's staged deletions rode into an unrelated commit (`9adc967`) because a commit takes the whole index — subagents never stage from now on.
**Next:** L64b — the S1 model + trainer (entity + coordinate patch tokens, full-res half-tile cell head, hand-masked card head, state-conditioned gate, wait-for-card head, crown-diff value head), with a board-blind histogram baseline on the same val rows measured FIRST so the top-1 number has an anchor.
**Cost:** ~40 min wall; nothing running; engine idle.
