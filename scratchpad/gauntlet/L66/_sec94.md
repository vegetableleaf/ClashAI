### §5cs.94 -- L66n (2026-09-07 13:2x-15:5x UTC): **the oracle-future hypothesis is tested and does NOT rescue the objective: averaging over K=4 jittered opponent futures moves the pro's cell from rank 24 to rank 21 of 49 (paired: 19 better / 24 worse / 22 same). Handed the pro's own cell as a candidate, the teacher picks it 2-6 times in 65 against the student's 22-24%. Five hypotheses, five nulls: THE ENGINE SEARCH TEACHER IS CLOSED. S3-as-search-teacher ends with a negative result; the next step is S4, which is the owner's**

**A. The test (a).** §5cs.93 G as specified: `--futures 4 --jitter-ticks 60 --jitter-tiles 1.0 --opponent both --include-pro`, opponent plays shifted U(-3 s, +3 s) and ~1 tile, the generator seeded per (state, k) so all 49 candidates in a state face the same four futures; pro's own follow-ups exact. Same 65 states / 12 tags as §5cs.93, h400, v2. ~2.5 h on 4 slots.

| mode | pro rank, median of 49 | pro in top quartile | pro co-best | pro >500 HP behind | teacher chose the pro cell EXACTLY | chosen -> pro, mean / median tiles |
|---|---|---|---|---|---|---|
| `replay` | 27 | 26.2% | 15 | 26 | 4 / 65 | 10.26 / 10.78 |
| `both` | 24 | 36.9% | 18 | 21 | 6 / 65 | 10.79 / 9.80 |
| **`jit4`** | **21** | 40.0% | 15 | 17 | **2 / 65** | 9.51 / 9.25 |
| *student v5lat, full bench (§5cs.87)* | -- | -- | -- | -- | *21.9-23.9%* | *3.34-3.48* |

Paired per state, rank(jit4) - rank(both): **19 better, 24 worse, 22 unchanged, median 0.** Chosen cell identical to `both` in only 9/65: the futures change the decision, they do not move it toward the pro. Median gap best-minus-pro falls 227 -> 114 HP (the plateau flattens under averaging, as it must), and the pro cell still sits in the bottom half more often than not.

**B. Reading (c).** The hypothesis predicted the pro cell would rise to the top quartile once the objective stopped seeing a known future. It rose three ranks. The monotone 27 -> 24 -> 21 across realism steps is real but each step is worth ~3 ranks and costs 1x -> 1x -> 4x; extrapolating to "rank ~1 with a proper opponent model and K=32" is not licensed by anything measured and would cost days of build (an opponent policy in the sandbox, student inference on the VM, feature parity between engine observation and the S1 features) to test. Per-card the movement is inconsistent (tesla 22.5 -> 10 and x-bow 26.5 -> 12 improved; skeletons 10 -> 26 and knight 33 -> 36 worsened; n 6-13), which is what noise around a small effect looks like, not a mechanism.

**C. Verdict on S3's search teacher (a).** Hand-written tower-damage rollouts, in five forms -- with and without the unit term (§5cs.88), with and without the recorded opponent (§5cs.91), at horizons 120-2400 (§5cs.92), with the pro's own sequence supplied (§5cs.93), and averaged over sampled futures (here) -- rank the pro's actual placement below the median random legal cell, and when handed that placement as a candidate choose it 3-9% of the time. The pre-registered gate (§5cs.53 C: searched targets agree with pros on exact cell at least as often as the student) is **failed in every configuration**, and the failure is not reachability (§5cs.90 B) and not any of the five mechanisms above. **The engine search teacher is closed.** This is the same finding as the reward-shaping era (L62l: four PPO arms, no gain in pro agreement) reached from the other direction: an objective this project can write down does not encode what pros do with troops and buildings. Spells are the exception (log rank 1, tornado 7, rocket 7-10 in every mode) -- an immediate, local effect the objective can see.

**D. What this does NOT establish (b).** That *no* teacher can beat the student -- only that no rollout objective tried here agrees with pros. That pros are optimal -- pro agreement is the gate because it is the only calibrated instrument, not because pros are the ceiling. That the engine is useless: it remains the only source of faithful states and was the plan's second use (IL pivot ruling: "sandbox engine for faithful states"). Its role in what follows is state generation and live-path parity, not labelling.

**E. Where S3 ends and S4 begins.** DAgger needs a labeller that agrees with pros on states the student visits; the only such labeller is a better student, and the student is corpus-limited at +1.50 pp per doubling with mining closed (§5cs.86). So there is no S3 step left that the evidence supports, and the next step is **S4: the v5 student on the live path** -- which the gauntlet may not touch (guardrail: live-play path). The live-path caveats already on record (L62l: detector observation vs training features, `play.py` gate rule) are the owner's first job there; an offline **feature-parity audit** (can the S1 input vector be built from the live detector observation at all, and with what error) is the one preparatory measurement the gauntlet could run without touching the live path, if the owner wants it.

**F. VM.** Nothing left to run. Powered off this loop (`sudo poweroff`; ~9 h of billing this window, ~$3.50). Restart if needed: `gcloud compute instances start clashbot-s3 --zone=us-central1-a` (owner); everything on it -- SDK, AVD, sandbox, runtime, tools -- persists on the disk, and the engine slots come back with `bash scripts/start_direct_service.sh --serial emulator-5554 --port 3703N --slot N` under `nohup setsid` after the emulator is up.
