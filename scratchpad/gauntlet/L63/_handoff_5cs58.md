### §5cs.58 -- L63f (2026-09-06 02:5x-03:1x UTC): **S0 STEP 2b DONE -- the own-click contract test measured the live side of the contract on 276 real human plays: same-class recall of the just-placed unit 0.81 within 1.5 s, x error 0.29 tiles median, and a systematic troop y bias (-0.73 tiles, the box centre is not the feet) that is now FIXED in `from_live`; `degrade()` is recalibrated to measured conf / team-tag numbers (20 tests OK).** Two labeller defects found in `label.py`. Corpus batches still running (icebow 132/619, hogeq 150/296 at 03:07 UTC).

**A. Instrument (a), `L63/s0/ownclick_run.py` + `ownclick_analyze.py`, record `ownclick.md` (279 lines), `ownclick.json`.** Sessions: icebow `20260804_173304`, `20260804_192006`, `20260815_222309`, hogeq `20260817_194419` (hogeq has no detector of its own -- `hogeq/config/config.yaml:1166` points at icebow's board-24-5). Plays from `label._extract_plays` (config values, not defaults); card identity from hand templates (262 of 276 identified); detector run on frames t+0.25..1.5 s after each board click (4,353 frames, 17,336 detections); click warped through the same `BoardWarp` as `from_live`; nearest same-class my/unknown-side unit = the placed unit. Ground truth = the click, EXCEPT where the game snaps the drop (river-line / enemy-half clicks, 66 of 232, excluded from bias estimates) and where nothing was placed (tray taps, below).

**B. Numbers (a), unclamped identified plays n=232 unless stated.**
- Recall of the placed unit within 0.25-1.5 s: same class + side my/unknown **0.81** (187/232); same class any side 0.87; any deck class 0.89; any new detection **0.97**. Per session strict 0.78 / 0.93 / 0.88 / hogeq **0.55** -- hogeq's loss is class confusion on an icebow-era detector (hog_rider -> knight, mighty_miner -> tesla/knight/skeleton_king, firecracker -> skeletons; class-agnostic 0.87). The existing `DEGRADE_RECALL 0.855` is consistent with this.
- Position error at first sighting: **x median 0.29 tiles, p90 0.83, unbiased** (signed median -0.03). **y median 0.72, p90 1.78, biased by class: troops sit 0.73 tiles ABOVE the click** (my-half, n=73; = -0.354 box heights), buildings -0.09, spells -0.05. Cause: `from_live` used `d.gy`, which is the box centre for ground units (`replay_mine.py:88-96`, shadow correction is flyers-only). With `fy = gy + 0.25*h` the troop residual is |dy| median 0.35 / p90 0.88.
- Team tags at first sighting of the human's OWN units (offline colour vote, no TeamTracker): mine 17%, unknown 53%, enemy 30%; own SPELLS tagged enemy 26/62 = 42%. Over all detections: unknown 0.252 (troop 0.235 / building 0.286 / spell 0.295).
- Conf: p10/p50/p90 = 0.471/0.835/0.912, p99 0.97, 12.3% below 0.5, 57% in 0.80-0.95 -> the old uniform(0.35, 1.0) is (c) contradicted in shape.
- Spells (71 plays): seen only as `*_aoe` transients; first appearance tornado 0.63 s median (p90 1.26), earthquake ~1.0-1.3 s, rocket/log 0.34 s.
- First-sighting latency of troops/buildings: 0.32 s median, p90 0.89.

**C. Contract changes (this loop, `pipeline/obs_contract.py`, tests 20 OK).** `from_live`: ground troops (kind troop, no `ground_cy`) use `fy = gy + TROOP_FOOT_K * h`, K = 0.25 (a, own-half troops; (b) for enemy troops -- same sprite geometry, unmeasured). `degrade()`: conf drawn from a measured-shape mixture (p 0.30 uniform(0.35, 0.78) else N(0.86, 0.045) clipped to [0.35, 0.995]); `pos_sigma_tiles` default 0.45 (measured TP noise after the foot fix); `unknown_team_rate` default 0.25 (measured); NEW `wrong_team_rate` per kind (side flipped), default troop/building 0.15 (b: after TeamTracker; offline it is 0.30) and spell 0.40 (a: 0.42 offline). `_FP_JITTER_TILES 1.0` stays UNMEASURED (no enemy ground truth in human play). Tests: synthetic troop detections now carry the centre-above-feet geometry; `test_rates` runs at sigma 0; new `TestOwnClickCalibration` checks the unknown/wrong rates, conf median 0.835 +- 0.03 and seed determinism.

**D. Labeller defects (a), NOT fixed (label.py is the old IL path; the S1 labeller is new and must not inherit them):** (1) `_extract_plays` (`label.py:73/77`) stamps the slot-select time, not the board click; (2) `which_slot` admits taps at frame y in [0.80, 0.86) as plays (`label.arena_bottom 0.86` while the card art reaches ~0.84): **25/276 = 9.1% of "plays" are tray-card taps that placed nothing** (0/25 produced any new detection) and each consumes the pending select. (3) River-line clicks are snapped by the game: the unit appears 1-2 tiles below the click (x-bow +2.3, skeletons +1.6) -- the click is not the label there.

**E. Not established.** K for enemy troops and for flyers (b); FP position distribution (b); whether TeamTracker brings the live wrong-team rate to ~0.15 (b -- measure from a play.py detection dump once the live logger exists, S4 prerequisite). The recall number is for the human's own units 0.25-1.5 s after placement, not a per-frame presence recall.

**F. Traps.** Both projects' packages are named `clashrl` (hogeq run through icebow's with a `cfg.root` override); `Vision`'s wall-clock hand-slot cache must be disabled offline (`hand.cache_diff=0`); `BoardWarp.frame_to_board` silently CLAMPS off-field points to y=1.0, so an off-field detection looks like a legal board-edge unit; the 08-04 sessions have a different window aspect (5/30 hand reads fail on 173304).
