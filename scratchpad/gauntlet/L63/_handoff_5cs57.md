### §5cs.57 -- L63e (2026-09-06 02:2x-02:5x UTC): **S0 STEP 3 LAUNCHED -- the corpus rebuild for BOTH decks is running through the engine with per-play recording (icebow 619 replays on slot 37031, hogeq 296 on 37032), and the driver now takes `--crawl <deck>`.** Step 2b (own-click contract test on the 4 record.py sessions) dispatched to an agent in parallel. Owner rulings applied at bedtime: `research/CR_ENGINE_EXTRACTION_REVIEW.md` deleted (never tracked; archive note at HANDOFF_ARCHIVE:6722); README rewritten for the new architecture (`74a3e80`); a session cron (:23/:53 hourly, `/gauntlet`) added as a second wakeup trigger.

**A. Machine-sleep hypothesis for the three lost nights: (c) CONTRADICTED.** Windows System log, last 4 days: 0 Kernel-Power 42 (sleep), 0 wake, 0 boot/shutdown/unexpected-shutdown events; only 3 Kernel-General 1 (clock sync). The box was awake every night; the failures were on the harness/session side (consistent with an archived session still firing tonight). Mitigation = triggers that bypass ScheduleWakeup: the cron above, and background jobs whose completion re-invokes the loop.

**B. Tooling (a).** `research/sandbox_tools/replay_drive.py`: L61's per-play recording (`replay_drive_rec.py`, the batch_v2 producer) promoted in as `record_plays=` / `--record-plays` (FULL observation immediately before every driven play of both sides, with play_index/side/card/x/y and both players' hand/cycle/next/elixir); new `set_crawl(deck_or_path)` + `--crawl` (deck name -> `<deck>/data/royaleapi/crawl2`, or a dir containing battles.csv). `replay_batch.py`: `--crawl`, `--out`, `--record-every`, `--record-plays`. Defaults unchanged. Smoke (`L63/s0/smoke_v3.py`, `smoke_v3.out`): icebow `000YLY0JCPGL` with recording on -> final state_hash `d0874ff2026fa69e` == batch_v2's, 121 play_frames == batch_v2's 121, 364 compact frames, 10.8 s; first hogeq tag `02RY9GQVP09R` loads and drives (54 driven / 51 accepted, engine terminal at tick 3903 vs last real play 5501, crowns mismatch -- one replay, not a rate).
Trap: `set_crawl("hogeq")` first resolved to the repo's `hogeq/` directory because `Path("hogeq").exists()` is true from the repo root -> "no battles.csv"; fixed by testing for `battles.csv` in the path.

**C. Tag lists (a), `L63/s0/make_tags.py` -> `scratchpad/gauntlet/ext/corpus_v3/tags_{icebow,hogeq}.json`.** Fully positioned (every non-ability play has x/y): icebow 619 of 1,253 battles (6 partial, 628 unpositioned; 54,142 plays + 1,412 abilities in the set); hogeq 296 of 598 (4 partial, 298 unpositioned; 24,668 plays + 1,147 abilities). The old 268-replay set is a subset; EB-evo replays are included on purpose -- the engine refuses them cleanly before tick 1 ("no native evolution form 26000043", §5ay) and the batch records the refusal.

**D. Running (a at launch).** `corpus_v3/icebow/` (slot 37031, background PowerShell task) and `corpus_v3/hogeq/` (slot 37032, detached pid 25660), both `--record-every 20 --record-plays --determinism-every 10` (default), logs `corpus_v3/{icebow,hogeq}_run.log`, resumable via `summary.jsonl`. First rates: icebow ~13 s/replay, hogeq ~8 s/replay -> ETA icebow ~2 h, hogeq ~40 min. Expected size ~330 MB (batch_v2 was 74 MB for 211). Nothing existing is overwritten (`batch/`, `batch_v2/` untouched). Box at launch: free RAM 4.28 GB, CPU 54% (qemu idle load), python = owner's uvicorn only.

**E. Not established.** Hogeq acceptance / crowns-match rates (first ever, next loop). Whether recording changes throughput on the batch (13 s vs L61's rate -- not measured here). Step 2b numbers.

**F. Next.** Read both batches when they finish: acceptance, crowns_match, determinism SAME rate, refusals by reason, per deck; compare icebow's 211-replay overlap with batch_v2 hashes (recording + new driver must reproduce every one). Then the cleanup loop (ruling 6) and S1.
