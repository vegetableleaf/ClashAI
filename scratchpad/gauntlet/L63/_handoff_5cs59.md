### §5cs.59 -- L63g (2026-09-06 04:1x UTC): **FIRST HOGEQ ENGINE NUMBERS -- 241 of 296 positioned hogeq replays converted (52 EB-evo refusals, 3 deal-inference failures), 17,514 / 17,745 plays accepted = 98.7%, determinism 27/27 SAME, but crowns-match only 56.4% (icebow 76.2% on 421 so far) and the engine ends 36.5% of hogeq matches BEFORE the real last play (median 1,008 ticks early).** Icebow batch at 528/619, all 211 batch_v2 tags reproduce their hashes under the new driver (211/211). Both scheduled triggers (wakeup 23:39, cron 23:53 local) FAILED to fire while idle -- observed directly (`CronList` still listed the one-shot at 00:11); the owner's message was the check.

**A. Hogeq (a), `corpus_v3/hogeq/grade.json` (`L63/s0/grade_corpus.py`).** 296 tags: 241 converted, 55 refused (52 "no native evolution form 26000043" = Elite Barbarians evo, 3 "no (hand, queue) assignment reproduces the play sequence" -- a deal-inference failure the icebow set never showed; (b) an ability/evo-cycle interaction in those decks). Plays 17,745 driven, 17,514 accepted (98.7%), rejections native_rejected 160 / card_not_in_hand 71, invalid placements 0. Termination: logic clock 192, tiebreak 49. **Crowns match 136/241 = 56.4%**; among the 150 matches with ZERO rejected plays it is still only 98/150 = 65.3% (icebow: 252/312 = 80.8%) -> the divergence is not driven by rejected plays. Engine ended before the last real play in 88/241 = 36.5% (icebow 97/421 = 23.0%), median 1,008 ticks (50 s) early, p90 2,260. Mean 11.2 s/replay, 75.8 MB.
(b) Why hogeq diverges more: every card is driven at level 11 (the crawl has no levels) and hog-cycle outcomes are level-sensitive (hog-vs-tower and EQ-vs-building HP breakpoints), while X-Bow control games are decided by longer, more level-tolerant exchanges. Measurement that would settle it: re-drive the crowns-mismatch subset at level 14/15 (tournament vs ladder-max) and see whether the match rate moves. Not done; the corpus is used for STATES BEFORE EACH PRO PLAY, which drift only as far as the engine has diverged by that tick -- the 36.5% early endings say that drift is material for late-game hogeq states.

**B. Icebow so far (a, partial: 528/619 at 04:11 UTC).** 421 converted, 107 refused (103 EB-evo, 4 "battles.csv says N plays, plays_ext.csv has 2N" = duplicated rows for 4 tags -- crawler wrote them twice, TRAP: de-duplicate plays_ext rows by (tag, play_index) before S1 uses the csv directly). Accepted 35,161 / 35,456 = 99.2%, crowns match 321/421 = 76.2% (batch_v1's 77.7% on 211), determinism 43/43 SAME, **211/211 batch_v2 tags reproduce their state hashes** with recording on and the new `--crawl` driver.

**C. Trigger failure (a).** ScheduleWakeup set 23:08 for 23:39 local and the :53 cron both did not fire; at 00:11 `CronList` still showed the one-shot pending. The session's idle scheduler does not fire in this environment; task-completion notifications do (two subagents re-invoked the loop tonight). Standing mitigation: keep the engine batches as background TASKS so their exit re-invokes the loop; nothing else is available from inside.

**D. Next.** When the icebow batch exits: final icebow grade, then the cleanup loop (ruling 6) and the S1 dataset build from `corpus_v3` play_frames through `pipeline.obs_contract.from_engine` (both decks).
