### §5cs.95 -- L67a+b (2026-09-07 16:xx-18:xx UTC): **S4 opens with the live-shift cost measured: the v5 student loses 4.2 pp exact cell (20.9 -> 16.8), 12.4 pp card (63.0 -> 50.6) and +1.0 tile of placement error when its own training states are passed through the measured live degradation -- more than a corpus doubling buys. Then the owner's FirstLight_CR pointer was read in full: a strong OFFLINE agent built on 252k replays / 1,528-match PPO with NO live path and NO learning curve; "has not plateaued" is unsupported by the repo. One transferable, cheap test found: its public dataset holds ~2,000 exact-icebow sides (est.) = one more corpus doubling for v6**

**A. L67a: feature-parity / distribution-shift audit (a).** Survey of the live path (`scratchpad/gauntlet/L67/survey_live_path.md`): `play.py` still loads the old CNN `PolicyNet` on the 18x24 grid with the hardcoded `sigmoid(g1-g0) <= 0.25` gate; `obs_contract.LiveReads`/`from_live` exist with zero callers; every input needed for a live `BoardState` already exists in `play.py` scope; live deck identities are 10 keys (tesla/tesla_evo, knight/knight_evo split) vs the student's 8 base slots. Unobservable live: unit HP/level/evo/deploying/age, king HP, opponent elixir, timer.

Measurement: a **degraded twin of the v3 dataset** (`build_degraded.py`: every `BoardState` through `obs_contract.degrade` -- the measured live shift: recall/precision, 25% side unknown, no unit hp/deploy/age, integer elixir, no king hp/opp elixir, conf redrawn, 0.45-tile position noise -- before `to_tokens`; rows, split and targets byte-identical to the clean build, 13,761 val rows / 3,796 val plays; units 338,763 -> 326,681). v5lat s0/s1/s2 scored on both (`eval_clean_vs_degraded.jsonl`):

| VAL | exact cell % | card top-1 % | mean dist (tiles) | gate acc | cell NLL |
|---|---|---|---|---|---|
| clean v3 | 21.4 / 21.0 / 20.5 | 63.2 / 63.2 / 62.7 | 3.54 / 3.56 / 3.61 | .818 / .826 / .822 | 3.23 / 3.22 / 3.22 |
| degraded v3 (seed 0) | **16.7 / 16.6 / 17.1** | **52.4 / 48.3 / 51.1** | **4.53 / 4.56 / 4.64** | .762 / .737 / .738 | 3.62 / 3.63 / 3.64 |

Clean reproduces the S2 instrument (v5 on v3 VAL 20.93 +- 0.49, §5cs.86). The shift costs **-4.2 pp exact cell, -12.4 pp card, +1.0 tile, -8 pp gate**; card is hit hardest because integer elixir + missing king HP/opp elixir are card-choice features. What it does NOT establish (b): that `degrade()` matches the real detector (it encodes the L62l/§5bl measurements, not a fresh side-by-side); one degrade seed (the val set is 3,796 plays, so seed noise is small but unmeasured). Obvious next S4 experiment (one change): **train v5 with `degrade()` as augmentation**, 3 seeds, graded on clean AND degraded v3 VAL.

**B. L67b: FirstLight_CR (owner: "he has successfully made a model that has not plateaued, and we can learn from him").** Full review in `scratchpad/gauntlet/L67/firstlight_review.md`; clone at `scratchpad/gauntlet/L67/firstlight/` (480 MB, not committed). Facts (a): offline stack on a modified Null's Royale 15.535.13 client with a C++ engine probe and Python `reset`/`step`, headless resident multi-match; 252,238 RoyaleAPI replays / 17.8 M actions public on HF (same source as our crawl; trophies/dates stripped); V4 model 12,684,339 params (ours 1,264,680), universal 122-card encoder, LSTM, 18x32 = 576-cell placement grid, deploy-delay head, decision every 5 ticks; IL 8 GPUs, PPO 1,528 concurrent matches. `cr_native_env.py` docstring: "without screenshot interpretation or ADB touch injection" -- **there is no live path in the repo.**

The claim (c for the repo, b for the world): the repo contains **no learning curve** -- the only numbers are four rows of argmax win rates vs frozen opponents (General vs IL 73.30%; Hog specialist 2 vs IL/ActiveIL/General 89.87/86.97/88.03%, 760 games), which the author says "should not be treated as one continuous win-rate curve". Their IL-only model is "modest"; all gains are PPO vs frozen pools measured by win rate vs those pools -- and they hit our L62 failure exactly: the "fixed-IL detour" won 80.10% vs IL with **296 of 306 wins** against an opponent that played <= 3 cards; their final specialist is "more passive" than its predecessor while scoring higher. "Hall of Fame on an account" is author-reported with no code path. In pro agreement they measured nothing. So the repo shows a strong offline result bought with ~150x our replays and ~400x our engine throughput, not the absence of the plateau we measured.

**C. What transfers (ranked).** (A) **Dataset as corpus extension**: HF shard 0 (5,000 replays) has 41 exact-icebow sides / 10,000 (0.41%), 176 x-bow+tesla, 212 x-bow -> ~2,070 exact-icebow sides over 50.4 shards (b, one shard); v5 1,638 -> v6 ~3,700 = one doubling, S2 slope predicts +1.5 pp exact cell; caveats: skill tier unknown (89% Ranked, level 16 = 89.8% of slots), overlap with our crawl unknown, format conversion needed. Cost ~0.9 GB + converter + 3 seeds. (B) **Universal card encoder** -- the mechanism that makes 252k replays usable; a new model family, build only if (A) shows the slope holds on foreign data. (C) Their Apache-2.0 engine -- private-server client, owner ruling needed, not on the S4 path. (D) Deploy-delay head, gate ACT weight, sequence LSTM. (E) Nothing for S4.

**D. Traps.** Their `training_history.md` is the best external confirmation to date of §5cs.49-51 (win rate vs a frozen policy selects for passive-opponent exploitation) -- cite it when the reward-shaping question comes back.

**E. State.** Box idle (no training); VM off; nothing running. HF shard 0 + review + degraded-twin tooling in `scratchpad/gauntlet/L67/` (parquet, .npz and clone not committed). Gauntlet STOPPED for the owner's ruling on the next action (A vs S4 augment retrain; see the Discord questions).
