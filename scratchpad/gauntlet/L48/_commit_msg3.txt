L48c: crown count fixed in engine.py per owner ruling (A) -- crowns() returns 3 the moment the enemy king is dead (was: dead-tower count, so a king kill with a princess standing read 2); reporting AND reward (take_enemy_tower/lose_own_tower pay x2 on such a king-fall). Landed while c2r runs: its workers/eval envs were spawned once at start, so c2r trained wholly on the OLD count; first training effect = next run; a --resume after this commit would split c2r into two reward regimes (trap recorded). Checks: state test 0/1/2/3/3, outcomes unchanged by construction, crown_undercount n=12 engine == real-CR (-0.917), 165 unittests OK. Post-fix crown_delta is a new instrument -- never compare against pre-fix tables. HANDOFF 5cs.17 + ledger row; compound-drills answer recorded (instrument read next loop, not a training change now)
