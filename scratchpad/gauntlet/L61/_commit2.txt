model.py: per-card cell bias map (owner-approved Q1, 2026-09-05) -- PolicyNet.cell_bias_map [n_cards, n_cells] added to the cell logits before the tanh cap; pre-existing checkpoints load strictly with a zero map (load_state_dict / load_compat) and are bit-identical (armE snapshot: z/cards/cells equal, max|d| 0.0); the L60 bias-map heads converted to native checkpoints reproduce exactly (v1 15.44/16.24/15.14, v2 15.00/14.63/14.93); unittest 1336/1337, the 1 failure (test_xbow_into_push clamped-row) is pre-existing on HEAD. Crawler: expand mode (roster 50->150) + icebow-wins-first replay order (owner steer)
