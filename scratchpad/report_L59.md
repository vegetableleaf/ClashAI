**GAUNTLET loop 59** — radius-graded reward: step 1 wired + ARM G LAUNCHED (owner asleep; decisions on their behalf, HANDOFF §5cs.30)
**Did:** `geometry_reward.py` restricted per the L58 gate (P2 buildings-only, P7 off for swarm, PATH-based P1 with pull_ok); wired into `sim/env.py` as `w_time*P5 + w_geom*place*gate`, paid only when the placement part > 0; X-Bow offensive credit = `w_wincon*P6`; `enabled:false` reproduces HEAD's reward to 1e-9. Fixed a pre-existing rollout-worker config seam (workers re-read config.yaml from disk, so `--config` env keys and `--drill-only` never reached them; c2r unaffected). Commit 794d030. Launched **arm G 04:49** from c2r_best (sha verified), c2r's exact CLI + the ONE change `env.geometry.enabled: true`.
**Found (measured):** path P1 fires on 53.3% of pro Teslas (snapshot 40.9%, n=807); modal (9,21) beats the corner 0.543 vs 0.143. Hog-vs-Tesla scenario: graded pays +2.85..+3.0 at hog tile 14.7-17.1 and 0 at 18.3+, the old binary paid +1.0 at 18.3/20.7 and 0 in the pull window — disjoint windows. Baseline of c2r_best under the new reward (seed 0, 6×400 steps): tesla scored 24 paid 2, mean P1 0.039 (corner Tesla near-silent on the pull band); skeletons 53 scored 0 paid. Arm G: rail guard rescaled the cell head ×0.0430 (raw 105); early curve = c2r's resume shape (avg_rew −29/−34 vs c2r −31/−31 at 125/150 eps), 0.6 ep/s.
**Untested / parked:** pre-placed Tesla (hog still on the enemy half) earns 0 because the env sees no threat yet — needs a doctrine change, not bundled. Found, not fixed: `_trade_reward` keyed by `id(u)` → elixir_trade flips 1 step in 512 across processes.
**Means:** G is a clean one-change arm vs c2r. RAM forbids two arms (one 12-worker arm = 9.7 GB RSS, 1.1 GB free) → G+E and E QUEUED with yamls/launchers ready; decision after G's m5k/m10k reads. Arm E = entropy floor 0.05 (the rail guard already rescales the head, so a temperature arm is redundant).
**Next:** m5k read (~2.5 h): place_probe ×3 seeds, geo_ledger_probe ×2, gate_prior_probe — detached `arm_gates.py` posts it here even if my wakeup dies. Compare tesla mean P1 vs 0.039 and distinct cells; one seed = screen.
**Cost:** ~5 h wall (agent 58 min); running: arm G trainer (12 workers, cuda), ppo_watchdog, arm_gates.
