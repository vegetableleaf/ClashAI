ClashAI — Model & Architecture Change Log
=========================================

Tracks changes to the agents' models, learning architecture, and the
vision / training pipeline. Dates are local (file history + working notes).
Newest entries are at the bottom.


------------------------------------------------------------------------------
2026-07-23  —  Project inception: the scripted bot  (trol/)
------------------------------------------------------------------------------
- First bot: a rule-based agent with a DQN scaffold (trol/src/clashbot/).
- Screen capture, window calibration, and template-matching vision.
- Mouse controller, and a home/match/end state machine for navigating menus.
- DQN + online-learning scaffolding (dqn.py, learning.py, learning_state.json).
- Outcome: workable for scripted behaviour, but hard to make it genuinely
  "play". Led to the rethink below.


------------------------------------------------------------------------------
2026-07-24  —  New direction: the learning bot  (real/)
------------------------------------------------------------------------------
- Started a second agent built around IMITATION LEARNING first, then RL
  fine-tuning (real/src/clashrl/).
- Recording pipeline: capture the game region + mouse clicks to
  data/sessions/<timestamp>/ (record.py; video.mp4 + events.jsonl + meta.json).
- Config-driven capture / observation / action space (config.py, config.yaml).
- Match-outcome detection: read win/loss (crowns per team) off the results
  screen automatically (outcome.py).
- Card knowledge base imported from open card data (card_import.py, cards.yaml)
  so rewards/placement can reason about card stats and ranges.


------------------------------------------------------------------------------
2026-07-25 (morning)  —  RL infrastructure, the policy model, reward shaping
------------------------------------------------------------------------------
- Tower & King HP reader: a small CNN digit OCR (tower_hp.py + hp_digits.npz)
  reads tower/king HP off the screen to drive reward shaping.
- Action space + grid placement helper (actions.py); menu-navigation
  diagnostics (diagnose.py, the `diag` command).
- POLICY MODEL (model.py, PolicyNet): a shared CNN trunk over the downscaled
  arena image, concatenated with a hand multi-hot, feeding two heads:
    * card head  — which card IDENTITY to play (an evolved card is its own
      identity, since it plays differently — not the tray slot, which cycles)
    * cell head  — which grid cell to place it on
- Behaviour cloning (train_bc.py) pretrains the policy to copy your recorded
  plays (card head trained with a mask over the cards actually in hand).
- RL fine-tune (train_rl.py): a DQN wrapper adds a wait/play GATE head over the
  shared embedding; target network + replay; epsilon-greedy with elixir-aware
  exploration.
- Live RL environment (env.py) + reward shaping (reward.py):
    * +take enemy tower, -lose own tower, +win / -loss
    * defend-your-towers (tower-HP based), troop-defeat + patience
      (enemy-mass detection)
    * spell rewards scaled by the troops HIT / KILLED; Rocket auto-aims the
      weaker enemy princess tower
    * range-aware, center-biased DEFENSIVE placement for troop cards
    * tornado-onto-your-own-king "tank / activate" reward (king-HP based)


------------------------------------------------------------------------------
2026-07-25 (afternoon)  —  Deck change, defensive doctrine, "next card" input
------------------------------------------------------------------------------
- DECK CHANGE: replaced Evolved Musketeer with ROYAL DELIVERY. Deck identities
  went 10 -> 9; the policy is rebuilt on the next `train-bc`.
- Royal Delivery reward: a delayed area spell cast on your own half — rewarded
  for the enemy mass it lands on, and more for the mass it kills.
- King-activation gating: the "tornado onto your own king to tank/activate it"
  reward now only applies while BOTH your princess towers still stand (once one
  falls, the king is auto-active and the mechanic is off).
- DEFENSIVE DOCTRINE: any non-Rocket, non-cycle card played in the enemy half is
  penalised. The Rocket is the only sanctioned offensive card (weaker princess);
  cheap cycle cards (Ice Spirit, Skeletons) are exempt so they can cycle freely.
- ARCHITECTURE CHANGE — "next card" input: PolicyNet now also takes the NEXT
  (preview) card as a one-hot, via a new next_fc branch (shared embedding grew
  from 256+32 to 256+32+16). Threaded end-to-end through the live env, live play,
  dataset labeling (dataset.npz gains a `nexts` array), BC training, and the RL
  replay/DQN. This lets the policy plan its cycle (e.g. hold a cheap card to line
  up the counter that's coming). Requires re-labeling + retraining to take effect.
- Next-card VISION: the preview is a smaller, blue-tinted rendering that does NOT
  reliably match the in-hand card templates, so it uses its OWN template set
  under templates/next/. `hand-templates` now also extracts next-preview
  candidates for labeling; `verify --hand` overlays the next-card match.
- Evolved Tesla's next-preview template was recovered from existing recordings
  via its unique purple evolution frame (it needs a separate template from the
  regular Tesla).


------------------------------------------------------------------------------
2026-07-25 (evening)  —  Rocket -> Tornado combo
------------------------------------------------------------------------------
- Added a rocket+tornado combo to the reward shaping (env.py / config.yaml):
    * A tornado cast at the SAME spot right after a rocket that killed a clumped
      group is treated as one play.
    * It WAIVES the enemy-king penalty when that rocket hit a princess tower and
      killed >=2 medium troops (so a legit tower combo isn't discouraged).
    * It is rewarded for wiping a push (scaled by the enemy mass removed),
      anywhere on the board — not only next to a tower.
- Detection knobs: rewards.rocket_tornado_combo, env.combo_window_steps /
  combo_radius / combo_kill_min.
- Caveat: the training env evaluates each spell in a blocking window through its
  flight, so it can't cast the tornado mid-rocket-flight; the reward teaches the
  rocket->tornado PATTERN + the troops the rocket killed. The live bot performs
  the actual sub-second timing.
- (tooling, same day) train-rl now records a FIXED-LENGTH training timelapse
  (data/timelapses/timelapse_<date_time>.mp4 -- one timestamped file per run):
  however long training runs, the mp4 is always timelapse_seconds long at
  timelapse_fps (default 30 s @ 30 fps = 900 frames). Frames are thinned in memory
  during the run and resampled to exactly the target count on save. Not a
  model/reward change. See clashrl/timelapse.py + config train.timelapse*.
- Reduced defensive hesitancy. The RL play/wait balance had learned to WAIT (the
  small patience reward + avoiding play penalties made sitting still locally safe),
  and troop placements picked in the enemy half couldn't deploy at all (the card
  tap selects but the arena tap can't place -> "shuffling"). Fixes: penalise idling
  while a real enemy push is on the board (rewards.idle_penalty, env.threat_mass);
  clamp TROOP placements to your half of the river so they actually deploy, spells
  still target anywhere (action.deploy_top + ActionSpace.deploy_clamp, applied in
  env + play); and make training exploration commit to plays more (lower
  train.explore_wait_prob / min_play_elixir).
- Placement rule refined + elixir awareness (ARCHITECTURE CHANGE). (1) Only ROCKET
  and TORNADO may be cast ANYWHERE; every other card -- troops, buildings, AND
  Royal Delivery -- is now clamped to your half of the river (deploy_clamp keys off
  rocket/tornado, no longer off "is a spell"). (2) The elixir counter is now a
  POLICY INPUT: the normalized current elixir (0-1, read off the bar, ported from
  trol) is fed to the network via a new elixir_fc branch alongside the arena image,
  hand, and next card -- so the bot can learn elixir conservation. Threaded through
  the env, live play, dataset labeling, and BC + RL training. (3) Sitting on a FULL
  elixir bar with an enemy push and playing nothing now costs elixir_waste_penalty.
  The elixir input changes the model, so it needs a re-label + retrain
  (label --all -> train-bc -> train-rl).
- Faster match transitions + rocket-cycle incentives. (1) nav.menu_delay 1.0 -> 0.5
  so the bot doesn't idle on the home screen after a match before hitting Battle
  (the delay was the post-menu-tap sleep). (2) Rocket-cycle shaping: launching a
  rocket at an enemy princess tower now earns rocket_tower_reward (+ the troop combo
  if it also catches a clump), and playing a cheap card while the rocket ISN'T in
  hand earns cycle_reward -- together these teach the deck's cycle-to-rocket plan
  (chip the tower when safe; defend + the rocket->tornado combo when a big push
  comes) and give the still-hesitant policy clear positive things to do. Reward-only
  (no retrain needed beyond the pending elixir-input rebuild).
- 1v1 migration. (1) The Battle button now auto-queues, so the PARTY / quick-match
  step was removed from both nav loops (env.reset + play). (2) Deck card levels are
  recorded per card in cards.yaml (1v1 levels vary: e.g. Evo Tesla 14, Rocket 13,
  Skeletons 13, Ice Wizard 11); CardDB scales the level-11 base stats to the
  configured level (deck() + a level() accessor). Card stats don't drive the vision
  / RL rewards, so this is informational -- it doesn't change how the bot plays.
  (3) Towers recalibrated for 1v1 from a real recording: nudged the tower anchors
  and repositioned the princess HP-number boxes, and set hp_full to a level-14
  princess. IMPORTANT: the digit OCR (hp_digits.npz) was trained on 2v2 level-11
  towers and MISREADS the 1v1 (level 14-15) tower HP, so hp_reward is DISABLED for
  now to avoid injecting false chip rewards -- re-enable after retraining the digit
  model on 1v1 samples. Tower destruction (crowns) + win/loss still drive the reward.
- 1v1 match-end loop + tower reader RETRAINED (defence/offence chip re-enabled).
  (1) The 1v1 results screen puts "Play Again" (bottom-left) and "OK" (bottom-right)
  in new spots; the bot now taps PLAY AGAIN by default (buttons.play_again) from both
  nav loops (env.reset + play) and the RL results-read, so it re-queues straight into
  the next match without a detour through HOME.
  (2) Retrained the tower-HP digit CNN for 1v1. First fixed the HP-number boxes: the
  enemy boxes were clipping the last digit, and the "white" digits on YOUR blue bars
  are blue-tinted, so tower_hp._digit_band's strict mask was relaxed to catch them
  (the main white mask was already fine -- do NOT loosen it). Then labelled 47 HP
  strips from a real 1v1 recording (tools/hp_ocr/labeled_digits_1v1.npz) and retrained
  hp_digits.npz (2v2 base for digit coverage + 1v1 upweighted x3; ~97% per-digit). All
  four princess towers now read correctly -- verified end-to-end with clean, monotonic
  chip timelines (e.g. an enemy princess 4032->3499->2966->2553->1727).
  (3) Re-enabled hp_reward, and gave YOUR princess towers their own full HP
  (env.my_hp_full 4424, ~level 15) separate from the enemy's (env.hp_full 4032,
  ~level 14) so defence chip is seeded + normalised on the correct baseline. The
  reward change takes effect on the next train-rl.
- Reactive-play PERCEPTION + replay analysis (foresight groundwork). New enemy-threat
  reader (clashrl/threats.py): from a frame it classifies the incoming push by COLOUR
  (saturated green = Goblins, since this deck plays none; purple; neutral), SIZE (one
  big unit vs a swarm), COUNT, LANE and DEPTH over an "engagement zone" (midfield + your
  half, with the enemy back line, YOUR towers and the arena border masked out so only a
  real push registers), plus APPROACH SPEED and a best-effort PROJECTILE-IN-FLIGHT
  detector (a small blob tracked over several frames on a fast, near-straight arc toward
  a tower -- e.g. a thrown Goblin Barrel). It exposes a 14-value feature vector (for the
  policy, next phase) and a readable label. New `analyze` command (clashrl/analyze.py):
  for every card play in your recordings it reads the threat in the window before the
  play and aggregates WHICH CARD you play against WHICH threat type, and WHERE (on /
  behind the threat, your king, the enemy tower, ...), writing data/analysis/patterns.json
  + a report. On the latest session it recovered real habits: rocket -> enemy princess,
  ice wizard placed BEHIND the push, tesla ON the push, tornado onto your king, cheap
  cycle cards on/behind the threat; ~76% of plays are reactions. New `verify --threats`
  overlay to eyeball + tune the reader. ANALYSIS ONLY -- no model/reward change yet. Next:
  feed the threat vector into PolicyNet + reward the key foresight counters (Royal Delivery
  onto a threatened tower; tornado pulling a landed goblin swarm to your king) -- that step
  needs a re-label + retrain.
- Reactive-play PHASE 2 -- enemy-threat vector as a POLICY INPUT + foresight reward shaping.
  (1) ARCHITECTURE: PolicyNet gains a threat branch (threat_fc; embed 256+32+16+8 -> +16 =
  328). The 14-value threat vector (colour / size / count / lane / depth / speed + projectile,
  from clashrl.threats) is fed alongside the image / hand / next-card / elixir and threaded
  end-to-end: the labeler writes a `threats` array into dataset.npz, BC trains on it, and the
  live env + live play + the RL replay/DQN all carry it -- so behaviour-cloning learns your
  card-vs-threat habits and RL keeps + sharpens them. Back-compatible (old datasets/checkpoints
  default to zero threats). (2) FORESIGHT REWARDS (env): Royal Delivery dropped ON a landing
  goblin (green) swarm -- or onto the tower a projectile (Goblin Barrel) is arcing toward --
  earns rewards.threat_counter_delivery; a Tornado by YOUR king while a goblin swarm is present
  earns rewards.threat_tornado_pull (pull them into the king to tank + kill). Both are keyed off
  the live ThreatTracker read at action-selection time. Takes effect after the FULL rebuild:
  `label --all` -> `train-bc` -> (delete data/policy_rl.pt) -> `train-rl`. NOTE: the threat
  thresholds are tuned to the current (clean) arena; recordings on the older red-tint arena give
  noisier threat features.
- Reactive-play: behind-the-bridge SIEGE detection + counter. Buildings (X-Bow, Mortar) and
  troops (Princess) chip your towers from the ENEMY side WITHOUT crossing, so the your-half
  signals (my_side_mass / depth) read them as low threat. threats.py now measures `behind_mass`
  (enemy red in the band just above the river) and flags `siege` when that mass PERSISTS and does
  NOT advance/cross into your half over several reads -- a walking push advances (its centroid
  moves toward you) and crosses (my_side_mass rises), breaking the streak, while a siege sits so
  the streak builds. Wired in: the threat vector grows THREAT_DIM 14 -> 16 (behind_mass + siege)
  so the policy conditions on it (BC-loader pads/truncates old-width arrays for safety); the
  `analyze` report + patterns.json + `verify --threats` overlay surface it (a "siege-<lane>" label
  + an orange band/marker); and env rewards rocketing a sitting siege with rewards.siege_counter
  (the rocket is the only card that crosses the river to reach it). Takes effect after the full
  rebuild (label --all -> train-bc -> del data/policy_rl.pt -> train-rl). NOTE: the siege
  thresholds (_BEHIND_MIN / _SIEGE_*) in threats.py are best-effort; tune with verify --threats.
- GRADUAL own-tower penalty (better per-play credit). Losing HP on YOUR princess tower is now
  penalised PROPORTIONALLY as it's chipped -- accumulating up to |rewards.lose_own_tower| (-3)
  per tower and topped up to exactly that on destruction -- instead of a flat -3 only when the
  tower fell. So a play that concedes chip is penalised at that moment (easier for the DQN's
  per-step credit assignment to attribute), and a fully-lost princess still totals -3.
  tower_hp.TowerHpTracker owns the gradual charge (_update_my, capped + accumulated in
  _my_applied, in absolute units -- no longer x hp_scale x hp_defense_scale, which is retired);
  env tops it up on a princess fall via on_my_tower_destroyed(i); reward.TowerTracker now only
  emits the flat lose penalty for the KING (princesses are gradual, so no double-count). Verified
  a fully-lost princess = exactly -3.000 (gradual), and a burst-without-HP-reads = -3.0 (fallback).
  Reward change -> effective on the next train-rl.
- Rocket as an ELIXIR INVESTMENT (deferred / conditional chip). A rocket's chip reward (its
  rewards.rocket_tower_reward + the enemy-tower HP chip it deals) is no longer paid on impact --
  it is DEFERRED and only paid out if a SUCCESSFUL DEFENCE follows. If within
  env.rocket_defense_window (3) steps your towers take MORE HP damage than the rocket dealt the
  enemy tower (compared in HP: your towers' HP lost vs the rocket's measured enemy-tower chip),
  or a princess falls, the chip is WITHHELD and an extra rewards.bad_rocket_penalty (-3) is
  applied -- so a rocket that trades DOWN (you lose more than you gain) is punished instead of
  rewarded, and chip damage only pays when the defence holds. Implemented in env (a pending-rocket
  state that folds the rocket's delayed HP chip + measured tower damage -- tower_hp.last_enemy_chip
  / last_enemy_frac -- into the deferred amount, then resolves: pay on a good trade, withhold +
  penalise on own-damage > rocket-damage / a felled princess). The learner is a per-step DQN, so
  this delayed reward is bootstrapped back onto the rocket via the value function.
  Reward change -> effective on the next train-rl.


------------------------------------------------------------------------------
2026-07-26  —  Anti-hesitation: potential-based troop reward + reweighting
------------------------------------------------------------------------------
- BUG: a hesitant agent could END a match NET POSITIVE (+7.7) while being
  three-crowned (a loss). Root cause was reward-SHAPE, not just magnitudes: the
  per-step troop-defeat reward used a RECTIFIED delta -- max(0, prev_mass - cur_mass)
  -- so it paid out on every DOWN-swing of the noisy enemy-mass signal (troops
  ebbing / dying at the towers, occlusion) but never charged the matching UP-swings.
  Summed over a ~150-step match this drift, plus a standing +patience per idle step,
  banked more than the -10 loss. It fired even on pure no-op steps, so DOING NOTHING
  was literally profitable.
- FIX (env.py): the troop term is now POTENTIAL-BASED -- the SIGNED per-step change
  in on-board enemy mass, clipped to +/-defeat_cap x rewards.troop_defeat. Being
  symmetric it telescopes over the match (board starts ~empty and, in a loss, ends
  non-empty -> the term is <= ~0 for a loss), so idling can't farm it and a growing
  push is penalised immediately. Removed the asymmetric clean_kill_bonus x2 from this
  term (it re-opened the same farm); killing troops before they reach your tower is
  still favoured because tower chip + tower loss are the (larger) negatives.
- REWEIGHTING (config.yaml) to guarantee a loss is always net-negative and acting
  beats stalling: patience 0.02 -> 0.0 (a quiet board is NEUTRAL, not rewarded);
  idle_penalty -0.3 -> -0.5 (bigger than any single-step positive shaping);
  loss -10 -> -12 (dominates residual shaping); env.threat_mass 0.10 -> 0.06 (the
  idle penalty engages on smaller pushes); clean_kill_bonus 2.0 -> 1.0 (deprecated).
- Net effect: a three-crown loss now nets clearly negative (loss + own-tower
  penalties + a <=0 telescoped troop term), and the standing incentive to sit idle is
  gone. Reward-shaping change -> effective on the next train-rl (consider deleting
  data/policy_rl.pt so the wait/play gate re-learns from the BC start).


------------------------------------------------------------------------------
2026-07-26  —  Defensive cards wouldn't deploy: placed ON your king tower
------------------------------------------------------------------------------
- BUG: Tesla / Ice Wizard / Ronin (the defensive_kind cards) "tapped rapidly but
  never played", even at full elixir. Cause: the centre-placement change (2026-07-25)
  places these at nx~=0.48 (dead centre), depth = front + range behind the enemy front.
  For anything but a shallow push that depth is ny 0.67-0.81 -- exactly YOUR king
  tower's footprint (my_towers king anchor 0.48,0.72). You can't deploy a troop on your
  own tower, so the arena tap is a no-op: the card stays selected and the bot shuffles.
  Spells (rocket/tornado) and the cheap cyclers aren't centre-overridden, so they played
  fine -- which is why only these three failed. Confirmed on a real frame + placement math.
- FIX (keeps the "central grass" intent the user drew, just off the tower):
  * reward.defensive_cell caps the depth at env.defense_back_limit (0.58) -> defenders land
    on the central grass IN FRONT of the king (the red-box area), never on it.
  * actions.deploy_clamp now also KING-AVOIDS: any troop cell landing on your king footprint
    (my_towers king anchor +/- action.king_avoid_half [0.09,0.06]) is pulled to the row just
    in front -- a general guard covering the no-threat / model-cell path too.
  * action.deploy_top 0.47 -> 0.44: opens the central grass just below the river (the humans'
    own deploy range starts there) so more of that space is usable.
  * back_limit threaded through env + play defense_params.
- Validated: for side +/-1 and fronts 0.46-0.80, all three cards now place at
  (0.44/0.56, 0.46-0.54) -- central grass, off the king (0.66-0.78); a dead-on-king cell is
  pulled to (0.44,0.54). MECHANICAL fix (env + live play) -- effective immediately, no retrain.


------------------------------------------------------------------------------
2026-07-26  —  Save the Tesla for the enemy's win condition (tower-targeter)
------------------------------------------------------------------------------
- Goal: if the enemy runs a WIN CONDITION (a troop that heads straight for your tower and
  ignores your troops -- Hog / Ram / Balloon / Giant / Royal Giant / Miner ...), HOLD the
  Tesla and only deploy it to defend that win condition; don't spend it early. If the enemy
  has NO win condition, the Tesla plays defensively as normal.
- PERCEPTION (threats.py ThreatTracker): a "win condition" is detected BEHAVIOURALLY (no
  enemy-card ID is possible from the vision) -- an enemy unit that PENETRATES deep into your
  half (depth >= env.wc_depth, 0 at the river .. 1 at your king) OR CROSSES fast toward it
  (speed >= env.wc_fast_speed), with a real unit present (my_side_mass >= env.wc_min_mass).
  Per match: seeing one latches `enemy_has_wc`; `wc_active` tracks one being on the board
  (wc_confirm reads to arm, wc_release quiet reads to clear -- hysteresis). New helper
  `should_hold_tesla() = enemy_has_wc and not wc_active`.
- BEHAVIOUR: live play (play.py) MASKS the Tesla out of the card choice while should_hold_tesla
  (it waits if the Tesla is the only card) -> the Tesla is saved. Training (env.py) REWARD-SHAPES
  the same policy: +rewards.wc_tesla_defend for playing Tesla against an ACTIVE win condition,
  rewards.tesla_hold_penalty for spending it early when one is known but off the board.
- Honest limits (told user): behavioural, so the FIRST win condition of a match can't be known
  in advance (it latches on first sighting, then the Tesla is saved for the recurring cycles);
  a deep undefended non-WC push can latch it too (still sensible -- Tesla answers deep threats).
  Thresholds are coarse pixel proxies -- calibrate wc_depth / wc_fast_speed with `verify --threats`
  (a red "WIN-COND" label marks a per-frame detection).
- config: rewards.wc_tesla_defend / tesla_hold_penalty; env.wc_depth / wc_fast_speed / wc_min_mass
  / wc_confirm / wc_release. Live gate is immediate; the reward shaping needs a train-rl run.


------------------------------------------------------------------------------
2026-07-26  —  Centre defensive placement is a RECOMMENDATION, not a mandate
------------------------------------------------------------------------------
- User: the centre placement is NOT a must -- it's a recommended default; in some cases other
  placements are better (block a lane so a troop can't reach your tower; drop a Ronin up front
  to catch an enemy ranged unit off guard). Let the model choose.
- Before: the range-aware centre spot was a HARD OVERRIDE -- env.step and play.act REPLACED the
  model's chosen cell for Tesla / Ice Wizard / Ronin whenever a threat was up, so the model could
  never place them anywhere else.
- Now: the override is REMOVED (env + live play). The model's own placement stands (still passed
  through deploy_clamp -> your half + off your king, unchanged). Centre is kept as a SOFT DEFAULT
  via a small reward: env._recommended_defense_cell (was _place_defensive) computes the suggested
  centre spot and env._defense_placement_reward gives rewards.defense_center_bonus (0.15) when the
  model places within a cell of it -- and NOTHING otherwise (no penalty for placing elsewhere). A
  better lane block / forward play earns more through the troop-defeat + Tesla-kill rewards, so the
  model deviates from centre when that's stronger.
- play.py: dropped the defensive-override branch + its now-dead setup (CardDB/_db, defensive_kind,
  defense_params, threat_min_frac, and the defensive_cell/threat_front/threat_side imports); kept
  the Tesla win-condition hold gate and the rocket weaker-princess aim. config: rewards.defense_center_bonus.
- Live effect is immediate (the override is gone); the centre-default nudge shapes the policy on
  the next train-rl. The earlier deploy_clamp king-avoidance still keeps every placement deployable.


------------------------------------------------------------------------------
2026-07-26  —  Object detection (YOLO11) — Stage 1: dataset bootstrap
------------------------------------------------------------------------------
- Direction: replace the coarse red-blob threat proxies with real per-unit OBJECT DETECTION so
  the policy has a robust "what troop/spell is where" read (the model was "guessing every turn").
  Chosen: Ultralytics YOLO11 (over vanilla DETR -- far more trainable on limited, custom data;
  RT-DETR is a drop-in later since the dataset format is identical). ~55-73 class taxonomy.
- Stage 1 (this change) = the DATA pipeline, since labels are the real bottleneck:
  * config/detect_classes.yaml -- ordered class taxonomy (your deck + common ladder cards +
    spells), team-agnostic (team inferred from colour at inference).
  * clashrl/detect.py + `run.py autolabel [--all] [--preview]` -- builds an Ultralytics dataset
    under data/detect (images/ + labels/ + data.yaml). AUTO-boxes YOUR OWN just-spawned troops
    for free: reuses label._extract_plays (your select+placement clicks) -> a known class at a
    known tap; the unit that appears is localised by a frame-diff around the tap (unions near-tap
    blobs so a Skeletons group boxes as one). Also exports extra active in-match frames (sampled
    AROUND your plays -- detect_state is a weak in-match positive, reads most in-match frames as
    UNKNOWN, so it's NOT used) with empty labels for you to hand-label the ENEMY on.
  * tools/detect/train.py -- Ultralytics trainer (yolo11n, imgsz 960, HSV jitter so blue-troop
    labels transfer to the red enemy side, no h-flip since lanes are asymmetric). Needs
    `pip install ultralytics`.
  * config.yaml detect: section (spawn_delay/search_radius/diff_thresh/general_per_session/...).
- VALIDATED on a real session: 26 plays -> 19 accurate own-troop boxes (Ronin/Ice Wizard/Ice
  Spirit/Skeletons) + 40 frames to hand-label; data.yaml valid (nc + names). Preview overlay
  confirmed the auto boxes land on the spawned unit. data/detect is gitignored (regenerable).
- CAVEAT (told user): auto labels only cover YOUR troops and a frame also holds enemy + other
  own units -- must hand-label those on each frame (a few hundred) before training, else the
  detector learns them as background. NEXT stages: (2) hand-label seed + active-learning + colour
  transfer for the enemy; (3) detections -> semantic-map channels into PolicyNet; (4) retrain BC+RL.
- FOLLOWUP: switched the default detector to RT-DETR-L (user: more accurate than YOLO11, worth the
  slower speed). tools/detect/train.py now auto-selects RTDETR vs YOLO by the --model name (default
  rtdetr-l.pt; --model yolo11s.pt for YOLO), auto-batch (-1) since RT-DETR-L is heavier, 120 epochs.
  Dataset / autolabel / taxonomy are detector-agnostic -- unchanged.


------------------------------------------------------------------------------
2026-07-26 (afternoon)  —  Object detection Stage 1b: full taxonomy + Label Studio hand-label loop
------------------------------------------------------------------------------
- Since the enemy side has to be HAND-LABELLED (the auto-labeler only boxes YOUR just-spawned
  troops), this pass built out the taxonomy + the labelling round-trip so the detector can see any
  ladder card, not just your deck.
- CLASS TAXONOMY grew 73 -> 217 (config/detect_classes.yaml), team-agnostic, keyed to the card KB:
    * +the full Clash Royale card roster (73 -> 131) auto-added from config/cards.yaml (deck indices
      preserved so the auto-labeler's own-troop classes don't shift).
    * +all 41 EVOLUTION variants (<base>_evo, 131 -> 172); the auto-labeler now prefers the exact
      evolved key (tesla_evo etc.) when you played the evolved card.
    * +14 HERO variants (<base>_hero, user-provided post-cutoff cards, 172 -> 186).
    * +9 champion ABILITY activations (<champion>_ability, distinct visual effects, 186 -> 195).
    * +22 spell AOE classes (<spell>_aoe, the target/impact circle for every base spell, 195 -> 217)
      so the detector can localise a spell's landing, not just its card.
- LABEL STUDIO round-trip (labels are the real bottleneck, so this is the workflow):
    * autolabel now also emits Label Studio helpers -- classes.txt + a ready-to-paste labelling-config
      XML listing every class -- so hand-labelling is set up in one paste.
    * `detect-frames` (add_frames): a NON-DESTRUCTIVE command to add more in-match frames from a
      session into data/detect for hand-labelling (only NEW indices; existing images/splits untouched).
    * `detect-import` (detect_import): ingest an unzipped Label Studio YOLO export -> remaps classes
      BY NAME (order-proof, so LS reordering can't corrupt indices), rebuilds the train/val split and
      data.yaml. Validated on a synthetic reordered export.
    * `detect-preview` (detect_preview): run the trained detector on RANDOM, UNBIASED in-match frames
      (pooled uniformly across ALL sessions, not clustered on your plays) and save annotated images to
      gauge real accuracy.
    * chore: stopped tracking Label Studio exports (annotation_export*/) and detector runs/ (regenerable
      / large); untracked an accidentally-committed export.
- troop_hp.py HP-BAR reader scaffold: reads a troop's health from the green fill fraction of the bar
  above a detected box (groundwork for per-unit HP once the detector is live). Scaffold only -- not
  wired into rewards yet.
- Still Stage 1 (perception plumbing) -- no model/reward change to the policy yet.


------------------------------------------------------------------------------
2026-07-26 (evening)  —  Identity-grounded threat features (card_threat / card-roles)
------------------------------------------------------------------------------
- Direction: once the detector names the enemy card, the coarse colour/size threat proxies (threats.py)
  can be REPLACED by what the card actually IS. New clashrl/card_threat.py resolves a detector class ->
  a KB THREAT PROFILE (win_condition / siege / spell / air / swarm / tank + elixir / hp / dps /
  tower-damage), the foundation for feeding identity-grounded threat features into PolicyNet.
- `card-roles` command (card_threat.roles_report): prints each detector class's resolved role + stats
  as a review report (`--all` / `--card <name>`), so the KB tagging can be audited.
- KB TAGGING (config/cards.yaml + card_threat.py):
    * pre-filled 17 win-condition / siege strategic tags + a singular royal_recruit; card_threat gained
      an explicit `siege` flag and flag-based `flying` (air).
    * tagged three_musketeers as a win condition.
    * surfaced `death_damage` as a first-class threat attribute/role (Balloon's death bomb etc.).
    * added a `melee` / `attack_range` attribute and curated range (melee / short / long) for ~70 troops
      -- for the planned MELEE tornado-pull (only melee units can be pulled onto your king to be tanked).
    * curated the 5 clarified special troops (furnace / goblin_machine / goblin_demolisher / goblinstein
      / spirit_empress) from the user's reach info; only terry / wizard_trio remain uncurated.
- `verify --threats` gained a CARD-IDENTITY layer: it now runs the detector + card_threat and draws each
  enemy unit's resolved KB attributes (win_condition / siege / air / ... + elixir / hp / dps / tower-dmg)
  on the frame -- the calibration view for the upcoming threat-vector integration.
- ANALYSIS / PERCEPTION groundwork only -- the identity features are not fed into PolicyNet or the rewards
  yet (that's the next stage, and needs a re-label + retrain).


------------------------------------------------------------------------------
2026-07-26 (evening)  —  Reward: close random-spell loopholes
------------------------------------------------------------------------------
- Tightened the anti-random-spell shaping (config.yaml / reward): spell-WHIFF penalty -0.5 -> -1.5 and
  the enemy-KING-damage penalty -4 -> -6, so casting into empty ground or clipping the king is clearly
  bad. Also started penalising ROYAL DELIVERY whiffs (it was previously exempt), and GATED the
  own-king tornado "tank / activate" reward on a REAL push being present (so it can't be farmed by
  tornado-ing your own king with nothing incoming).
- Reward-shaping change -> effective on the next train-rl.


------------------------------------------------------------------------------
2026-07-27  —  Live-play robustness (watchdog + runtime log) + remote monitoring
------------------------------------------------------------------------------
- Long unattended train-rl / play runs were silently stalling on unexpected screens; this pass makes
  the live loop self-heal and observable.
- STUCK-SCREEN WATCHDOG (play.py): an UNRECOGNISED post-match popup (chest / level-up / season / offer)
  no longer hangs navigation -- after play.stuck_timeout (25 s) on an unknown screen the bot taps to
  dismiss it; also self-heals a hung matchmaking search.
- RUNTIME LOG + crash guard (play.py): each run writes a per-run log (data/play_<ts>.log) of the states
  it sees and the taps it makes, and the whole loop is wrapped in try/except so an exception LOGS A
  TRACEBACK and keeps navigating instead of silently stopping on the results screen.
- REMOTE MONITORING (clashrl/monitor.py, config monitor.*): optional Discord screenshot alerts during
  train-rl. A dedicated DAEMON THREAD posts a screenshot of the game window every monitor.interval_min
  (default 30, min 1) EVEN IF training itself hangs, so you can tell remotely whether a run is
  progressing or stuck. The webhook URL is a SECRET -- read from the monitor.webhook_env env var
  (CLASHRL_DISCORD_WEBHOOK, preferred) or the git-ignored monitor.webhook_file (data/discord_webhook.txt);
  if neither is set, monitoring is simply off. Never committed.
- MECHANICAL / tooling -- no model or reward change.


------------------------------------------------------------------------------
2026-07-27  —  Stage 4 groundwork: external-replay strategy mining (deferred, detector-gated)
------------------------------------------------------------------------------
- Motivation (user): the user isn't a pro, so BC from only their own play caps out. Idea: have the
  bot learn from strong ROCKET-CYCLE players' replays -- their defensive placements, positive elixir
  trades, and when they rocket the enemy tower. Assessed frame-by-frame BC from online footage as the
  WRONG vehicle (hard blockers: external video has NO mouse log for labels; the whole vision stack --
  card templates / elixir / tower / HP OCR -- is calibrated to the user's ONE rendering; pixel-BC
  suffers domain shift across renderings; and a different "rocket cycle" deck doesn't match this deck's
  card-identity action space). Chosen instead: STRATEGY DISTILLATION -- mine footage into compact,
  DECK-AGNOSTIC priors that reward shaping leans on (the channel by which strategy already enters this
  bot), not pixel cloning.
- Built the groundwork now (the parts that don't need the detector), so it's ready to point at footage
  the moment Stage 2/3 lands (clashrl/replay_mine.py, `run.py mine-replays`):
    * DETECTOR PLUG-POINT: BoardDetector / load_detector wrap the SAME Ultralytics YOLO/RT-DETR weights
      as detect-preview (runs/detect/*/weights/best.pt); team (mine/enemy) inferred from colour tint.
      `.available` is False with no weights, so the miner reports READINESS + next steps instead of
      crashing (verified: it prints the Stage-3 dependency and staging instructions).
    * PRIOR SCHEMA (versioned JSON, schema_version 1): StrategyPriors aggregates placements
      [threat_bucket][card][zone], elixir TRADES [threat_bucket][card] (n / delta-sum / positive), and
      ROCKET-TOWER launch conditions (elixir histogram + board quiet-vs-pressured). Threat is bucketed
      DECK-AGNOSTICALLY via card_threat (win_condition / siege / tank / air / swarm / spell / support),
      which is what lets the priors transfer across decks.
    * REWARD HOOKS (pure, ready for env at Stage 4): placement_bonus / trade_bonus / rocket_opportunity
      each return ~[0,1]; empty priors (no data/file) return 0, so they're safe by construction. GATED
      behind rewards.strategy_prior_scale (default 0.0 = OFF) -- nothing affects training until opted in.
    * MINER: mine_replays runs the detector over videos in replay_mine.replays_dir (data/replays),
      recovers each learn-side play (a new own-side unit = a play; coarse, documented), keeps only cards
      in YOUR deck, and writes data/analysis/strategy_priors.json + strategy_records.json.
- config: new replay_mine.* (replays_dir / frame_stride / detect_conf / learn_side) + rewards.
  strategy_prior_scale. NOT wired into env.py yet (the reward hook needs the detector's enemy-identity
  read IN the live env, which is Stage 3) -- deliberately left as ready-to-call functions, mirroring how
  card_threat.py stages the detector bridge without touching the policy.
- ANALYSIS-ONLY groundwork -- no model/reward/behaviour change. ACTIVATION (Stage 4): train the detector
  (Stage 2/3) -> drop pro rocket-cycle replays into data/replays -> run.py mine-replays -> set
  rewards.strategy_prior_scale > 0 and wire the hooks into env's per-step reward -> train-rl. CAVEATS
  told to user: elixir/tower reads assume the user's rendering (external footage may misread them), and
  only shared-deck cards yield usable placement/trade priors.


------------------------------------------------------------------------------
2026-07-27  —  Defensive kill-credit: lower Tesla, tiny credit for all defenders, direct+indirect kills
------------------------------------------------------------------------------
- BUG/CONCERN (user): the Tesla kill reward (mass drop near the Tesla x rewards.tesla_kill) was strong
  enough that a Tesla catching a Skeletons swarm could FARM enough kill points to make a THREE-CROWN LOSS
  net-positive -- the same "shaping outfarms the -loss" failure mode as the 07-26 troop-reward fix, but
  local to the Tesla.
- FIX (env.py + config): (1) LOWERED rewards.tesla_kill 3.0 -> 1.5. (2) Added a per-placement ANTI-FARM
  CAP rewards.defense_kill_cap (1.5): a single defensive placement can accumulate at most that much kill
  credit over its window, so a swarm-catch is bounded (verified: a Tesla on a big swarm totals exactly
  1.5, and 4 maxed defenders = 6.0 < the -12 loss, so a loss stays net-negative).
- FEATURE (user): TINY kill credit for the OTHER defensive cards too -- Ice Wizard / Ice Spirit /
  Skeletons / Ronin -- via rewards.defense_kill (0.5). The single-Tesla tracker (env._tesla /
  _tesla_reward) is generalised to a LIST of tracked defenders (env._defenders / _defense_kill_reward):
  each placement of a defensive card starts its own kill tracker (weight = tesla_kill for the Tesla,
  defense_kill for the rest), reusing the same tesla_radius / tesla_track_steps.
- INDIRECT KILLS (user): the credit is the drop in enemy (red) mass NEAR the card, which already counts a
  troop that dies there no matter WHAT killed it -- so a defender that blocks/distracts a troop long
  enough that YOUR PRINCESS TOWER finishes it off near the card is credited the kill, exactly as asked
  (added to the Tesla and to every other defender by construction). And because a troop that instead
  reaches your tower triggers the (separate) GRADUAL own-tower penalty, the NET only pays for a kill that
  actually SAVED the tower -- no explicit tower-damage gate needed.
- DEFERRED (user, post-Stage-3): scale the reward by REAL per-unit damage dealt instead of the red-mass
  proxy -- needs the object detector / per-unit HP (troop_hp.py). Noted in config + the code docstring.
- config: rewards.tesla_kill 3.0->1.5, new rewards.defense_kill / defense_kill_cap; env kill-credit
  comment generalised. Reward-shaping change -> effective on the next train-rl.


------------------------------------------------------------------------------
2026-07-27  —  Detector: switch default RT-DETR-L -> YOLO11m (memory + small-data fit)
------------------------------------------------------------------------------
- Context: RT-DETR-L hit MEMORY pressure in training. Decision (weighed for THIS project): switch the
  detector default to YOLO11. The usual "RT-DETR = more accurate but heavier" tradeoff does NOT hold here.
- Reasoning:
  * Benchmark parity: YOLO11l ~= RT-DETR-L on COCO (~53 mAP), YOLO11x exceeds it -- little/no ceiling lost
    even before data effects (the earlier "RT-DETR more accurate" note was the big-data intuition).
  * Data regime is decisive: the dataset is small + label-bottlenecked with 217 classes; DETR-family
    transformers are data-hungry (learn queries + bipartite matching without CNN priors) and typically
    UNDERperform a CNN on sparse custom data -- the transformer's edge needs volume we don't have.
  * Small objects need a high imgsz (~960): RT-DETR-L's memory forced cutting imgsz/batch, which
    disproportionately kills small-unit recall (skeletons / spirits) -- exactly the defensive reads the
    policy needs. YOLO11 keeps 960 affordable, so the memory fix and the accuracy need align.
  * Scalable knob: Ultralytics RT-DETR ships only L/X (can't scale DOWN); YOLO11 is n/s/m/l/x, so VRAM can
    be dialed precisely while holding imgsz.
  * Live budget: the detector runs during live play + live RL alongside capture + the policy; YOLO11 is
    faster + lighter and competes less for VRAM during train-rl.
- Steelman for RT-DETR (revisit later): NMS-free decoding helps dense/occluded crowds and it has a higher
  ceiling -- but only with a large, densely-labelled dataset. Not the current regime; zero-cost to revisit
  since the YOLO-format labels are detector-agnostic.
- Change: tools/detect/train.py default --model rtdetr-l.pt -> YOLO11 (per user, set to yolo11x.pt -- the
  LARGEST YOLO11, highest ceiling, to test). CAVEAT: yolo11x is bigger than rtdetr-l, so at imgsz 960
  auto-batch may shrink the batch or OOM -- step down --model yolo11l/m/s.pt for less VRAM / faster training;
  --model rtdetr-l.pt to go back. Dataset / taxonomy / autolabel / detect-preview pipeline unchanged. (User
  exports annotations as YOLO, so model choice is free.)


------------------------------------------------------------------------------
2026-07-27  —  Nav robustness: shared MenuNavigator fixes the match-end hang (train-rl + play)
------------------------------------------------------------------------------
- BUG: train-rl kept HANGING on the results screen. env.reset() (what train-rl uses for nav) and play had
  each grown their OWN copy of the HOME / MATCH_END / UNKNOWN tap logic and DRIFTED -- play got a stuck
  watchdog, env did not, so env stalled on unexpected post-match screens.
- FIX: new clashrl/nav.py MenuNavigator -- the SINGLE source of truth for match-to-match nav, used by BOTH
  env.reset() and play. Robustness over a fixed-coordinate tap: (1) buttons LOCATED by template (Battle via
  the home template; Play Again via optional buttons.play_again_template) so a tap follows the button if the
  layout/window shifts; (2) a MATCH_END not advancing after nav.match_end_timeout (6 s) ESCALATES to the OK
  button -- alternating the CENTERED "OK only" variant and the bottom-right OK (buttons.results_ok_center /
  results_ok) -> HOME, where located Battle re-queues; (3) an unrecognised popup past play.stuck_timeout is
  tapped to dismiss; (4) every action logged to data/nav_<label>_<ts>.log.
- play.py rewired onto MenuNavigator too, and its play/wait gate synced with train-rl (same gate + elixir-
  COST affordability check) so live behaviour matches training. config: buttons.results_ok_center,
  nav.match_end_timeout. MECHANICAL (nav + live) -- no model/reward change.


------------------------------------------------------------------------------
2026-07-27  —  Reward-farming loopholes closed + shaping capped below a loss (env.py / config)
------------------------------------------------------------------------------
- Theme: a run of fixes so shaping can NEVER sum past the terminal loss (the recurring "a hesitant/farming
  agent ends a LOST match net-positive" failure). Reward-shaping changes -> effective on the next train-rl.
- SELF-EXPLOSION FARM (bug): a rocket / Royal Delivery counted its OWN fireball as enemy mass killed, so the
  model farmed +size x spell_troop_damage by rocketing its OWN empty side. Fix (reward): measure enemy mass
  BEFORE impact (excludes the explosion flash), so an empty-ground rocket now scores a WHIFF, not a kill.
- BAD-PLACEMENT PENALTIES (new env._placement_penalty, judged on the pre-action frame):
    * WRONG LANE (rewards.wrong_lane_penalty -1.5): ANY card committed to the lane OPPOSITE the enemy push.
      Cheap cyclers pay only wrong_lane_cheap_frac (0.3x); a ROCKET is EXEMPT when it chips the opposite
      princess OR erases a big clump there (rocket_clear_min 0.10 mass = a split-lane wipe).
    * ROYAL DELIVERY on the ENEMY half (rd_enemy_half_penalty -0.75): RD can only cast on your half; the
      deploy clamp hides the misaim, so the ATTEMPT (raw pre-clamp cell) is penalised.
    * RANGED ON TOP (ranged_ontop_penalty -0.75): Tesla / Ice Wizard dropped on a troop instead of a kiting
      gap behind it (ranged_ontop_radius / _size).
    * BACK CORNER (back_corner_penalty -0.25): a card dumped deep against an edge (back_corner_x/_y) -- the
      cheap-card / RD corner spam.
    * LONE TORNADO CHIP (tornado_chip_penalty -1.0): a tornado alone on the enemy princess -- it's not a chip
      tool; a real rocket->tornado combo is exempt.
    * PREMATURE DEFENSE (premature_defense_penalty -0.5): a defender / RD played on a QUIET board (no enemy
      troops) -- nothing to react to.
- TESLA BIMODAL-PLACEMENT FIX (env._building_place_reward): the Tesla was landing at the BRIDGE (bypassed /
  focused) or DUMPED BEHIND your towers (can't reach the push). Now a building is rewarded for a MODERATE
  depth in FRONT of your towers (building_front_y..building_back_y) AND toward CENTRE (building_center_reward
  x centrality, building_center_span), and penalised outside that band (building_misplace_penalty). Buildings
  are also excluded from the ranged-on-top penalty. Only rewards central when there's a real push.
- PER-MATCH SHAPING CAP (env._bonus / rewards.shaping_match_cap 8.0): the repeatable one-sided bonuses
  (defense-kill + cycle + foresight counters) are capped in TOTAL per match, so no amount of them can out-sum
  a loss. Penalties pass through uncapped.
- REWEIGHT: loss -12 -> -15; rocket_tornado_combo 15 -> 8 (two combos can't out-sum a loss). Rocket
  elixir-investment penalty extended to ANY wasted rocket -- one that neither chipped a tower nor erased a
  clump AND is followed by >= rocket_bad_min_hp (700) of own-tower HP loss is a bad investment.


------------------------------------------------------------------------------
2026-07-27  —  Learner: vanilla DQN -> Double DQN + gradient clipping (train_rl.py)
------------------------------------------------------------------------------
- Kept DQN over PPO (verdict: off-policy REPLAY is the sample-efficiency win that matters for one slow live
  match at a time). But upgraded vanilla -> DOUBLE DQN: the ONLINE net selects the greedy next action
  (gate / card / cell), the TARGET net only EVALUATES it. Vanilla took max() from the target for both, which
  systematically OVERESTIMATES Q -- the value-inflation bias underneath the reward-farming this project keeps
  fighting; Double DQN de-biases the bootstrap without touching replay.
- Added GRADIENT CLIPPING (train.grad_clip 10.0): cap the global grad-norm per update to stabilise the noisy
  live-match TD targets. ARCHITECTURE/learning change -> effective on the next train-rl.


------------------------------------------------------------------------------
2026-07-27  —  Crown under-count fixed on fast king-kills (reward.py) + verify --all
------------------------------------------------------------------------------
- BUG: a king fall ENDS the match instantly, so the king's destruction latch (needs `confirm` consecutive
  "gone" reads) often couldn't finish before the frame cut to the results screen -- a 3-crown win read as 2,
  muddying the terminal reward / W-L. FIX: TowerTracker.king_trending_down() also counts a king that was
  TRENDING destroyed (>=1 "gone" read) at match end, recovering the near-miss so a 3-crown finish counts as 3.
- Tooling: `verify --all` runs the overlay over EVERY session (cli.py flag).


------------------------------------------------------------------------------
2026-07-28  —  Monitor: periodic video CLIP instead of a screenshot
------------------------------------------------------------------------------
- clashrl/monitor.py now posts a startup SCREENSHOT, then a short VIDEO CLIP of the game window every
  monitor.interval_min (was a still each interval) -- a clip shows whether the bot is actually PLAYING vs
  frozen. New config: monitor.clip_seconds (30), clip_fps (10), clip_scale (0.5 -- downscaled to stay under
  Discord's ~8 MB webhook limit). _post_screenshot generalised to _post_file (image OR video). Tooling only.


------------------------------------------------------------------------------
2026-07-28  —  DECK SWITCH: Rocket Cycle -> Miner X-Bow Control (Phase 1: identities + placement + doctrine)
------------------------------------------------------------------------------
- User is switching decks (a pure rocket cycle is too much for a from-scratch agent). New deck (Classic
  1v1, ALL cards level 11): Evo Tesla, Miner, X-Bow, Ice Wizard, Skeletons, Electro Spirit, Rocket, Royal
  Delivery. Shared defensive core (Tesla / Ice Wizard / Skeletons / Rocket / Royal Delivery) is kept;
  Electro Spirit replaces Ice Spirit, Miner replaces Ronin, X-Bow + Miner are new, and TORNADO is gone.
- DOCTRINE INVERSION (the crux): the old shaping was "defend only, never play forward, rocket is the sole
  offense". This deck's win condition is the X-Bow placed FORWARD to lock the enemy tower, plus the Miner
  deployed ANYWHERE -- so several offensive penalties had to be re-scoped, not just card lists swapped.
- Confirmed with user: Classic 1v1 (all level 11); ONLY Evo Tesla; X-Bow deploys on YOUR side just behind
  the bridge (never across), two modes (forward win condition / back-centre defence); the X-Bow's ~11.5-tile
  RANGE must gate the offensive reward so it isn't placed too far to reach the tower; 2x/3x elixir will be
  read TIME-based (match clock); phased -- core first.
- PHASE 1 (this change) -- the core so the user can record + BC + RL:
  * cards.yaml: new deck (all L11, evo tesla). Miner / X-Bow already curated (win conditions); Electro
    Spirit present. `run.py cards` verified all 8 resolve (avg elixir 3.38).
  * Card identities (env.py + play.py): dropped tornado/ronin/ice_spirit; added miner_ids, xbow_ids;
    cheap = electro_spirit + skeletons; ranged = ice_wizard (Tesla stays a building); blocker = skeletons /
    electro_spirit / miner; defensive_kind = tesla / ice_wizard. building_ids = Tesla ONLY (X-Bow is a
    building too but gets its OWN shaping, not the Tesla intercept-zone reward).
  * Placement: anywhere_ids = ROCKET + MINER (was rocket + tornado) -- Miner can chip the enemy tower /
    drop behind an enemy unit; X-Bow stays clamped to your half (its forward spot near the bridge is inside
    it). deploy_clamp docstring updated.
  * NEW rewards (env, all routed through the per-match shaping cap _bonus): _xbow_place_reward -- +xbow_wc_reward
    when the X-Bow is WITHIN env.xbow_range (0.36 ~= 11.5 tiles) of the nearer enemy princess (a real forward
    win condition), +xbow_defense_reward for a back-centre defensive X-Bow, xbow_misplace_penalty for a
    forward X-Bow out of tower range; _miner_reward -- +miner_chip_reward for a Miner on/near an enemy princess.
  * Doctrine exemptions: Miner + X-Bow are exempt from the enemy-half offensive_penalty and the wrong-lane
    penalty (a Miner goes anywhere; an opposite-lane X-Bow to punish an overcommit is a real play). They're
    also NOT in reactive_ids, so placing them on a quiet board is fine (proactive win-condition play).
  * config: rewards.xbow_wc_reward / xbow_defense_reward / xbow_misplace_penalty / miner_chip_reward;
    env.xbow_range / xbow_defense_y. README deck blurb updated.
- YOUR SIDE (told user): the BC dataset is deck-specific -- RE-RECORD from scratch with the new deck (old
  recordings teach dead cards), rebuild card templates (hand-templates -> rename to the new keys -> verify
  --hand), re-verify tower calibration, then label --all -> train-bc -> train-rl.
- PHASE 2 (deferred): time-based 2x/3x elixir reader gating the rocket-cycle-on-tower; Miner tank-for-X-Bow
  + snipe-support-behind-a-tank; X-Bow-protect micro; Electro Spirit stun/reset. Mechanical/reward change
  -> effective on the next train-rl (after the re-record + retrain).


------------------------------------------------------------------------------
2026-07-28  —  Miner X-Bow: reward-parameter audit + X-Bow opposite-lane nuance
------------------------------------------------------------------------------
- User: an opposite-lane X-Bow is a legit PUNISH of an over-committed / FAILED push, but it should STILL be
  penalised if it leaves a live enemy push undefendable. Phase 1 had made the X-Bow fully wrong-lane-exempt
  -- too permissive.
- FIX (env._placement_penalty): the Miner stays fully wrong-lane-exempt (it deploys ANYWHERE). The X-Bow is
  now NUANCED -- an opposite-lane X-Bow is free when the near-lane push is SPENT, but if a REAL push is still
  live (enemy_mass >= env.threat_mass) it pays rewards.xbow_wrong_lane_frac (0.6) x wrong_lane_penalty (a
  REDUCED share -- the X-Bow does apply its own counter-pressure). config: rewards.xbow_wrong_lane_frac.
- FULL REWARD AUDIT for the new deck (decisions recorded in real/DECK_SWITCH.md §3): KEPT all the shared
  defensive / spell / anti-farm shaping; ADDED the X-Bow (win-condition / defence / misplace) + Miner-chip
  rewards (Phase 1) + the X-Bow lane nuance (this change); marked the Tornado/Ronin knobs INACTIVE in config
  (king_tank_reward, threat_tornado_pull, rocket_tornado_combo, tornado_chip_penalty, env.combo_* -- their
  code paths are already inert since tornado_ids is empty, but the comments now say so, so they aren't
  re-tuned); left the offense WEIGHTS (xbow_wc vs rocket_tower vs miner_chip) to be re-tuned from train-rl
  data rather than guessed. Reward change -> next train-rl.
- HANDOFF: added real/DECK_SWITCH.md -- the single ordered runbook (user record/template/train steps + the
  remaining AI/code work + the reward audit + the roadmap), linked from real/README.md, so the training-
  device agent has everything in order. Waiting on the user to re-record before Phase 2.


------------------------------------------------------------------------------
2026-07-28  —  DESIGN NOTE (Stage 5, post-Stage-3): learned dynamics / world model
------------------------------------------------------------------------------
- User idea (via a programmer friend): add a PREDICTIVE component -- a transformer that predicts how the
  game state progresses from the previous state + action (e.g. a placed troop advances toward its nearest
  target along the shortest path) -- trained first, then RL on top. Verdict: promising and well-aligned with
  this project's core bottleneck (live matches are slow + un-parallelisable, so sample efficiency is king),
  but it is a POST-STAGE-3 endeavour and should be its own stage.
- WHY post-Stage-3: a world model that predicts "the troop moves toward the nearest target" needs a
  STRUCTURED state -- a list of entities {type, x, y, hp, team} -- to predict over. That is exactly what the
  object detector (Stage 2/3) produces. Predicting raw PIXELS instead is far harder and less useful, and the
  coarse red-blob threat proxies aren't enough. So the detector is the prerequisite.
- WHAT it is: model-based RL (Dreamer / MuZero / Trajectory-Transformer family). "Train the predictor, then
  RL" == learn a dynamics model, then use it to (a) pretrain the policy's representation, (b) plan short
  look-aheads (roll imagined futures to score candidate actions), and/or (c) generate imagined rollouts to
  train the policy with far fewer real matches.
- HOW (phased, after Stage 3): collect (entity_state, action, next_entity_state) transitions from recordings
  + RL rollouts -> train a transformer/graph dynamics model over the entity SET (predict next positions,
  spawns, deaths, HP, tower chip) -> validate SHORT-horizon accuracy (error compounds over long rollouts) ->
  wire in as representation pretraining first, then optional planning / imagined rollouts, then RL.
- HONEST CAVEATS (told user): significant research scope; CR dynamics are complex (targeting, collisions,
  spell timing) and PARTIALLY OBSERVED (enemy elixir/hand hidden), so keep horizons short and expect the
  model to be a helper, not an oracle. Sequence it AFTER: the Miner X-Bow BC+RL baseline works AND the
  detector reliably produces entity states. Tracked as Stage 5 in real/DECK_SWITCH.md.


------------------------------------------------------------------------------
2026-07-28  —  Headless SIMULATOR + train-sim: no-vision RL from scratch (clashrl/sim)
------------------------------------------------------------------------------
- User: can we simulate thousands of matches so the model gains experience WITHOUT the vision stack?
  Verdict: yes -- and it's arguably the highest-leverage lever (RL one live match at a time is the wrong
  regime). Built a MEDIUM-fidelity, stat-driven, headless engine (not a faithful CR clone) as a new track.
- clashrl/sim/ (new package):
  * engine.py -- SimEngine: normalized-coord arena (enemy top / you bottom, river 0.5, bridges 0.25/0.75),
    entities built FROM the card KB (build_spec: hp / dps / reach / speed / count / flags), elixir economy
    (single->double->triple by match time), lane movement with BRIDGE crossing, nearest-target acquisition
    (building-only -> tower; siege -> nearest foe in sight else tower; else nearest), DPS combat + splash,
    princess/king towers that fire + king activation on a princess fall, area spells (rocket instant / RD
    delayed, reduced tower damage), win/loss/draw + crowns + regulation/overtime.
  * opponents.py -- scripted archetypes (hog-cycle / beatdown / control / siege) with representative KB
    decks; defend the deepest threat, else pressure per style. make_opponent() samples one per match.
  * env.py -- SimMatchEnv: mirrors LiveMatchEnv's interface so the SAME PolicyNet trains here. Renders a
    crude synthetic top-down obs at observation.arena_size (towers + unit blobs, blue=you/red=enemy) +
    hand/next/elixir/threat vectors (threat is a best-effort approximation of threats.vector()); reward
    from GROUND TRUTH using the SAME config weights (tower chip, crowns, potential-based troop shaping,
    win/loss) + the deck's placement doctrine (X-Bow in tower range, Miner on the enemy tower), under the
    shaping_match_cap. Agent decides every sim.agent_dt (1s) over sim.sub_dt (0.1s) physics ticks.
- train_sim.py + `run.py train-sim [--matches N] [--resume] [--seed]`: trains the SAME PolicyNet/DQN
  (reuses train_rl._build_net + the Double-DQN update) against the sim, FROM SCRATCH (no BC needed), fast,
  no capture. Rolling win-rate/avg-reward summaries every sim.log_every_matches; checkpoints data/
  policy_sim.pt every sim.save_every_matches; Ctrl+C saves + exits. Warm-start live RL by copying
  policy_sim.pt -> policy.pt (dims match), then train-rl to fine-tune (close the sim-to-real gap).
- config: new sim.* (agent_dt/sub_dt, tower HP/dps/range, regulation/overtime, siege_sight, epsilon
  schedule, log/save cadence) + train.sim_checkpoint.
- READINESS DECISION: YES -- start sim training now. Validated end-to-end here (no torch on this box, but
  the engine/env ran 8 random-agent matches: correct termination, sane rewards -- losses ~-25, a win +10.4,
  ~7/8 losses for a random policy = a learnable gradient). obs/vector contract matches the model (incl. the
  9 identities from Tesla's evo/normal cycle). It can run in PARALLEL with the user's recording.
- HONEST LIMITS (told user): medium fidelity (no exact pathfinding/aggro/pushback/champions/evolutions; a
  synthetic top-down obs), so a sim-trained policy is a strategic/elixir/timing PRIOR to fine-tune live, not
  a finished bot. Grow fidelity + add self-play later. This sim is also a HAND-CODED world model -- the
  twin of the Stage-5 learned one.


------------------------------------------------------------------------------
2026-07-28  —  Sim upgrade: vectorized train-sim + meta-deck opponents (top-200 via CR API)
------------------------------------------------------------------------------
- VECTORIZED training (`run.py train-sim --envs K`, config sim.envs default 8): K match instances step
  together and feed ONE learner -- a single BATCHED network forward picks all K actions and one optimiser
  step runs per tick over a shared replay. Better GPU utilisation + sample diversity + throughput; K
  matches progress at once. Single-process (GIL), so the K engine steps are serial -> this is VECTORISED
  (batched), not multi-core parallelism; the engine is cheap so the win is GPU amortisation. (Multiprocess
  actors could saturate cores later.) train_sim.py rewritten around a per-env state loop + choose_batch().
- META-DECK OPPONENTS (replaces the 4 hardcoded archetypes): opponents now sample from a POOL of real
  meta decks and pilot each by its inferred STYLE (cycle / control / beatdown / siege, from avg elixir +
  siege/tank/win-condition flags). Files: sim/meta_decks.py (load_meta_decks + classify_style), rewired
  sim/opponents.py (ScriptedBot takes cards+style; make_opponent samples weighted by popularity),
  SimMatchEnv loads the pool once. config/meta_decks.yaml ships a curated 23-deck fallback (all keys
  validated against the KB -> all 23 resolve; styles: 8 control / 7 cycle / 5 beatdown / 3 siege).
- LIVE TOP-200 IMPORT (`run.py decks-import`, deck_import.py): pulls the current meta from the OFFICIAL
  Clash Royale API (sanctioned, no scraping) -- top global players -> their battle logs -> 8-card decks
  both sides used -> tallied -> top N -> written to config/meta_decks.yaml. Needs a FREE token (env
  CLASHRL_CR_API_TOKEN from developer.clashroyale.com, IP-locked); card names mapped to KB keys (evolutions
  fold to base). Best-effort (rankings endpoint/season can move); without a token or on error the curated
  pool is kept. config: sim.envs / meta_decks_file / api_token_env / api_base.
- VALIDATED here (no torch on this box): compile clean, both YAMLs parse, all 23 curated decks resolve +
  classify, and the vectorized multi-env loop ran (4 envs -> 12 matches, sane outcomes). The torch training
  path mirrors the proven train_rl update (batched); the live API import can't be tested without a token.
  Ready to run on the GPU/torch training device -- `train-sim --envs 16` (+ optional decks-import).


------------------------------------------------------------------------------
2026-07-28  —  Repo: real/ -> icebow/ rename; DECK TEMPLATES recorded; nav-log + monitor fixes
------------------------------------------------------------------------------
- RENAMED the learning-bot project folder real/ -> icebow/ (git-tracked files only). NOTE: the git-ignored
  data/ does NOT move with `git mv`, so on a device that has recordings/labels the old real/data/ (incl.
  data/detect annotations) is left behind -- move it: `Move-Item real\data icebow\data`. All paths in newer
  entries below use icebow/...; older entries' "real/..." are historical.
- DECK TEMPLATES BUILT + committed: the user recorded Miner X-Bow play and built the hand-recognition
  templates -- 213 PNGs now tracked under icebow/templates/cards/ + next/ (x_bow / miner / electro_spirit /
  royal_delivery / tesla + evo / ice_wizard / skeletons, next-card previews, and a play_again button). So
  `label` -> `train-bc` is unblocked for the new deck.
- nav: menu-navigation logs now go to a data/nav/ subfolder (tidier than loose data/nav_*.log).
- monitor: fixed a NameError from a half-reverted merge (the 30s periodic-CLIP feature was restored).
- Housekeeping only -- no model/reward change.


------------------------------------------------------------------------------
2026-07-28  —  Miner X-Bow Phase 2: reward tuning from recorded play
------------------------------------------------------------------------------
- With real recordings in hand, tightened the Miner X-Bow doctrine (all in icebow/src/clashrl/env.py +
  config). Reward-shaping changes -> effective on the next train-rl.
- MINER discipline: dropping the Miner on the enemy KING tower ACTIVATES it early (wakes their defence for
  the match) -> rewards.miner_king_penalty (-2.0). A Miner DUMPED in your own backfield (at/below
  env.own_backfield_y 0.62) with no push to defend does nothing -> rewards.miner_backfield_penalty (-0.75;
  defending a real push there is exempt). Conversely the Miner now EARNS the defensive kill-credit when it
  tanks/snipes -- troops that die near it on defence are credited (same mechanic as the other defenders).
- X-BOW: dropping the X-Bow right ONTO an oncoming push (a red blob within env.xbow_ontop_radius 0.10) gets
  it swarmed before it chips -> rewards.xbow_exposed_penalty (-1.0). A "defensive" X-Bow shoved too deep
  (past env.xbow_defense_back 0.70, i.e. onto your king) is a misplace, not defence.
- STOP KING-TOWER HUDDLING + engage CLOSE pushes: once a push's deepest troop reaches your towers
  (env.close_defense_y 0.58), a mobile defender stops kiting and is placed BETWEEN the push and your king to
  body-block it (env.defense_close_side_bias 0.13 clears the king footprint, defense_close_depth 0.03 sits
  it just behind the enemy front) -- instead of far-dumping defenders on/behind the king.
- CYCLE / SETUP allowances: Electro Spirit + Skeletons are now EXEMPT from back_corner_penalty (the back
  corner is exactly where you cycle them); and the premature-defense penalty is WAIVED at/above
  env.defense_setup_elixir (9) -- pre-placing a defender on a FULL bar is an elixir-efficient setup, not a
  premature waste.


------------------------------------------------------------------------------
2026-07-28  —  Sim: opponent pool 23 -> 100 meta decks; train-bc --init (sim prior + BC)
------------------------------------------------------------------------------
- Expanded the curated opponent pool (icebow/config/meta_decks.yaml) from 23 to 100 distinct, KB-valid meta
  decks so the sim faces far more varied, realistic matchups; `run.py decks-import` now defaults to the
  top-100.
- train-bc gained `--init`: warm-start behaviour cloning FROM an existing checkpoint (e.g. the sim prior
  data/policy_sim.pt) so BC COMBINES the simulator-learned policy with your recorded play, instead of
  starting BC from scratch -- the sim-prior -> BC -> RL pipeline the sim was built to feed.


------------------------------------------------------------------------------
2026-07-29  —  DESIGN NOTE (Stage 4 sub-task): BC refinement from pro icebow replays (YouTube)
------------------------------------------------------------------------------
- User wants to refine BC by cloning strong icebow play mined from YouTube / public media (not a pro, no
  pro contacts). Verdict: viable IN PRINCIPLE now that the deck matches the action space, but a LATER-STAGE
  endeavour -- GATED ON THE DETECTOR (Stage 3): a video has no mouse log, so plays must be RECOVERED
  (detect a friendly unit spawn -> its card + cell + board state).
- YOUTUBE CAVEAT (the crux): public footage is a DIFFERENT RENDERING than the user's Google Play window
  (resolution / skin / overlays / facecam), so the hand/elixir/tower readers misread it AND image-BC clones
  pixels->action -> raw-pixel BC from YouTube transfers POORLY. Do NOT feed YouTube pixels into the image
  policy. Instead (post-detector): detector -> structured play -> ONE of (i) CANONICAL RE-RENDER into the
  bot's own synthetic top-down (like the sim render) then BC; (ii) train on the detector's SEMANTIC obs
  (Stage-3 shared representation, rendering-independent); or (iii) use as REWARD PRIORS.
- Plan: extend replay_mine.py to emit a train-bc-loadable dataset.npz from detector-recovered plays. Queued
  as a Stage-4 sub-task in icebow/DECK_SWITCH.md. START only after the detector is trained + wired. No code
  change yet.


------------------------------------------------------------------------------
2026-07-29  —  Deck tweak: Electro Spirit -> The Log (icebow)
------------------------------------------------------------------------------
- User swapped Electro Spirit out for THE LOG (a 2-elixir Legendary SPELL). DB check: kind spell, elixir 2,
  damage 266, crown_tower_damage 40 (poor tower chip). Behaviour (curated into cards.yaml, absent from the
  stats import): a ROLLING log -- a lane-wide LINE (not a circle), GROUND ONLY (no air), with KNOCKBACK /
  charge-reset; CAST POINT is YOUR half only but it ROLLS to the enemy side. User's value order: best when
  allied defenders are locked onto an enemy and a knockback/reset buys them time, or to hit a large ground
  group; bridge cycle-chip ONLY if Skeletons isn't available to cycle AND there's nothing to Log AND you're
  at full elixir (avoid leaking).
- CHANGES: cards.yaml deck electro_spirit -> the_log (+ a curated the_log entry: kind spell, attacks
  [ground], flags [knockback, splash], range long, cast your-half). env.py identities: the_log joins
  cheap_ids (2-elixir cycler) and spell_ids (auto, kind==spell); REMOVED from blocker_ids (a spell can't
  tank/block, unlike Electro Spirit). NOT in anywhere_ids -> deploy-clamped to your half (correct: cast
  point is your side; it rolls forward). Existing spell rewards already handle it (troops-in-blast reward /
  empty-ground whiff penalty). Deck avg elixir 3.38 -> 3.5. Verified: `run.py cards` shows The Log 2e.
- USER records tonight to build the the_log template (hand-templates -> rename a crop to the_log.png ->
  verify --hand) so hand recognition + the elixir-cost link work. Opponent meta decks that run electro_spirit
  are left as-is (still a valid opponent card).
- SIM auto-catches-up: the simulator is DECK-DRIVEN (reads cards.yaml deck), so on `git pull` the sim agent
  uses The Log, not Electro Spirit -- verified (agent deck has the_log as a spell; no electro_spirit anywhere
  in sim/). NO engine parameter change was needed for the swap itself. (The Log's sim FIDELITY was then
  upgraded from a point-blast to a rolling ground-only knockback corridor -- see the 2026-07-29 entry below.)
- PHASE 2 (deferred): Log-specific shaping -- the knockback/reset-to-buy-defenders-time bonus and the
  CONDITIONAL bridge cycle-chip (only under the 3 conditions above). Reward change -> next train-rl/BC.


------------------------------------------------------------------------------
2026-07-29  —  The Log sim fidelity: rolling ground-only knockback corridor
------------------------------------------------------------------------------
- Upgraded the sim's The Log from a generic point-blast to a ROLLING CORRIDOR (sim/engine.py _resolve_roll):
  a forward strip from the cast point (toward the enemy -- up for team 0, down for team 1), width
  _LOG_ROLL_HALFW (0.07) x length _LOG_ROLL_LEN (0.30), that damages GROUND troops only (skips air) and
  KNOCKS them back _LOG_KNOCKBACK (0.05) in the roll direction; if the corridor reaches an enemy tower it
  chips it (40 crown dmg = the bridge cycle-chip). Data-driven: any spell with the `knockback` flag rolls;
  ground-only from attacks:[ground]. Verified: a knight took 266 + was pushed up the arena; air minions untouched.


------------------------------------------------------------------------------
2026-07-29  —  DESIGN NOTE: per-arena background calibration (vision robustness)
------------------------------------------------------------------------------
- User: progressing ARENAS changes the background tile COLOUR, which throws off the colour-threshold vision
  (enemy=red / ally=blue troop masks in threats.py + reward._red_mask, the "quiet board" baseline, tower
  reads). Every arena change, the bot should re-examine the BACKGROUND so it separates arena from troops.
- APPROACH (design; needs multi-arena footage to build + validate, so NOT built yet): at match start (the
  arena is briefly empty), sample a BACKGROUND REFERENCE of the arena region (the ground tiles); detect
  foreground (troops/spells) as pixels that DEVIATE from that reference (background subtraction) COMBINED
  with the existing SATURATED team tints (troops are saturated red/blue; ground isn't). Re-sample on an
  arena change (a background-signature shift) or per match; optionally identify WHICH arena by its signature
  and load per-arena thresholds. Touches threats.py + reward.py + vision; calibrate with verify --threats /
  --towers on recordings from EACH arena. GATED on recording across the arenas you play -- a background
  model can't be built or verified without that footage. Queued as a vision-robustness task.


------------------------------------------------------------------------------
2026-07-29  —  The Log: its own reward path (knockback/reset + conditional cycle-chip)
------------------------------------------------------------------------------
- Phase-2 Log shaping (env.py + config): The Log is scored as itself, NOT as a rocket/tornado. New
  rewards.log_reset_reward (0.3) when the Log rolls THROUGH a real enemy push -- the knockback/reset buys
  the defenders time, so it's valued EVEN WITHOUT a kill (plus size x spell_hit for troops it clears). New
  rewards.log_whiff (-0.3, much softer than the -1.5 rocket whiff since it's only 2 elixir) for a Log that
  hits nothing -- but WAIVED to 0 for the justified last-resort cycle (no Skeletons in hand, board quiet,
  near-full bar). Reward change -> next train-rl/BC.


------------------------------------------------------------------------------
2026-07-29  —  Sim: higher-fidelity engine + real card levels + Evo Tesla stun
------------------------------------------------------------------------------
- Big sim/engine.py fidelity pass so the sim behaves closer to CR: AGGRO / TARGET-LOCK (a troop notices +
  commits to an enemy unit within env.sim.sight_range 0.12, with a 1.8x leash, else it marches the tower);
  DEPLOY DELAY; HIT-SPEED cadence (attacks land on the card's hit_speed, not a smeared DPS); CROWD CONTROL
  statuses -- SLOW (Ice Wizard: slow_factor 0.5 x move+attack for slow_duration 2.0s), STUN (0.5s: Zap /
  Tesla-evo pulse / Electro), FREEZE (1.0s); soft COLLISION / body-block (units can't stack, so a wall of
  troops holds up a tank); and KING activates on CHIP damage.
- REAL CARD LEVELS: the deck's cards.yaml levels went from all-11 to the user's ACTUAL account levels
  (royal_delivery 13, evo tesla 14, ice_wizard 12, X-Bow 16, rocket 14, miner 13, the_log 14, skeletons 13)
  so the sim scales stats realistically; the sim/cards now scale hp+damage by level. Evo Tesla got full
  curated stats + its periodic STUN PULSE (pulse_damage/radius/stun 0.5s/interval 3.0s) and a 25s lifetime.
- ENEMY LEVELS: opponent cards each roll a random level from sim.enemy_levels [13,14,15,16], weighted
  [2,4,3,2] (skewed to L15/16; L14 still the mode at ~36%), so the policy learns to counter higher-level
  decks. (sim/opponents.py assigns per-card levels; sim.env passes them through.)


------------------------------------------------------------------------------
2026-07-29 / 30  —  Training + tooling: BC --iterations, RL affordability mask, detect-timelapse, Instructions.txt
------------------------------------------------------------------------------
- train-bc gained `--iterations N`: run N successive warm-starting BC passes in ONE command (each seeds the
  next), for a stronger imitation baseline without babysitting.
- train-rl AFFORDABILITY MASK: the DQN `choose` and the Double-DQN target now mask out cards the agent can't
  afford (mirrors play.py), so RL stops emitting wasted "Not enough Elixir!" plays (which polluted the
  replay with no-op transitions and slowed learning).
- detect-timelapse: a command that builds a timelapse of in-match frames to HAND-LABEL for the detector
  dataset (Stage 2/3 tooling; detect.py).
- DOCS: new icebow/Instructions.txt (434-line from-scratch onboarding -- fork -> training/playing, with
  PATH A = the simulator (no game needed) and PATH B = record + live), surfaced on the front-page README.


------------------------------------------------------------------------------
2026-07-30  —  DESIGN NOTE: PPO vs Double-DQN -- verdict + trigger conditions
------------------------------------------------------------------------------
- Question: is the project at a point where PPO beats DDQN? VERDICT: NOT YET -- keep DDQN. Reasoning:
  * LIVE RL (train-rl) is SCARCE data (one ~3-4 min match at a time). DDQN's OFF-POLICY REPLAY reuses every
    transition many times = the sample-efficiency that regime needs; PPO is ON-POLICY (discards data) -> far
    more sample-hungry -> a poor fit for scarce live data. This is why the project chose DQN.
  * DQN isn't FAILING here: the past "reward-farming" issues were reward-SHAPE bugs (fixed by reward design),
    and value-overestimation was already fixed by vanilla->Double DQN + grad clip. The real bottlenecks now
    are reward shaping, sim fidelity / sim-to-real, and perception (detector, arena robustness) -- none of
    which PPO addresses. Switching now = rework + instability risk for no bottleneck relief.
- WHEN PPO BECOMES A GOOD POINT (the sim changes the calculus): PPO's on-policy inefficiency stops mattering
  when data is CHEAP + PARALLEL -- exactly the SIMULATOR regime. Trigger conditions:
  (1) the SIM becomes the primary trainer at SCALE (dozens-hundreds of parallel envs) -- PPO is the industry
      standard for massively-parallel game RL and is more stable than DQN at scale;
  (2) SELF-PLAY (leagues / past-selves, AlphaStar-style) -- policy-gradient + stochastic policies are the
      standard there;
  (3) you want a STOCHASTIC, less-exploitable policy (a deterministic DQN argmax is exploitable by a
      repeating opponent; PPO + entropy resists that -- valuable in adversarial 1v1);
  (4) longer-horizon / temporal action structure (timing, action sequences) -- policy gradients + GAE handle
      credit assignment more gracefully.
- RECOMMENDED PATH: keep DDQN for live RL; introduce PPO FOR THE SIM when you scale parallelism + add
  self-play. The rework ports cleanly -- the CNN trunk + factored heads (card / cell / gate) become a
  factored CATEGORICAL POLICY; add a VALUE (critic) head, GAE, and an on-policy rollout loop replacing the
  replay buffer. Moderate effort. Analysis-only note; no code change.


------------------------------------------------------------------------------
2026-07-30  —  icebow OFFENSE->DEFENSE phase machine (sim): rocket-cycle when the X-Bow can't break through
------------------------------------------------------------------------------
- User doctrine: if the X-Bow fails to lock / take a tower before 2x elixir, OR once you TAKE any tower, the
  bot switches to a DEFENSIVE X-Bow (back-centre snipe, Tesla in front) with ROCKET-CYCLE as the ONLY tower-
  damage source -- everything else clears pushes / stalls / cycles to more rockets.
- IMPLEMENTED IN THE SIM (sim/env.py -- the primary trainer; it knows the match clock + tower state): a
  per-match phase flag `_defensive` (starts False = offensive X-Bow win condition). It latches True once you
  take a tower (crowns(0) >= 1) OR double elixir arrives (t >= regulation-60 = 120s) and the X-Bow never
  broke through (cumulative enemy-tower chip < env.xbow_success_frac 0.30 of a princess). Once defensive the
  X-Bow REWARD FLIPS: a forward (in-range) X-Bow no longer earns the win-condition reward -- it's now a
  MISPLACE (xbow_misplace_penalty); a back-centre X-Bow earns xbow_defense_reward; and a rocket aimed at an
  enemy princess earns rewards.defensive_rocket_reward (0.3) = the sanctioned cycle-chip. Verified end-to-end:
  offensive forward +1.0 / back +0.3 / rocket 0; defensive forward -0.75 / back +0.3 / rocket +0.3.
- config: rewards.defensive_rocket_reward (0.3), env.xbow_success_frac (0.30). info now carries `defensive`.
- LIVE (env.py) NOT wired yet: the "took a tower" trigger is available live (crowns), but the "2x elixir"
  trigger needs the TIME-BASED match-clock reader (a Phase-2 perception item, not built). TODO: once the
  clock reader lands, mirror this phase machine in env.py so live play / train-rl follow the same doctrine.


------------------------------------------------------------------------------
2026-07-30  —  DESIGN NOTE: self-play in the sim (integration plan, for the other machine to build)
------------------------------------------------------------------------------
- Goal (user): train the policy against PAST VERSIONS of itself, not just the scripted meta-deck bots.
  Analysis of the current engine + the concrete scheme:
- CURRENT ENGINE: agent = team 0 (bottom); opponent = team 1 (top) driven by make_opponent() = a scripted
  bot piloting a sampled meta deck. To make the opponent a POLICY, team 1 must be driven by a frozen net.
- KEY DETAIL -- MIRRORING: the policy is trained as team 0 (bottom). To control team 1 it must see a
  MIRRORED observation (flip the board vertically so team 1 reads itself as 'bottom') and its chosen CELL
  must be un-mirrored back to team-1 coordinates. Add mirror(obs)/mirror(cell); feed the frozen net the
  mirrored obs+hand+elixir+threat, take its action, un-mirror the placement, deploy for team 1.
- LEAGUE + CADENCE (recommended -- tune later):
  * SNAPSHOT every sim.selfplay_snapshot_matches (~1000) into a LEAGUE = a ring buffer of the last
    sim.selfplay_league_size (~8) frozen weight-sets.
  * PER-MATCH opponent = a league snapshot with probability sim.selfplay_prob, else a scripted meta bot. Do
    NOT hard-alternate every match -- SAMPLE stochastically so each match is a fresh mix (scripted ~40% of
    the time keeps deck-diversity robustness; the rest is self-play).
  * RAMP selfplay_prob ~0 -> ~0.6 over sim.selfplay_ramp_matches (early self-play vs a near-random policy is
    useless / can collapse -- ramp it in as the policy gets competent).
  * League SAMPLING: mostly the most-recent snapshot, but include older ones (uniform tail) so the policy
    doesn't overfit to beating only its latest self (avoids strategy cycling / collapse).
  * The self-play opponent plays the SAME icebow deck (a mirror match); scripted opponents keep the varied
    meta decks -- together = mirror skill + deck robustness.
- WHERE: a new SelfPlayOpponent (wraps a frozen DQN, does mirrored inference), selectable in the train-sim
  loop / make_opponent; config sim.selfplay_prob / selfplay_snapshot_matches / selfplay_league_size /
  selfplay_ramp_matches. TIES TO THE PPO VERDICT: self-play is the trigger where PPO starts to beat DDQN --
  consider building the PPO sim trainer at the same time. Analysis-only note; no code yet.


------------------------------------------------------------------------------
2026-07-30  —  Matchup-aware X-Bow doctrine (sim): defensive vs cycle/beatdown + the beatdown punish
------------------------------------------------------------------------------
- User doctrine: vs a fast CYCLE deck (2.6 Hog / 2.9 Hog-EQ) OR a heavy BEATDOWN deck (Pekka / Mega Knight /
  Golem / Elixir Golem), play EXCLUSIVELY defensive X-Bow (never the offensive win condition). EXCEPTION for
  beatdown: if they drop a 7+ elixir card BEHIND their king during 1x elixir, immediately X-Bow the bridge in
  the OPPOSITE lane (punish the expensive, committed play while they're out of position).
- IMPLEMENTED IN THE SIM (the sim KNOWS the opponent's style + its deploys):
  * sim/env.py reads the opponent's inferred STYLE (cycle/control/beatdown/siege from meta_decks.classify).
    On reset, vs cycle/beatdown it starts in DEFENSIVE mode (self._defensive True from t=0 -> back-centre
    X-Bow + rocket-cycle, forward X-Bow = misplace); vs control/siege it's offensive-first (transitions per
    the 2x-elixir / tower-taken rule).
  * BEATDOWN PUNISH: the engine now records each team's last deploy (engine.last_deploy). When the beatdown
    opponent drops a TROOP with elixir >= env.beatdown_punish_elixir (7) at/behind their king
    (y <= env.enemy_king_behind_y 0.18) during 1x elixir, a window (env.beatdown_punish_window_s 3s) opens
    that rewards an X-Bow placed at the bridge in the OPPOSITE lane with rewards.punish_xbow_reward (1.0) --
    overriding the defensive misplace for that counter-push. The beatdown bot now places its tank truly
    behind the king (0.18 -> 0.10) so the trigger is realistic.
  * SPLIT-LANE HARD-COUNTERS (2026-07-30): a deck built on Royal Recruits / Royal Hogs (wide two-lane win
    conditions a single X-Bow can't cover -- they hard-counter siege) ALSO forces defensive mode for the
    WHOLE match (defensive X-Bow + rocket-cycle, never the offensive win condition). Detected from the
    opponent deck's card keys (new ScriptedBot.cards) intersected with env.split_lane_counter_cards
    [royal_recruits, royal_hogs], independent of the inferred style (these decks usually classify 'control').
  * Verified: cycle/beatdown -> defensive from start; a royal_hogs / royal_recruits / hogs+recruits deck ->
    defensive from start (even though it classifies 'control'); plain control/siege -> offensive; a Golem
    (8e) behind their left king opens the punish -> X-Bow opposite (right) bridge +1.0, same-lane -0.75.
- config: rewards.punish_xbow_reward, env.beatdown_punish_elixir / beatdown_punish_window_s / enemy_king_behind_y /
  split_lane_counter_cards [royal_recruits, royal_hogs].
- LIVE = POST-STAGE-3: live can't know the opponent's archetype a priori -- it must INFER it from the enemy
  cards it sees, which needs the OBJECT DETECTOR (identify enemy cards -> classify cycle/beatdown by avg
  elixir + win condition; spot Royal Recruits / Royal Hogs to flag the split-lane hard-counter), and the
  "7+ behind king during 1x" trigger needs the detector (card + elixir + position) + the match-CLOCK reader.
  So the LIVE version is gated on Stage 3 + the clock. TODO once both land.


------------------------------------------------------------------------------
2026-08-04  —  Deck tweak: Royal Delivery -> Tornado (back to Tornado)
------------------------------------------------------------------------------
- Swapped royal_delivery OUT, tornado IN (1-for-1) in config/cards.yaml deck. Deck stays "Miner X-Bow
  Control (Classic 1v1)"; identity COUNT unchanged (9: tornado, tesla, tesla_evo, ice_wizard, x_bow,
  rocket, miner, the_log, skeletons) so the policy's card-head width is unchanged -- but the swapped index
  now MEANS tornado, so old checkpoints are semantically stale there: RETRAIN (sim from scratch, then BC
  from NEW recordings that play Tornado). Tornado level set to 13 (placeholder -- user to confirm real level).
- Tornado is a 3-elixir defensive PULL spell (KB flags [pull, splash, splash_air]); already fully defined
  in cards.yaml, and 20 hand templates (tornado_*.png) already exist, so hand recognition needs no new work.
  royal_delivery stays in the card KB (opponents still play it).
- LIVE env (env.py): Tornado now classified as a REACTIVE defensive spell -- new self.tornado_ids, added to
  reactive_ids alongside royal_delivery_ids (casting it on a QUIET board is 'premature', like the other
  defenders). Its spell-impact timing already resolved right (any non-rocket spell -> env.tornado_time 1.2s;
  the 3s royal_delivery_time branch keys on royal_delivery_ids, which Tornado is NOT in). Not in anywhere_ids
  -> your-half only, matching its defensive use (as RD was). Fixed a stale play.py comment.
- SIM: deck-driven, so tornado is auto-picked-up; the engine treats it as a generic small-damage spell
  (radius 0.09) -- the PULL mechanic is NOT modeled (low-fidelity, as before). Fine for retraining; a proper
  tornado-pull sim is a future item.
- The OLD Tornado/Ronin reward knobs (king_tank_reward / threat_tornado_pull / rocket_tornado_combo /
  tornado_chip_penalty / combo_*) were removed in the earlier reward rebuild and are NOT wired -- Tornado
  rides the generic spell + reactive paths. Docs updated (README deck line, Instructions checklist +
  template-rename list, DECK_SWITCH deck line + template list + reward-audit note).
- USER TODO before it plays well: re-record matches playing Tornado -> label --all -> train-bc (warm-start
  from a fresh train-sim) -> train-rl. (Sim can retrain immediately with no recording.)


------------------------------------------------------------------------------
2026-08-04  —  Deck tweak: Miner -> Knight (now the standard icebow / X-Bow 2.9 list)
------------------------------------------------------------------------------
- Swapped miner OUT, knight IN (level 16) in config/cards.yaml. With Royal-Delivery -> Tornado (above),
  the deck is now the STANDARD icebow / X-Bow 2.9 control list: X-Bow, Evo Tesla, Ice Wizard, Knight,
  Skeletons, The Log, Rocket, Tornado (the list Hunter CR runs). Renamed "Miner X-Bow Control" ->
  "Icebow X-Bow Control (Classic 1v1)". avg elixir 3.5.
- PURE DATA + DOCS -- no code-logic change. The card-role system is deck-key-driven: Knight is a plain
  your-half DEFENSIVE TROOP (KB [mini_tank], melee) -- not rocket/xbow/spell and NOT in anywhere_ids
  (your-half clamped), scored by the generic threat-response + trade + troop-defeat rewards. The SIM even
  applies its "premature defender on a quiet board" penalty to Knight automatically (any non-wincon/
  non-spell/non-miner troop). Miner's special paths (anywhere_ids, miner_ids, miner_chip reward in live +
  sim) now produce EMPTY sets -> inert/harmless, like royal_delivery_ids after the Tornado swap. Miner
  stays in the card KB + detector classes (opponents still play it).
- Knight is NOT added to reactive_ids (live premature-penalty set = ranged defenders + spells) -- kept a
  plain troop like Skeletons, since Knight is also used proactively (tank for the X-Bow). If live play wants
  stricter "hold Knight to defend" shaping, add it to reactive_ids later (a tuning call, not needed now).
- HAND TEMPLATES: knight has NO hand template yet -- user must run `hand-templates`, rename a crop to
  knight.png (+ extras knight_2.png ...), then `verify --hand`. DETECTOR: knight is already a class
  (detect_classes.yaml: knight / knight_evo / knight_hero / ...), so existing annotations cover it.
- Identity COUNT unchanged (9) -> card-head width unchanged, but the swapped slot now MEANS knight ->
  RETRAIN (train-sim from scratch; re-record + label + train-bc + train-rl for live). Docs updated
  (README x2, Instructions checklist + template list, DECK_SWITCH title/deck/doctrine).


------------------------------------------------------------------------------
2026-08-04  —  Knight is the EVOLUTION (Evo Knight), not normal
------------------------------------------------------------------------------
- Deck marks knight `evolved: true` (like Evo Tesla). Evolution data from the KB / Fandom wiki: 2-CYCLE
  evolution (cards.yaml knight.evolution.cycles = 2 -- you cycle Knight twice to charge it). Its evo power
  is DAMAGE REDUCTION, not a shield pool: it takes 60% LESS damage from ALL sources WHILE NOT ATTACKING,
  and that drops the instant it deals a hit (back on when it moves again). cards_stats.json knight_evo
  carries the BASE stats (HP 1766 / dmg 202 / hit 1.2s / dps 168 @ L11) -- the evo is pure survivability.
- deck_identities() 9 -> 10: an evolved card yields BOTH forms (knight + knight_evo), since evolutions
  CYCLE between normal and evolved -> the policy's CARD-HEAD WIDTH grows by 1 (a real model-shape change;
  retrain from scratch, don't reuse the 9-wide checkpoint).
- SIM: build_spec reads knight.evolution.damage_reduction (0.60) into CardSpec.damage_reduction; Unit gains
  an `attacking` flag (True when a target is in reach, cleared while moving); _hurt multiplies ALL incoming
  damage by (1 - dr) while not attacking. Verified: Evo Knight loses 40 of a 100 hit while moving, 100 while
  attacking; normal Knight 100. (Earlier this day I wrongly modeled it as a 0.5x-HP shield POOL -- reverted.)
- HAND TEMPLATES: evolved cards show a DIFFERENT (glowing) portrait when charged and cycle back to the
  normal face, so BOTH are needed -- capture knight.png AND knight_evo.png (as Tesla already has tesla.png
  + tesla_evo.png). Docs updated (README, Instructions checklist + template list + evo note, DECK_SWITCH).


------------------------------------------------------------------------------
2026-08-07  —  Overlay replays: record the opening of every match with boxes
------------------------------------------------------------------------------
- NEW config block `overlay_replay` (OFF by default): when on, a background thread records the
  first `seconds` of EVERY match to data/overlayed_replays/match_<stamp>.mp4 with the detector's
  team-coloured boxes burned in. Same shape as the preview window -- its own private WindowCapture
  (mss handles are per-thread, so the env's capture is untouched).
- The BOXES refresh at observation.perception_hz (~0.1s, the same feed the preview uses) while the
  VIDEO runs at overlay_replay.fps, so a clip shows the policy's real perception latency rather
  than hiding it.
- 60 fps is ALLOWED but is the ceiling: measured on this machine, grab 14.86ms + draw 0.94 + resize
  0.32 + encode 0.97 = 17.09 ms/frame => ~58 fps with nothing else running, and the GRAB alone is
  87% of that. Default 30. ~25 MB per 60s clip at 60fps/scale 0.5.
- WALL-CLOCK PACED: cv2.VideoWriter bakes the REQUESTED fps into the header, so a run that only
  sustains 40 while claiming 60 plays 1.5x too fast -- fatal for a tool meant to judge reaction
  timing. Missed slots are now filled by duplicating the newest frame, and the ACHIEVED capture
  rate prints when each clip closes.


------------------------------------------------------------------------------
2026-08-07  —  Sprite bank: the cutter was grabbing the unit standing ON TOP
------------------------------------------------------------------------------
- BUG: `_cut_sprite` seeded GrabCut with only the target's own box and knew nothing about the other
  annotations in the frame. When a second unit overlaps, the shared pixels are unattributable from a
  2D box alone and GrabCut prefers the UNOCCLUDED body -- so the bank filled with sprites of the
  WRONG card, which `--synth` then pasted into training images as pure label noise.
- SCALE: of 5120 train boxes, 32.6% are overlapped at all, 10.4% by >30%, 3.2% by >70%. The
  worst-hit classes are the worst-recall ones -- bats 25% heavily occluded, guards 26%, knight 16%,
  valkyrie 14% -- while the deck cards are clean (tesla 0.048 mean, x_bow 0.052, miner 0.027).
- FIX: the other boxes are free supervision. Build an occluder mask, REJECT above 35% covered, and
  below that pin those pixels to definite BACKGROUND so the occluder's colours train the background
  model. The exclusion is re-asserted after grabcut, after the morphology, and after the alpha
  feather (that last one was the difference between 1.07% and 0.00% residual). `_aoe` decals are
  excluded from the occluder set -- units stand ON them.
- MEASURED: share of sprite pixels that are actually a neighbour's body went mean 11.66% -> 0.00%,
  worst 51.22% -> 0.00%. Bank rebuilt: 4663 kept, 318 rejected as too occluded.
- NB rebuilding the bank REQUIRES regenerating the synths, or training still carries the old
  contamination.


------------------------------------------------------------------------------
2026-08-07  —  Annotation plumbing: adopt / merge / multi-json import
------------------------------------------------------------------------------
- `detect-import --export` now accepts a single .json, a COMMA-SEPARATED LIST, or a folder holding
  several (all merged). Previously a folder silently used only `sorted(glob)[0]`.
- It also DEDUPES tasks by image basename, keeping the copy with the most boxes. Label Studio keeps
  several tasks per image when a frame is re-labelled: 54 such duplicates existed, 53 of them
  DISAGREED, and the old loop was pure last-write-wins -- an arbitrary coin flip over which
  annotation survived. The printed counts are now DISTINCT IMAGES, which is what lands on disk
  (reporting len(pairs) had overstated the dataset by the duplicate count).
- NEW `run.py detect-adopt --json X [--images F] [--prefix P] [--dry-run]` -- ingest someone else's
  export + image folder. Each incoming file is md5-compared against the queue entry of the same
  name: free name -> copy; IDENTICAL bytes -> already adopted, skip (re-running is a no-op);
  DIFFERENT bytes -> collision, and the whole batch takes a prefix. WHY: a helper's generic
  frame_0005.png restarts every batch, and their second batch reused all 40 names with different
  pixels -- copying it in would have overwritten the first batch's images and left ITS 215
  annotations describing unrelated frames, silently.
- NEW `run.py detect-merge [--sources] [--out] [--dry-run]` -- fuse every batch*.json into one
  self-contained export (default data/detect/batch_all.json), deduped by the same rule so importing
  the merged file equals importing the sources together. Skips *.raw.json.bak and its own output.
- NEW `tools/detect/train.py --resume [RUN]` -- continue an interrupted run in its own folder,
  keeping its epoch count and best.pt. NOTE resume restores args from the checkpoint, so you CANNOT
  lower --batch on a resume; survive an OOM by freeing memory or changing the allocator instead.


------------------------------------------------------------------------------
2026-08-07  —  Detector diagnosis: knight is NOT a lookalike problem
------------------------------------------------------------------------------
- Every val ground-truth box was bucketed by WHY it failed (detect at conf 0.03, then classify as
  HIT / right-class-but-below-gate / WRONG CLASS / blind). Each bucket has a different fix, and this
  had never been measured.
- KNIGHT (n=63): 59% hit, 17% right-class-but-under-confident (conf median 0.19), 13% blind, and
  only 11% wrong class -- those 7 scattered across SIX classes, so there is no confuser to
  disambiguate against. The earlier "knight is lookalike-confused" claim in these logs was WRONG.
- The classes that DO need disambiguation: wizard 60% wrong class (-> musketeer), valkyrie 31%.
  Genuinely data-starved (blind-dominated): guards 36%, bats 25%. Gate-limited: hog_rider 42%
  under-confident, knight 17%.
- KILLED HYPOTHESIS: base/_evo/_hero classes splitting confidence on one box so neither clears the
  gate -- across the whole val set only 2 boxes would flip. Do not revisit.
- NEW `observation.detector_conf_by_card` (base key -> gate), applied inside BoardDetector.detect()
  so every live path benefits with no call-site changes: predict runs at the FLOOR, then each box is
  judged against its own class's gate. hog_rider sat at precision 1.00 at gate 0.40 (purely
  over-gated); knight improves on BOTH axes at 0.30; guards gains nothing at any gate. Left EMPTY on
  purpose -- one generation on 12-63 val instances is not a stable measurement.
- NEW `run.py label-queue [--classes] [--n] [--copy] [--device cpu]` -- rank the ~2600 unlabelled
  frames by AMBIGUITY (two classes claim one box, scored so a 0.45-vs-0.40 standoff outranks
  0.80-vs-0.05) and UNCERTAINTY (mid-band lone detections), so scarce labelling time goes to frames
  that resolve a confusion instead of boards the detector already reads.


------------------------------------------------------------------------------
2026-08-07  —  Data-scaling law + annotation priorities (measured)
------------------------------------------------------------------------------
- Fitted per-class recall against TRAIN instance count over 36 classes:
  recall = 0.183*log10(n) + 0.330, i.e. +5.5 pp per DOUBLING of a class's instances.
  CAVEAT weighted R^2 is only 0.20 -- class difficulty dominates volume (at ~50 instances recall
  ranges from guards 0.27 to archers 1.00), so this predicts the population, not a single class.
- From 1488 train images: +1000 images ~ +3.8 pp. But it is logarithmic, so the marginal rate
  collapses -- 1st +1000 = 3.3 pp/1k, +5000 total = 1.0 pp/1k, +10000 = 0.5 pp/1k.
- "Per 1000 images" is the wrong unit: +100 instances is worth +11.6 pp to firecracker (30) and
  +1.4 pp to tesla (504). Target weak classes; it wins on the macro (whitelist-like) metric even
  though it loses on the support-weighted one.
- Gap list ranked by ENCOUNTER x MISS over the 1000-deck meta pool (a card you never face is worth
  zero labels): fireball is the MOST COMMON card in the meta at 21.6% of decks and has 12 boxes,
  with fireball_aoe at 4 -- the single highest-value target, and the class blocking splash-memory.
  Then zap_aoe 0 boxes / 11.4%, lightning_aoe 0 / 9.7%. NB filter out party_*/super_* first: they
  are event-only, excluded from the taxonomy by design, and otherwise pollute the ranking.


------------------------------------------------------------------------------
2026-08-07  —  replay_mine pointed at a folder that never existed
------------------------------------------------------------------------------
- `replay_mine.replays_dir` was `data/replays`, which does not exist -- mine-replays would have
  found nothing and reported "no replays" rather than failing loudly. Repointed at the real folder.
- `frame_stride: 3` on 60 fps footage = 516k frames -> 172k detector passes (~6 h). Raised to 30
  (2 samples/sec -> 17.2k passes, ~35 min); a card can only be played about once a second, so no
  placement is lost.
- The Hunter CR footage is 143 min, 60 fps, portrait, aspect 0.463 -- the SAME aspect as the helper's
  annotation frames (0.461), so those labels come from this very footage and improve detection on
  exactly what will be mined. He also plays the same X-Bow/Tesla archetype, so the priors are
  directly relevant.
- CAUTION: `mine-replays` only "reports readiness" when NO detector exists. With weights present it
  starts mining on the GPU immediately -- do not run it while a detector training is in flight.


------------------------------------------------------------------------------
2026-08-11  —  Deck level, The Log's depiction, and the BC/sim threat-width bug
------------------------------------------------------------------------------
- DECK: skeletons 13 -> 15 in config/cards.yaml (real account level). Nothing else needed changing:
  the level lives in ONE place and both the sim and the RL env derive HP/damage from it at runtime
  via `1.1^(level-11)` (CardDB.deck()/deck_levels(), sim/engine.build_spec), and `fair_eval_level:
  null` recomputes the fair-eval level from the deck mean (13 -> 14). VERIFIED by loading the KB:
  skeletons resolves at level 15, hp/damage 119.
- NB `real/config/cards.yaml` still carries skeletons at level 11. That project is maintained
  separately and had already diverged, so it was deliberately NOT touched.
- SIM-VIEW: The Log rendered as a bare cross at the cast point, so in the debugger it read as a
  POINT BLAST localised to the cast tile. The engine was always correct -- `_resolve_roll` tests a
  forward CORRIDOR (9.6 tiles forward, 1.0 behind, +/-2.2 half-width, ground-only, knockback) -- only
  the picture was wrong, which is exactly the class of bug sim_view.py exists to prevent (the Rocket
  already drew its blast ellipse; the Log drew nothing). sim_view now draws the roll rectangle,
  oriented forward by team from the same roll_len / back-slop / half-width the engine uses.
- BUG (train-bc): `train-sim-ppo --init data/policy.pt` reported "shape-incompatible". The mismatch
  was threat_dim 46 (checkpoint) vs 52 (env) -- the +6 is the TOWER-HP block added in 2819923.
  The root cause was NOT a stale file: `train_bc._load_datasets` computed its target threat width as
  16 + identity/memory + interactions and OMITTED the tower term that sim/env.py and replay_bc.py
  both include, so it TRUNCATED the correctly-52-dim replay datasets back to 46 and every BC policy
  came out permanently incompatible with the (tower-HP-on) simulator. Fixed to include
  `view.TOWER_DIM` under the same `observation.use_tower_hp` gate. VERIFIED: target width 52, the
  four replay_bc sets load as (1182, 52) with no truncation.
- CAVEAT `data/sessions/*` (2026-08-04) are doubly stale -- 3-channel obs AND 46-dim threats
  (pre-canvas-flip, pre-tower-HP). Re-run `label --all` before mixing them with replay data.


------------------------------------------------------------------------------
2026-08-11  —  Identity block now watches from the BRIDGE, not the river
------------------------------------------------------------------------------
- SYMPTOM: an enemy Hog Rider at the bridge did not trigger a Tesla answer even with the obs canvas
  on. The detector and KB ARE wired in (policy_rl.pt is threat_dim 52 / in_ch 9, so identity+memory+
  interactions+tower all load; hog_rider is whitelisted in `detector_cards`; detector_conf 0.35; the
  KB has hog_rider win_condition=True, building_targeting=True, which is precisely the Tesla cue).
- ROOT CAUSE: the identity block only looked at units that had ALREADY CROSSED (`y >= 0.5`), so a
  wincon at the bridge lit NOTHING, and the moment it crossed it lit up at depth ~0 -- the latest
  possible instant, after which a defensive building can no longer deploy and acquire in time.
  MEASURED before the fix: y=0.46 -> present 0 / wincon 0; y=0.50 -> wincon 1 but depth 0.00.
- FIX: new `observation.identity_front_y` (0.44 = the deploy line / bridge approach), with depth
  RENORMALISED over the watched span so depth AND approach-velocity mean something before the river.
  MEASURED after: y=0.46 -> present 1, wincon 1, depth 0.036, vel 0.119.
- The watch line was hard-coded to 0.5 in FIVE separate producers. All now read one shared helper
  (`card_threat.identity_front` / `identity_depth`): live play, the live RL env, sim/view.identity_items,
  the mirrored self-play opponent, and the offline labeller. VERIFIED sim/live parity: 0 mismatches.
- threat_dim is UNCHANGED at 52 (same 10 features, different population), but the observation
  DISTRIBUTION changed -> a fresh sim run is required before any checkpoint benefits.


------------------------------------------------------------------------------
2026-08-12  —  Passivity collapse: diagnosis, instrumentation, and the first fixes
------------------------------------------------------------------------------
SYMPTOM (user, ~10 h of live train-rl): no progress and visible REGRESSION -- the policy drifted
toward making no plays at all despite a full bar and an active opponent, appearing to end matches
early to stop accruing penalties.

This is the THIRD instance of the same attractor. The two prior ones are already recorded in
config.yaml: `cycle_spare_elixir` ("a flat tax on every action ... the policy's answer was to stop
playing", plays/match 46.8 -> 26.0, winrate 8.3% -> 2.8%) and the `counterfactual` note
("cycle_plan fired 0 bonuses against 305 penalties and threat_response's quiet-board branch 0
against 257, and both taught the policy to stop playing").

CAUSES IDENTIFIED (ranked):
  C1 Positive shaping was capped per match (`correctness_cap`) while penalties were UNCAPPED. Over a
     180-300 decision match that is not a cap, it is a slope: the bonus saturates, the penalty stream
     does not, so the highest-value policy is the one that ENDS THE MATCH SOONEST.
  C2 The outcome compass is invisible: win/loss +/-2.0 discounted by gamma 0.99 over ~200 steps is
     ~0.27 at match start, against undiscounted per-step penalties. A fast loss costs a BOUNDED -3
     to -5; surviving under a -0.1/step drift costs -25.
  C3 ARCHITECTURAL: `play_val = gate[play] + max_card + max_cell` vs `wait_val = gate[wait]` -- three
     heads against one. All three train on the same TD error, so any systematic negative return on
     plays pushed all three down together and the no-op won BY CONSTRUCTION, independent of tuning.
  C4 Positives are perception-conditioned, penalties are not. When the detector misses, defending
     scored -0.4 (quiet-board branch) while idling scored 0.
  C5 Doctrine mismatch (icebow is X-Bow 3.5, NOT 2.9 -- it cannot out-cycle, so defence must be
     elixir-EFFICIENT). cycle_plan/cycle_waste were DELETED from the sim for being one-sided but are
     still live; the quiet-board penalty punishes pre-placing Tesla, the deck's signature play.
  C6 Throughput: ~12 matches/hour live is far too little for DQN to improve, but ample to slide into
     the passivity attractor. Live RL also had NO keep-best gate, so the regression was PERSISTED.

IMPLEMENTED (Batch A -- safety + measurement):
- NEW `src/clashrl/reward_stats.py` (`RewardTerms`): per-term fires / positive / negative / totals,
  per match and per session. `add()` returns its input UNCHANGED, so instrumenting a reward assembly
  is a pure wrapper and cannot alter the number the agent sees.
- Wired into BOTH envs. The live env prints a per-match breakdown and appends
  `data/reward_stats/live_<ts>.jsonl`; `policy-stats` aggregates the sim pool, prints terms
  most-negative-first, and flags any term that fires but NEVER pays positive (genuinely one-signed
  outcome terms are excluded from that flag).
- KEEP-BEST GATE for train-rl. `policy_rl.pt` is now promoted only when the recent window is at least
  as good as the best window this session; every save still lands in `policy_rl_last.pt`, so nothing
  is lost. Scored on WIN RATE and guarded by plays/match, because the collapse RAISES mean episode
  reward (a shorter match accrues fewer penalties) -- a reward-scored gate would have ENDORSED it.
  New knobs: `train.rl_keep_best_window` (10), `train.rl_keep_best_min_play_frac` (0.5).

IMPLEMENTED (Batch B -- structural):
- C1 fixed: `_bonus` now caps BOTH signs by the same per-match budget, in the live env and the sim.
- C3 fixed: dueling-style identifiability. The card and cell heads carry only the RELATIVE preference
  among LEGAL options (advantage <= 0, exactly 0 at their own masked argmax), so max over (card,cell)
  of Q(play) IS gate[play] and the play/wait decision is ONE head against ONE head. Applied at action
  selection, the TD target and the DDQN bootstrap -- and in policy_stats, whose DDQN gate rule must
  stay in lockstep with the trainer (that drift once cost 33pp).
  VERIFIED Q(play, greedy) == gate[play] to 1e-6; a non-greedy card scores strictly lower;
  all-masked rows stay finite.
  CONSEQUENCE: existing DDQN gate heads were trained under the ADDITIVE rule, so their logits mean
  something different now -- DDQN checkpoints need a fresh sim run. PPO checkpoints are unaffected.

MEASURED AFTERWARDS (this is the important part):
- SIM, policy_sim_ppo_best.pt, 150 greedy matches: win 11%, 26.2 plays/match. Dominant terms
  threat_miss_idle -2168.0 (2168 fires, ZERO positive) and wincon_exec +680.6.
- LIVE, 5 matches (all losses): threat_miss_idle and wincon_exec fire ZERO times -- the sim's two
  biggest terms are ABSENT live. What remains is negative-only: elixir_trade -22.6 (76 pos/140 neg),
  threat_response -16.8 (7 pos/43 neg -- defending punished 6:1), cycle_plan -8.8 (2 pos/24 neg).
  EVERY match totalled between -12 and -23. There is NO reachable positive pathway in the live env.
  That is the passivity gradient, measured directly.
- PLACEMENT COLLAPSE (the finding that reorders everything): 3 distinct cells used out of 432, with
  79.2% of all plays on ONE tile. The counts decompose exactly -- cell 237 (x=0.213, y=0.527, LEFT
  lane) = X-Bow 801 + Skeletons 787 + Log 785 + Knight 578 + Tesla 115 + Ice Wizard 51; cell 94 =
  Rocket; cell 280 = Knight Evo. SIX different cards deploy to the identical tile regardless of the
  board. The cell head is a CONSTANT FUNCTION, not a policy -- which is also why Knight/Ice Wizard
  are thrown down the wrong lane: with a fixed left-lane cell, a right-lane push cannot be answered.
- ROOT CAUSE of that collapse: `ppo_explore_floor` was applied to the CARD distribution ONLY; the
  432-way CELL head had no floor and shared a single `ppo_entropy: 0.01`. Max entropy is ln(10)=2.30
  vs ln(432)=6.07, so the big head's entropy gradient could not resist collapsing to a delta, and a
  never-sampled action gets no gradient -- self-reinforcing.

IMPLEMENTED (cell-collapse fix):
- NEW `sim.ppo_cell_explore_floor` (0.25) -- cell sampling is now (1-floor)*policy +
  floor*uniform(DEPLOYABLE), mirroring the card floor. The stored log-prob is the MIXTURE's, so the
  PPO ratio stays exact importance sampling.
- NEW `sim.ppo_cell_entropy` (0.05) -- the cell head's entropy is weighted separately from
  gate/card, because one coefficient cannot hold a 10-way and a 432-way categorical at once.
- `policy-stats` now reports `cells_used` and `top_cell_share` -- the acceptance metric.
- VERIFIED on a synthetic fully-collapsed head: pure sampling gives 1 distinct cell over 20k draws,
  the mixture gives 213 (= every deployable cell), 25.0% of samples land off the frozen argmax, the
  mixture sums to 1.0 and puts ZERO mass outside the deployable mask.
- Both floors are ROLLOUT-ONLY: the greedy benchmark and live play remain the pure policy.
- START HIGH, THEN ANNEAL: 0.25 is deliberately noisy; it is the price of repopulating a dead
  distribution. Walk it to 0.05-0.10 once cells_used recovers.

PLANNED, NOT YET IMPLEMENTED (Batch C onward, in priority order):
  1. RE-TRAIN FROM SCRATCH IN SIM and gate on placement spread. A floor PREVENTS re-collapse; it does
     not un-freeze dead weights. ACCEPTANCE: cells_used moves from 3 into the dozens and
     top_cell_share falls well below 79%. If it does NOT, stop -- suspect the observation is not
     reaching the cell head at all, which is a deeper problem than exploration.
  2. GIVE THE LIVE ENV A REACHABLE POSITIVE. Today it has none (see the measurement above). Includes
     perception-gating the PENALTIES too, so a defensive play is never punished on a frame the
     detector could not justify, and deleting the live-only cycle_plan/cycle_waste (already gone from
     the sim) and the quiet-board defender penalty.
  3. FIX elixir_trade CREDIT ASSIGNMENT. It fires every step (216 fires over 5 live matches) and
     credits ambient enemy-mass change the agent did not cause -- towers killing things, troops
     expiring. Replace the immediate spend charge + stochastic kill credit with DELAYED SETTLEMENT:
     judge a play by what happened in the following N seconds (enemy elixir neutralised / elixir
     spent; X-Bow seconds-firing-on-tower; tower HP differential), carried back by train.n_step.
     This is the real architectural rework -- it replaces the hand-written per-play classifiers with
     one two-sided mechanism that works LIVE (no engine deepcopy, unlike `counterfactual`).
  4. EPISODE-LENGTH NEUTRALITY (C2). Decide AFTER 2 and 3: if the negative drift is gone there may be
     nothing to counterweight. If needed, an action-INDEPENDENT per-step constant is safe; raising
     win/loss alone is not (gamma discounts it into irrelevance).
  5. RECONCILE LIVE DECISION CADENCE. Timestamps in the live reward_stats show ~2.2 s per decision
     against the trained `act_period: 1.0` (= the sim's agent_dt). Live reactions are running at
     roughly half the trained rate -- an unflagged train/serve mismatch.
  6. REGIME: train in the sim, use live only for fine-tuning and evaluation, and benchmark every
     change with policy-stats.

GATES between batches (any change must hold ALL of these on >=150 sim matches):
  plays/match does not fall; mean elixir does not rise; every retained shaping term shows >0 positive
  fires; win rate flat or better. A term that fires 0 positives gets deleted, per precedent.

DO NOT (each repeats a known failure):
  - Add another one-sided "correctness" term. Three have now collapsed runs.
  - Add anti-hoarding pressure. icebow is X-Bow 3.5: banking to ~10 for X-Bow(6) + Tesla(4) is
    CORRECT doctrine, not a bug.
  - Raise win/loss in isolation (see C2).
  - Port `counterfactual` to the live env -- it requires deep-copying a real game.


------------------------------------------------------------------------------
2026-08-12 (evening)  —  Batch C execution: BC-init baseline, live reachable positive,
                         cadence reconciliation, elixir_trade design
------------------------------------------------------------------------------
Context: the from-scratch PPO sim retrain (plan item 1) is RUNNING, initialised from the
Hunter-CR replay-BC checkpoint (data/policy.pt, via replay-bc -> train-bc). This batch executed
the work that can land in parallel without touching the running sim's behaviour.

DECK NOTE (user correction, recorded): icebow is a 3.5 avg-elixir CYCLE deck, NOT "X-Bow 2.9" --
substantially slower than a standard cycle deck but it still functions as one. README's deck
line said 2.9 and is fixed; C5's doctrine consequences (elixir-efficient defence, banking to ~10
is correct) already assumed 3.5.

BC-INIT BASELINE (policy-stats --ckpt data/policy.pt --matches 150 -> data/policy_stats_bc_init.json):
- PLACEMENT: cells_used **1 of 432**, top cell **100.0%**. The replay-BC init's cell head is
  ALREADY a constant function -- worse than the collapsed PPO checkpoint's 3. Cause is the
  training data, not exploration: ~1.2k detector-noisy samples against a 432-way softmax, and
  the loss-minimising solution for a small noisy set is the marginal mode. So the warm start
  contributes CARD-CHOICE structure (7 of 10 identities played at humanish shares: skeletons
  20.0%, ice wizard 19.9%, log 19.1%, tornado 15.3%, knight 14.3%) but ZERO placement spread.
- CONSEQUENCE FOR THE RUNNING RETRAIN: spread recovery rides entirely on sim.ppo_cell_explore_floor
  (0.25) un-freezing the head -- the exact scenario the plan flagged. Judge the acceptance gate
  against a baseline of 1, not 3, and run policy-stats MID-run (~a few k matches in): if
  cells_used is still single-digit there, stop and suspect the obs->cell-head path per the plan.
- CAVEATS: train-bc saves no gate head, so the baseline's gate was random-init (gate held 0%,
  forced waits 80%) -- plays/match (37.1) and wait rates are NOT comparable to PPO reads. X-Bow /
  Rocket / Tesla-Evo were never played under that degenerate gate (win 12% means little).

LIVE ENV: A REACHABLE POSITIVE (plan item 2 -- env.py; sim behaviour untouched):
- QUIET-BOARD DEFENDER PENALTY DELETED from _threat_response_live, mirroring the sim's retirement
  (sim: 257 fires / 0 positives; live measured the same one-sided shape, 7 pos / 43 neg over the
  5-match diagnosis). Live it was doubly wrong: `tid` unlit also covers every frame the DETECTOR
  could not read, so it punished defending exactly when perception was blind (C4).
- BUILDING BRANCH ADDED (live/sim parity gap): the sim grades a building on counter role ALONE
  (a building ATTRACTS -- central Tesla vs an off-lane wincon is the classic right play); live
  still demanded same-lane intercept, so the deck's signature defensive play earned ZERO live.
  One of the two missing reachable positives, recovered by mirroring sim/env.py's branch.
- cycle_plan / cycle_waste DELETED live (assembly, helpers, weights). Sim's copy went at 110
  fires / 0 positives; the live copy measured 2 pos / 24 neg -- the third recorded instance of
  the same action-tax shape. config knobs marked INACTIVE (kept for the collapse history),
  config_edit no longer offers them, sim's dead reads/never-called helper trimmed (no behaviour
  change; the sim's documented _cycle_plan stub stays).
- PERCEPTION-GATED SPEND (interim until the trade rework): a defensive play (cy >= 0.5) on a
  frame where the detector recognised nothing but the coarse mass reads ACTIVE no longer pays
  the trade term's elixir spend (_perception_blind, alongside the existing forced-spend waiver).
  The mass-delta half still applies EVERY step, so the telescope/anti-farm property is intact --
  this waives a tax in unmeasurable states, it does not add a term. Subsumed by the elixir_trade
  rework when that lands.

LIVE DECISION CADENCE (plan item 5 -- measured, then fixed):
- MEASURED: the 5-match JSONL gives 3.3-5.5 s/step including nav; per-component bench on a real
  frame (under PPO-training CPU contention, so upper bounds): clock.update **865 ms** (2x/3x
  badge = up to 2 templates x 8 scales x whole-frame matchTemplate, EVERY step),
  recognize_hand 301 ms (large per-card template stacks), detect_state 230 ms, tower-HP OCR
  40 ms, threats 21 ms, rest <10 ms -- pipeline sum ~1.5 s. Old loop shape = sleep(act_period
  1.0) THEN pipeline => ~2.2-2.5 s served vs the trained 1.0 s. Spell casts additionally stall
  up to ~3.6 s sampling the impact frame (and this deck is spell-heavy).
- FIX 1, badge window (clock.py + elixir.badge_check_window_s 20): the badge NEVER decides the
  multiplier (the clock is authoritative), so the cross-check now runs only within +-20 s of an
  expected transition. Zero behaviour change, ~0.87 s/step returned. play.py benefits too -- it
  was paying clock.update at its 6 Hz poll rate.
- FIX 2, paced wait (env.py): the act wait is now anchored to the LAST FRAME GRAB -- wait only
  for what REMAINS of act_period, so the pipeline runs INSIDE the period. Served cadence
  converges to act_period whenever pipeline+learner fit inside it (and to their sum otherwise),
  instead of always being their sum plus the period. Event-driven early wake unchanged.
  (play.py already paced correctly via its poll loop -- no change there.)
- INSTRUMENTATION: env.step now accounts every phase (loop/wait/spell/grab/vision/hand/threat/
  obs/act); each match prints a `[cadence]` line and appends the means to the reward-stats JSONL
  ("cadence" + "decisions" fields), so the trained-vs-served mismatch stays measured. `loop`
  minus the named phases ~= the trainer's inference+learn share.
- REMAINING, deliberately not touched: the spell-impact sampler (retired by the elixir_trade
  rework -- see below); recognize_hand's 301 ms template stack and detect_state's 230 ms
  multi-template sweep (optimise only after the next live run's [cadence] lines confirm their
  uncontended share).

ELIXIR_TRADE REWORK: DESIGN COMMITTED, NOT IMPLEMENTED (plan item 3 -> icebow/ELIXIR_TRADE_DESIGN.md):
- Decision: mirror the SIM's reworked term (a true potential over both sides' board value +
  elixir bars, where a deploy is a TRANSFER and only consequences move the score) rather than
  per-play attribution windows, which would re-introduce hand-written classifiers with tunable
  attribution knobs -- the family that has failed three times. Live sources: own bar = pip read;
  enemy bar = OpponentElixirEstimator; board value = team-tagged detector tracks x KB elixir
  (full value until a track dies -- HP is unobservable live; the transfer property makes a
  missed enemy deploy self-cancelling in the potential). Perception-validity gate: delta-phi
  only between healthy perception frames, else carried forward (freezes BOTH signs).
- RETIRES: the blocking spell-impact sampler (the settlement reads consequences off the regular
  frame stream) and the interim _perception_blind spend waiver. Acceptance gates in the doc;
  headline: elixir_trade must fire BOTH signs live, and a no-play segment must net ~0 (telescope
  check). Implement as its own change AFTER the current sim run is judged.

Files: env.py, clock.py, config.yaml, config_edit.py, sim/env.py (dead code only), README.md,
ELIXIR_TRADE_DESIGN.md (new), data/policy_stats_bc_init.json (new measurement). Reward changes
are LIVE-ONLY and take effect on the next train-rl; the running train-sim-ppo is unaffected.


------------------------------------------------------------------------------
2026-08-12 (night)  —  wincon_exec farm caught mid-run; repeat-credit gate + Hunter X-Bow doctrine
------------------------------------------------------------------------------
MID-RUN DIAGNOSIS (the from-scratch PPO retrain, ~2.7h in). The user flagged a sharp console
winrate drop. Part of that is structural (selfplay_prob 0.5 ramps over 5k matches and PFSP power
2.0 over-samples the snapshots the agent LOSES to; the cell floor keeps 25% of placements random),
but two greedy policy-stats snapshots 105 min apart exposed a real regression:
- PLACEMENT re-collapsed 16 -> 5 cells (top share 61% -> 76.8%); the mode MIGRATED from a
  bottom-edge dump tile (0.31, 0.98) to ONE central tile (0.47, 0.65) shared by SIX cards
  (x_bow 297 / log 275 / skeletons 266 / knight 204 / tesla 77 / knight_evo 64 of its 1188 plays)
  -- the constant-cell attractor of 2026-08-12 morning, re-formed around a new food source:
- wincon_exec flipped -100.2 (82+/218-) -> **+687.9 with 374 of 374 fires positive** (~6.2/match,
  saturating correctness_cap): the X-Bow credit was RENEWABLE BY RE-PLACEMENT, so parking every
  card on the credited centre tile and re-dropping the bow forever became the reward optimum
  (59/60 losses; policy_sim_ppo_best stale since 20:09 -- the keep-best gate did its job).
- Side symptom: ALL 241 rocket plays went to one corner tile outside any aim radius (spell_waste
  221 fires, never positive) -- consistent with wincon-bank pressure (rocket in hand masks cheap
  cards at >=3 elixir, so dumping it fastest unfreezes the hand) plus a constant cell head.
VERDICT: stop the run; do NOT plain-resume (the attractor is reward-geometric); fix the one
measured exploit; restart from the BC init.

REPEAT-CREDIT GATE (sim/env._wincon_exec + live twin in env._wincon_exec_live):
- An X-Bow deployed while an allied X-Bow is ALREADY STANDING earns NO placement credit. The gate
  nulls ONLY the positive side: a misplace while one stands still charges, and a double-bow is
  neutral, never punished (a real play at triple elixir). SEQUENTIAL re-placement -- the defensive
  cadence icebow leans on at 2x/3x -- re-credits every time, because the previous bow is dead.
- Sim: `_ally_xbow_standing` is read PRE-deploy in step() so a just-placed bow cannot gate its own
  credit. VERIFIED: 1st bow +1.20, 2nd while standing +0.00, 3rd after the first dies +1.20.
- Live twin: live cannot reliably SEE its own bow, but it KNOWS when it played one -- "standing" =
  within the KB lifetime (30 s, from cards_stats lifetime_s) of our last X-Bow play. Perception-
  independent; worst case briefly over-waives a bonus after an early bow death. Reset per match.
- Self-limit math: at lifetime 30 s a continuous re-placement chain maxes ~10 credits/match, and
  correctness_cap still bounds the total -- but the chain now requires the previous bow to DIE,
  which is precisely the consequence-coupling the flat credit lacked.

HUNTER CR X-BOW DOCTRINE, MEASURED (data/replay_bc/*/dataset.npz, 89 X-Bow plays over 4 videos),
prompted by the user's correction that icebow leans on DEFENSIVE X-Bows far more than other siege:
- 47 (53%) OFFENSIVE in tower range, mean (0.48, 0.50): behind-bridge spots BOTH lanes
  (x 0.16/0.27 and 0.79/0.84 at y 0.50) plus central front-row (0.42-0.63, y 0.46-0.53).
- 22 (25%) DEFENSIVE centre band, mean (0.51, 0.57), tight in x 0.42-0.63 / y 0.56-0.59 --
  squarely inside env.xbow_defense_front/back (0.52-0.62): the band geometry is validated.
- 18 (20%) "other" -- but ~10 of those are central FRONT-ROW bows (0.47-0.53, y=0.50) that the
  SIM scores as in-range via tile_dist (~10.2 tiles <= 11.5) while the LIVE env's isotropic
  normalized `d <= xbow_range 0.36` calls them out-of-range (misplace!). PARITY BUG, live-side
  only, noted for a later gated fix: the live range test needs tile-aspect-correct distance.
- Elixir at play: mean 7.3 (offensive) / 7.0 (defensive) -- he BANKS before committing, exactly
  the anti-anti-hoarding doctrine already on the do-not list.
- WHEN, measured (CPU tray-scan, x_bow leaving the hand + 2x/3x badge at each play ->
  data/analysis/xbow_timing.json; badges verified transferring to this footage, best x2 0.92 /
  x3 0.88). NB classify the badge by ARGMAX of the two scores -- x3-first priority misread most
  2x badges as 3x (the small glyphs cross-match at 0.7+).
  * 76 plays kept over ~143 min of footage, est. ~14 matches -> **~5.4 X-Bow plays/match** --
    the user's pushback confirmed; the earlier "1-3/match" guess was far too low.
  * PHASE split: 1x 39 / 2x 32 / 3x 5. Double elixir is only ~40% of a match's clock, so the
    per-minute X-Bow rate RISES at 2x. Approximate WHERE x WHEN join (order-aligned, directional
    only): offensive bows 25/21/4 across 1x/2x/3x, defensive-band bows 7/10/1 -- the defensive
    share grows from ~19% of 1x plays to ~30% of 2x plays. CAVEAT the tray-gone guard discarded
    226 candidate events (all of video 1's), and discards cluster at match END -- so the true
    late-game share is likely HIGHER than measured, favouring the same conclusion.
  * RE-BOW CADENCE: within-match gaps median 24 s, p25 8 s -- FASTER than the 30 s lifetime, so
    his bows usually DIE early under attack and are re-placed promptly. The SIM gate keys on
    actual death (engine truth) and credits exactly this. The LIVE twin's 30 s lifetime window
    is therefore TOO CONSERVATIVE at real icebow cadence (it would waive legitimate re-bows
    after early deaths) -- flagged: replace with detector death-evidence or a shorter window
    BEFORE the next live fine-tune. Sim-side training is unaffected.
  * OBSERVATION for later (not a change): Hunter still throws 21 offensive bows at 2x, while the
    sim's phase machine calls a forward bow a MISPLACE once 2x arrives without breakthrough
    (xbow_success_frac flip). The hard flip may be harsher than his real doctrine -- re-measure
    against match context before touching it.
  * ACCEPTANCE for the restarted run, calibrated: X-Bow plays/match healthy at ~3-7 (Hunter 5.4);
    wincon_exec positives must track distinct deployments; offensive:defensive placement mix
    ~2:1 with the defensive share rising by 2x.

RESTART: stop the running train-sim-ppo (Ctrl+C saves; best@20:09 is preserved), then fresh
`run.py train-sim-ppo --matches 100000 --envs 32 --init data/policy.pt`. Acceptance per the
standing gates plus: wincon_exec positive fires must track DISTINCT bow deployments (no standing
bow), not re-placements; rocket whiff-corner loop should decay; cells_used judged against the
BC-init baseline of 1 (data/policy_stats_bc_init.json).

DEFENSIVE-RESPONSE DOCTRINE: coverage audit + Hunter's measured response matrix (user directive:
the X-Bow is not the only thing to mine -- the model must recognise the exact enemy troop, link
it to a threat class, play the KB-correct counter (Tesla for a wincon, Ice Wizard for a swarm),
fall back to legitimate MITIGATION when the counter is out of rotation (Ice Wizard + Skeletons vs
a wincon), and learn the synergies: rocket+nado, ice-wiz+nado, nado-to-king activation on melee,
nado dragging a troop back into a placed building's aggro. Never grade random cards in random
places as a "response". Recorded here because parts predate the logs.)
- ALREADY ENCODED (audited against card_threat.py + sim/env.py):
  * recognise -> detector whitelist; classify -> identity_threat_vector role flags;
  * counter -> counters() role table: building-targeter->building (Tesla vs Hog), swarm->splash/
    spell (Ice Wizard), air->attacks_air, tank->building/DPS>=150, and the BARE-WINCON branch
    (non-tank wincon -> ANY body) is what makes the Ice-Wiz+Skeletons mitigation stack a CREDITED
    threat_response, not a threat_miss -- the fallback the user requires is already legal;
  * holding with NO counter in hand is NOT taxed (threat_miss_idle requires an affordable counter
    in hand), so "do what you can" never competes with a phantom optimal;
  * all four tornado synergies live in sim _nado_shaping: clump, >=2-pulled-die combo (the
    rocket/ice-wiz payoff), king-activation pull, and the retarget drag credit.
- GAPS (flagged, each its own later change): live env has NO nado execution credits (sim-only,
  engine truth; the elixir_trade potential rework is the live channel for consequence credit);
  role table scores Ice Wizard as a NON-counter to a TANK-classed wincon (Giant), where doctrine
  reads it as legitimate support -- consequence terms (counterfactual/chip) carry that case;
  rewards.strategy_prior_scale still 0 (priors not yet active).
- HUNTER'S RESPONSE MATRIX, measured TODAY from the npz threat blocks (each mined play stored the
  identity block at play time; 1182 plays, 189 (16%) with a recognised threat -- recall-limited
  on the foreign rendering, so treat as a PRIOR not a census):
  * vs SWARM (n=76): skeletons 64%, log 11%, ice_wizard 8% -- cheap chaff + splash, placed in the
    threat lane mid/front (L-mid x21, R-front x12).
  * vs TANK (n=56): log 21% / knight 18% / skeletons 14% / tesla 12% -- mitigation STACKING.
  * vs recognised-other (n=41): ice_wizard 68%, centred C-mid -- the universal support/slow.
  * vs BUILDING-TARGETING WINCON (n=12, mean depth 0.07 = answered AT the bridge): skeletons 33%
    (distraction), tesla+tesla_evo 25%, ice_wizard 17% -- "Tesla for the wincon" + body fallback,
    exactly the doctrine chain.
  The counters() table grades everything above as positive-by-role -- the KB mapping and the
  pro's actual play AGREE, which is the strongest validation the role table has had.
- NEXT (queued, detector-gated): after board-25 finishes + (if) promotes, run `mine-replays` over
  the Hunter videos -> StrategyPriors (placements[threat_bucket][card][zone] + trade stats +
  rocket conditions); THEN decide rewards.strategy_prior_scale > 0 as its OWN gated change. That
  is the formal channel by which "where he plays his other cards against which threats" shapes
  the reward, replacing intuition with his measured priors.


------------------------------------------------------------------------------
2026-08-13  —  board-25 verdict: NO promotion — board-24-5 was never cut off, it early-stopped
------------------------------------------------------------------------------
- board-25 (yolo11s / 960 / batch 4 / seed 0 / deterministic, board-24-5's exact recipe on the
  unchanged dataset) ran 6.4 h and EARLY-STOPPED at epoch 69, best at epoch 39 (patience 30) --
  the IDENTICAL trajectory and stop as board-24-5, epoch-for-epoch to 3 decimals. best.pt is
  BITWISE IDENTICAL to the incumbent (499/499 tensors equal). VERDICT: nothing to promote; the
  detect.weights pin stays on board-24-5. No frozen-subset eval needed -- identity implies
  identical numbers by construction.
- The premise behind the run ("board-24 was cut off mid-training and didn't reach its full
  potential") was WRONG: 24-5 stopped at a genuine 30-epoch plateau. The signature was already
  in its results.csv (best@39 + patience 30 = stop@69) and was checkable BEFORE spending the
  GPU-hours -- lesson recorded: read the EarlyStopping line / best-epoch math before re-running
  a "truncated" training. What the replay DID buy: a hard receipt that training is deterministic
  and the dataset was unchanged since board-24-5 -- useful, but not worth 6.4 h on purpose.
- CONSEQUENCE: this recipe+dataset's ceiling IS the epoch-39 model. The levers that can actually
  move the detector, per the measured 2026-08-07 scaling law + gap list: (1) LABELS -- fireball
  (12 boxes, 21.6% of meta decks), zap_aoe (0), lightning_aoe (0); `label-queue` ranks the
  backlog; the annotation-harvest frames from train-rl/record sessions are waiting in
  images/to_label. (2) A real RECIPE change as its own experiment (yolo11m at batch 2-3 is the
  candidate; raising patience alone cannot help a genuine plateau; a new seed is noise-level).
- UNLOCK: `mine-replays` (Hunter strategy priors) no longer waits on a detector generation --
  the detector didn't change, so it can run on the incumbent whenever the user gives the go
  (GPU is free again).


------------------------------------------------------------------------------
2026-08-13 (morning)  —  Overnight PPO regression measured: keep-best vindicated, self-play suspect
------------------------------------------------------------------------------
- USER flagged policy_sim_ppo_best.pt stale since 23:27 (57 min into the gated restart). Matched
  150-match policy-stats on BOTH checkpoints (same seed/protocol) settles it -- a GENUINE
  regression, not gate conservatism:
    BEST 23:27:  win 19.3% (the strongest sim policy measured to date), 25.3 plays/match,
                 towers taken 122, wincon_exec 649+/4-, FULL deck in use (tesla 453,
                 tesla_evo 151, ice_wizard 190) -- but placement narrow: 7 cells, top 85.6%.
    LAST 07:21:  win 1.3%, towers 2, chip_offence 2478 -> 381 fires, rocket spam back up
                 (540 plays, spell_waste ~5/match), support cards dying (ice_wizard 35,
                 tesla_evo 6, tornado 10) -- but SPREAD doubled: 14 cells, top 40.8%.
- READINGS: (1) The keep-best gate did exactly its job all night -- nothing after 23:27 deserved
  promotion. (2) SPREAD AND STRENGTH ARE ANTI-CORRELATED across these snapshots: a 7-cell policy
  hit 19.3% vs the scripted pool, so placement spread is NOT the binding constraint on strength
  at this stage -- the cell floor (0.25 = a quarter of training placements random) is paying its
  exploration cost without buying benchmark wins right now. (3) The regression window coincides
  with the self-play ramp maturing (selfplay_prob -> 0.5, PFSP power 2.0 over-sampling the
  hardest snapshots): the policy kept optimising against its league while its performance vs the
  FROZEN SCRIPTED benchmark fell -- classic league drift; the benchmark pool is scripted.
- ACTION: champion archived as data/policy_sim_ppo_best_20260812_2327_win19.pt (a fresh run's
  first promotion would otherwise overwrite _best with a weaker policy -- best_wr resets on a
  non-resume start).
- RATIFIED (user, 2026-08-13 morning): selfplay_prob 0.5 -> 0.0 in config for the attribution
  run; the user restarts FROM the champion (--init data/policy_sim_ppo_best_20260812_2327_win19.pt).
  If the scripted benchmark resumes climbing past 19.3%, the self-play mix was the regressor;
  reintroduce it softer (0.25 / pfsp_power 1.0) only after scripted-pool strength is banked.
  ALSO decided: the detector STAYS yolo11s -- live inference speed is a priority (fast in-match
  decisions; s->m would halve perception_hz for an estimated +1-3pp recall). The detector lever
  is LABELS (label-queue priorities), not model size. QUEUED BEHIND the attribution run (each
  its own gated change): anneal
  ppo_cell_explore_floor 0.25 -> ~0.10 (spread recovered 1 -> 14; the plan says anneal once it
  does), and the threat_miss_idle x wincon-bank interaction (the bank masks affordable cheap
  counters while the bar climbs; threat_miss_idle then charges -1/step for the hold the bank
  itself mandated -- measured -21/match on the CHAMPION, dwarfing the +/-2 outcome signal).


------------------------------------------------------------------------------
2026-08-13 (evening)  —  First live session on the repaired rewards + sim attribution day-read
------------------------------------------------------------------------------
LIVE train-rl, ~9 h from the champion (259 matches, real Classic 1v1 ladder):
- RECORD 1-258. Brutal but informative -- real humans, not the scripted pool. The two collapse
  signatures the session was watching for did NOT appear: plays/match held flat (15.7 first
  third -> 16.5 last third; no refusal spiral), and threat_response came out NET POSITIVE live
  (+139.7; 545 pos / 405 neg) -- the reachable-positive repair works under live perception.
  elixir_trade remains the largest shaping negative (-2.7/match; the potential rework stays
  queued), and the keep-best gate correctly never promoted, so policy_rl.pt is STILL the intact
  champion copy.
- THE BINDING FAILURE IS SERVING CADENCE, again, with a new culprit: recognize_hand degraded to
  1.28 s MEAN (1.78 s by day's end) under all-day CPU contention -- the per-card template stacks
  (~20 variants x 10 identities x 4 slots each step). Full loop 2.5-3.0 s vs the trained 1.0 s;
  the paced wait sat at 0.00 s (pacing works; the pipeline alone overruns the period 2-3x).
  QUEUED FIX (own change): slot-change caching -- frame-diff the 4 slot crops (~1 ms) and only
  re-match a slot that changed; plus cap templates per identity. Validate with verify --hand.
- X-Bow barely exists live: wincon_exec fired 0.4/match live vs 5.4 in the sim evening read --
  cadence + sim-to-real gap; revisit after the hand-reader fix.
- BUG (mine, fixed): the new per-play telemetry was silently LOST all day -- dump_match merges
  match_summary() after the extra dict, and both used the key "plays", so the int count
  clobbered the array. Renamed to "play_log"; next session records per-play detail.

SIM attribution run (scripted-only from the champion), day read at ~9.5 h:
- win 5.0% (07:39, re-init update shock) -> 13.0% (17:14, 100 matches), plays/match 30.9,
  X-Bow 5.1/match (Hunter band), wincon 543 fires / 0 negative, full deck in use except tornado
  (greedy never picks it; sim-side note). best.pt banked once at 08:18 and not since --
  RECOVERING toward the champion, not yet past it.
- ATTRIBUTION verdict so far: removing self-play STOPPED the decay (last night 19.3 -> 1.3
  overnight; today monotone recovery all day). Self-play (0.5 + PFSP^2) stands as the regressor.
  But scripted-only is treading water NEAR champion level rather than surpassing it -> the next
  queued change is due: anneal ppo_cell_explore_floor 0.25 -> 0.10 (spread has been paid for;
  13 cells / top 82.8% is the same shape at a quarter random placements' cost) on the next
  restart from the champion.

DECISIONS RECORDED (user Q&A): optimizer is ALREADY Adam in both trainers (train_sim_ppo Adam
lr 5e-5 eps 1e-5; train_rl Adam) -- no optimizer change; every measured failure has been reward
geometry, league dynamics, or serving, never optimisation. An LLM bridge between vision and
policy: declined as a RUNTIME component (the engineered KB layer already is the semantic bridge,
at zero latency; an LLM would deepen the exact cadence bottleneck just measured) -- LLM value
lives OFFLINE here: KB curation, doctrine-from-guides, automated jsonl post-mortems. Adaptive
opponents: agreed in direction; sequence = gentle self-play reintroduction (0.25 / PFSP 1.0)
after banking scripted strength, then REACTIVE upgrades to the scripted bots; full ML opponents
piloting meta decks deferred (per-deck action spaces = a large lift the mirror league avoids).


------------------------------------------------------------------------------
2026-08-13 (night)  —  Hand-reader slot cache + sim round-2 package (floor anneal, gentle selfplay)
------------------------------------------------------------------------------
HAND-READER SLOT CACHE (vision.py + hand.cache_diff/cache_ttl_s) -- the fix for the day's live
cadence killer (recognize_hand 1.28-1.78 s/call):
- Each tray slot keeps a small COLOUR thumb of its last crop + the matched id; a call re-matches a
  slot only when (a) the thumb moved >= cache_diff, (b) the previous match was NOT confident
  (score < match_threshold + 0.15 -- near-threshold results flip between lookalikes on pixel
  noise and are never cached; they re-match every call, bit-identical to the uncached reader), or
  (c) the entry is older than cache_ttl_s (4 s hard staleness bound). recognize_next same deal.
- LESSONS from the equivalence tests: grayscale thumbs slept through hue-only flips (fixed:
  colour thumbs); the first "mismatches" were on the STALE Aug-04 pre-UI-update recordings where
  the PLAIN reader itself oscillates across four identities on one slot -- an unstable oracle.
  VERIFIED on current-UI footage (live overlay clips from the 08-13 session): 300 frames,
  **0 mismatches**, ~3x faster uncontended -- and the win compounds under load, which is exactly
  when the uncached reader was being starved to 1.8 s. Expected live loop: ~2.5-3.0 s -> ~1.2-1.5 s
  (verify with the [cadence] line next session).
SIM ROUND-2 CONFIG (takes effect on the next train-sim-ppo restart; each knob independently
revertable for bisection):
- ppo_cell_explore_floor 0.25 -> 0.10: repopulation phase over (spread 1 -> 13-16 across all
  post-gate reads; the 19.3% champion is a 7-cell policy -- spread is not the binding constraint,
  and 25% random placements is pure benchmark drag now).
- selfplay_prob 0.0 -> 0.15 + selfplay_pfsp_power 2.0 -> 1.0: gentle reintroduction per plan +
  the user's DECK-RARITY point: icebow is rare on ladder, and the frozen self can ONLY pilot the
  mirror deck (its card head is our 10 identities; meta-deck ML opponents would need per-deck
  nets -- deferred). So the mirror stays a ~1-in-7 slice and deck variety lives in the scripted
  85%: 100 meta decks with adaptive_prob 0.65 of bots reactive (anti-siege / counter-hold /
  dump-punish / split-push -- that Tier-1 adaptive layer already existed and is ON for training,
  benchmark frozen non-adaptive).
- User Q&A recorded: self-play "across several meta decks" is architecturally mirror-only today;
  the implemented interpretation = small mirror slice + adaptive scripted meta pool.
RESTART: stop the attribution run when ready, then
  run.py train-sim-ppo --matches 100000 --envs 32 --init data/policy_sim_ppo_best_20260812_2327_win19.pt
Gates: benchmark must climb past 19.3% and bank promotions; wincon fires track deployments;
X-Bow 3-7/match; watch the first checks for any floor-anneal spread regression (>= 13 cells held).


------------------------------------------------------------------------------
2026-08-14  —  Round-2-from-BC failed fast (floor/init mismatch); round 3 from the champion
------------------------------------------------------------------------------
- The user restarted round 2 FROM THE BC INIT (policy.pt) rather than the champion. At ~1.9 h the
  matched 60-match read showed the failure the anneal note warned about, in a sharper form than
  predicted: cells_used **2** (top cell 90.8%), win 5%, and -- the mechanism -- ALL 332 X-Bow
  plays on the BC head's frozen tile (0.31, 0.77), off-centre and deep, so wincon_exec charged
  the misplace on every one (391 fires, 59+/332-, net -175.4). Under floor 0.10 the frozen
  argmax never repopulated (the 0.25 floor did 1 -> 16 cells in <2 h on the same init a day
  earlier), so the run was carving "never play X-Bow" into the card head via the placement it
  could not fix. NOT a self-play effect (signature present from t0, before the ramp matters).
- LESSON, recorded: the 0.10 anneal is valid only for an ALIVE cell head (champion-class inits).
  A from-BC restart re-enters the repopulation phase and needs 0.25 until cells_used reaches
  dozens, THEN the anneal. Floor and init are a PAIR, not independent knobs.
- ROUND 3 (user chose option 2): failing run stopped; relaunched FROM THE CHAMPION
  (policy_sim_ppo_best_20260812_2327_win19.pt) under floor 0.10 + selfplay 0.15/pfsp 1.0 --
  the pairing the anneal was designed for. Launched DETACHED (survives session closes) with
  stdout/err at data/sim_ppo_r3.log / sim_ppo_r3.err.log. Gates: hold >= champion spread
  (7 cells / 85.6%), wincon strongly positive again (the 649+/4- signature), best banks past
  19.3%; league-drift watch as selfplay ramps.


------------------------------------------------------------------------------
2026-08-14 (night)  —  Live smoke test: cadence FIXED; placement collapse root-caused to the
                       CELL-HEAD ARCHITECTURE (dense readout, no spatial structure)
------------------------------------------------------------------------------
LIVE SMOKE TEST (user, ~2 matches on the champion):
- Cadence fixes VERIFIED in serving: loop 1.26 s (was 2.5-3.0), hand 0.40 s under contention
  (was 1.28-1.78 -- the slot cache works), wait 0.17 s (pacing active), per-play play_log
  recording correctly for the first time.
- Placement: **44/44 plays on ONE tile (0.369, 0.591)** -- seven different cards, including all
  7 plays with a recognised threat, and including Tornado (which sim-greedy never plays). The
  user called it: single-tile regardless of the enemy.
PROBE (champion net, frozen context vectors, only the image varied; 40 live frames from the
session's overlay clip + 40 sim states):
  A real RGB + canvas:    1 distinct cell (352)     C real RGB + zero canvas: 1 cell (352, == A)
  B zero RGB + canvas:    1 distinct cell (232)     D sim obs, frozen vecs:   2 cells (88% top)
                                                    E sim obs, REAL sim vecs: 1 cell (100%)
- A == C exactly: the canvas contributes ~nothing to the cell argmax when RGB is present.
- D/E: the head is near-constant EVEN ON SIM OBSERVATIONS. The champion's "7 cells / 85.6% top"
  from policy-stats was context-switching between a few fixed tiles, not board-reading; the live
  obs shift merely RELOCATES the constant (and to a worse tile). Sim-to-real is real but
  SECONDARY.
ROOT CAUSE (model.py): `cell_head = nn.Linear(embed, n_cells)` reads 432 placement logits out of
a GLOBAL embedding whose image half passed through a Linear(64*6*4 -> 256) bottleneck. There is
NO spatial correspondence between board positions and cell outputs -- "put the Tesla in the
Hog's lane" requires the trunk to encode exact positions into a global vector and a dense layer
to decode them back out, which three generations of training provably never learned. The floors
/entropy work treated the SAMPLING symptom; the function class itself fights board-conditioned
placement.
PROPOSED FIX (needs user go; it is an architecture change + fresh training): SPATIAL cell head
-- keep the conv feature map (12x8 before the flatten), condition it on the context embedding
(broadcast-concat or FiLM), 1x1 convs -> a logit MAP reshaped/upsampled to the 18x24 grid.
Placement then inherits translation structure from convolution: threat features at x produce
logits near x by construction. Fold the CANVAS-ONLY image branch (drop RGB, in_ch 9 -> 6) into
the same rebuild -- the canvas is rendering-independent, killing the sim-to-real image gap in
the same retrain. COST: PolicyNet change; old checkpoints incompatible (fresh sim run + BC
re-init); train_bc/train_rl/train_sim_ppo/play/policy-stats touch the same heads and need a
coordinated pass. Round-3 keeps running meanwhile (its card/timing learning is still valid
science; its placement ceiling is now understood).


------------------------------------------------------------------------------
2026-08-14 (late night)  —  SPATIAL CELL HEAD implemented (model.py); retrain path defined
------------------------------------------------------------------------------
- PolicyNet's cell head rebuilt as a LOGIT MAP (user go): pre-pool conv feature map (12x8 for the
  96x64 obs) conditioned on the full context embedding (broadcast-concat) -> 1x1 convs -> 1-ch
  map -> bilinear resize to the 18x24 grid -> flatten in ActionSpace's row-major order. The
  global path (pool -> trunk -> card head, RL gate heads) is UNCHANGED in shape and parameter
  names; embed stays 328; n_cells/n_cards/threat_dim unchanged, so every consumer (train_bc,
  train_rl, train_sim, train_sim_ppo, play, sim_view, policy_stats) works without edits. The
  grid is inferred from n_cells ({432:(18,24), 576:(18,32)}; grid_wh param to override).
- VERIFIED: shapes + embed width; UNTRAINED net's cell argmax already TRACKS a moved blob on
  both axes (gx 5->17 left->right, gy 3->18 top->bottom) -- the translation structure the dense
  head could not express and never learned. Old checkpoints fail a strict load (cell_head.* gone)
  -- deliberate: the head must be retrained.
- CANVAS-ONLY (drop RGB) SEQUENCED AFTER, not folded in (revised from the first proposal): the
  zero-RGB probe was confounded (an RGB-trained net sees zero-RGB as OOD), so the honest order is
  spatial head first, re-run the live A/B probe on its checkpoint, and only flip in_ch 9 -> 6 if
  RGB still carries the live gap. One architecture change per training cycle.
- RETRAIN PATH (no re-recording, no re-mining): (1) run.py train-bc --data data/replay_bc
  --val-frac 0.2 (existing Hunter datasets feed the new head; BC now trains context->PLACE on his
  pairs, which the map head can actually generalise); (2) stop round-3 (old arch; its weights
  cannot transfer); (3) run.py train-sim-ppo --matches 100000 --envs 32 --init data/policy.pt.
  ppo_cell_explore_floor set BACK to 0.25 for this from-BC run per the floor/init pairing rule;
  anneal to 0.10 after spread + board-conditioning are measured on the new head.
- PENDING (user to give next steps): DOCTRINE-PRIOR EXPLORATION -- the user's scaffolding idea
  (guide placements for specific card-vs-threat situations, then remove). Agreed, with the shape
  that keeps PPO exact and avoids the one-sided-term trap: a sampling MIXTURE like the existing
  floors (doctrine distribution instead of uniform for the guided share, mixture log-prob stored,
  rollout-only, annealable to 0, eval/live untouched, acceptance measured with guidance OFF).
  Their three examples map onto existing live aim-assist helpers the sim never used:
  xbow_lock_cell (in-range band), tesla_pull_cell (centre pull vs lane wincon), and the
  defensive intercept spot (body on a ranged attacker's lane at the bridge).


------------------------------------------------------------------------------
2026-08-14 (day)  —  DOCTRINE.md reviewed+corrected; doctrine-prior exploration implemented
------------------------------------------------------------------------------
- DOCTRINE.md (57 counters / 10 synergies / 6 standing priors) reviewed by the user; corrections
  applied and independently confirmed online: #14 anti-EQ Tesla = the "(0-3)" spot (centre, ~3
  tiles from river -- EQ anchored at the princess's inner corner kills every standard-depth
  centre Tesla; Theria Tesla guide); #15 Rocket-on-Balloon demoted to LAST resort; #27 skels ON
  a tower-locked bridge princess; ALL log-vs-air rules purged (The Log cannot touch air -- #19
  rephrased to the spawned skeletons, #25/#38 IW/pulse only); #41 Firecracker KING-ACTIVATION
  bait (unit 4 tiles in front of the king, 2 inside the princess; her shrapnel splashes the king
  -- zleague/TikTok tech, "works with most units"); #49 Mortar = Knight INTO the 3.5-tile blind
  spot (forces lock loss/retarget; wiki + clash.world) + near-bridge Tesla answers a bridge
  mortar.
- IMPLEMENTED sim/doctrine.py (tranche 1) + train_sim_ppo integration + sim.doctrine_frac (0.6):
  when a rule matches (card, ENGINE state), that share of the CELL FLOOR's mass samples from the
  doctrine distribution instead of uniform. Rollout-only, mixture log-prob stored (PPO exact),
  annealable to 0 = the user's "remove the hardcoding". Rules: Tesla band + anti-EQ row flip;
  X-Bow behind-bridge locks with punish-lane weighting + EDGE column vs rocket decks + defensive
  centre; Knight body-block / mortar blind-spot walk-in / FC king-bait; skels surround + princess
  smother + cycle corners; IW depth band; nado king-activation + clump (never a lone tank);
  ground-only Log paths (HARD air guard); rocket pump / weak-tower / tower+support aims.
- VERIFIED on synthetic engine states: every rule fires where DOCTRINE.md says, the Log rule
  returns None on an air-only board, and the anti-EQ Tesla flips to a DISTINCT grid row (14 vs
  15) -- caught + fixed a QUANTISATION trap: the 24-row grid is 1.33 tiles/row, and the first
  depth constants landed both Tesla depths in the same row, silently erasing the EQ distinction.
- LAUNCH SEQUENCE for the spatial-head retrain (everything now in place): stop round-3 (old
  arch; archive its best if wanted) -> train-bc --data data/replay_bc --val-frac 0.2 -> fresh
  train-sim-ppo --matches 100000 --envs 32 --init data/policy.pt, which loads: SPATIAL cell head
  + cell floor 0.25 (from-BC pairing) + doctrine_frac 0.6 + selfplay 0.15/pfsp 1.0 + wincon gate.
  Gates: spread + BOARD-CONDITIONING (the live/sim A-B probe repeated on the new checkpoint),
  wincon two-sided, X-Bow 3-7/match; anneal doctrine_frac then the floor as greedy internalises.


------------------------------------------------------------------------------
2026-08-14 (midday)  —  Round-4 @14k evaluated; threat_miss_idle x wincon-bank waiver shipped
------------------------------------------------------------------------------
- ROUND-4 (spatial head) at 14k matches, 100-match greedy read: win 12.0% (ON the historical
  from-BC trajectory -- the user's "extremely low" was the ROLLING TRAINING winrate, which this
  run's 25% cell floor + doctrine sampling depress by construction). The architecture is
  delivering: **60 cells used / top 36.3%** (old arch: 5-16 / 61-91%), the board-conditioning
  probe IMPROVED with training (t0 12 cells/60% top -> 14k: 18 cells/20% top), X-Bow 3.4/match,
  wincon_exec +494 two-sided, 66 towers taken. Doctrine X-Bow spots audited vs sim tile_dist:
  ALL in range (10.2 <= 11.5) -- the prior feeds credited placements.
- THE MEASURED THROTTLE: threat_miss_idle -1595/100 matches (16/match, 3x the next term) + leak
  -466, on 16.6 plays/match with 57% FORCED waits. Mechanism: the WINCON BANK masks every card
  under 6 elixir while a wincon is held and the bar climbs from 3 -> the policy is FORBIDDEN its
  counters, and threat_miss_idle charged -1/step for the hold the bank itself mandated. Also why
  the support trio starved (ice_wizard 16 / tesla_evo 8 / tornado 1 of 1664 plays -- all live in
  the masked cost range).
- FIX (sim/env._threat_miss_idle): "answerable" now means playable UNDER THE SAMPLER'S OWN RULE
  -- a counter that wincon_bank would mask at this (hand, elixir) does not make the hold a miss.
  Bank params mirrored into the env (pure function of hand+elixir, scalar form of
  wincon_bank.apply_wincon_bank). VERIFIED 3-state: banking (tesla+xbow in hand, elixir 5) ->
  waived; elixir 10 (bank off) -> charges; no wincon in hand -> charges. Live env untouched (no
  bank exists there). Removes a false penalty; adds nothing -- two-sided-safe by construction.
- RESTART: --resume (NOT --init policy.pt, which would reset to BC): keeps the 14k spatial-head
  progress + best_wr. Next checks should show tmi total collapsing, plays/match recovering from
  16.6 toward the mid-20s, and the support trio reappearing.


------------------------------------------------------------------------------
2026-08-14 (afternoon)  —  Doctrine tranche 2 (midladder S rows) + MK/Boss-Bandit tech verified
------------------------------------------------------------------------------
- Round-4 RESUMED with the bank waiver (11:16, --resume: 14k progress intact).
- DOCTRINE tranche 2 implemented in sim/doctrine.py, all VERIFIED on synthetic states:
  * KITE rule (#58/#61): knight vs PEKKA/Mega Knight kites CENTRE (0.48, 0.62) instead of
    body-blocking -- replaces the on-path block for those two.
  * SPLASH-MELEE placement (#58/#64): skels go BEHIND MK/Valkyrie/Dark Prince (y+0.06), never on
    top; E-GIANT (#66) skels only at his BACK (y-0.06) -- his zap reflex punishes the front.
  * SUPPORT-WALL rocket (#63): >=3 troops of cost >=3 inside one blast radius = rocket clump
    value anywhere on the board, no tower alignment needed (the midladder signature).
- MK KING ACTIVATION corrected per the user + community confirmation ("5 ways without tornado"):
  PRIMARY line = skels bait his jump, KNIGHT placed in front of the king timed to the jump; the
  Tornado pull is the fallback. Both stay L (the sim has no jump-slam splash to activate off).
- BOSS BANDIT verified (Fandom/RoyaleAPI/Theria): dash window 3.5-6 tiles (0.8s windup, 2x dmg);
  Getaway Grenade = 1 elixir, 1s invisible, 6-tile BACKWARD teleport, 2 uses/3s cooldown -> she
  re-dashes the target the teleport put back in her window. COUNTER (user's read confirmed):
  keep a body INSIDE 3.5 tiles of her post-teleport landing -> no dash triggers. Doc row 65
  rewritten; teleport-smother is live-only (champion abilities not modeled in sim).
- WHAT THE REMAINING L ROWS NEED (recorded for planning): (a) MK activation lines (#59/#60) +
  Boss Bandit teleport-smother (#65): either ENGINE features (jump-slam splash w/ king
  activation; champion abilities) or a LIVE AIM-ASSIST implementation (the natural home --
  hardcoded live behaviours like the rocket snap/tesla pull assists, policy decides WHEN);
  (b) #21/#27/#40-class rows tagged L: detector must NAME the card live (whitelist growth via
  labelling); (c) #59-class timing sequences additionally need the live act loop's event wake
  (exists) + sub-second sequencing (two placements ~1s apart -- feasible at the current cadence).


------------------------------------------------------------------------------
2026-08-14 (evening)  —  User corrections cascade: E-Giant AOE, MK jump splash was silently
                         single-target (latent engine bug), king activation now modeled, BB
                         ability variability, engine-vs-screen geometry discovered
------------------------------------------------------------------------------
- E-GIANT (user correction): his reflect is an AOE RADIUS -- side does not matter. The skels-at-
  his-back doctrine rule was WRONG and is deleted; vs E-Giant skels get NO spot (corner fallback)
  and the counter is ranged-only. DOCTRINE.md #66 rewritten.
- SPLASH-MELEE GEOMETRY (user correction): "skels behind" only exists for DEEP units (y >= 0.60,
  behind = river-ward). A blocker at our OFFENSIVE bow has its back on the enemy side --
  undeployable -- so shallow targets get a LATERAL distraction spot instead (buys the bow 2-3
  shots). doctrine.py fixed + verified both depths.
- BOSS BANDIT (user: modeled, but fires automatically at a flat 60% HP): trigger is now ROLLED
  per unit (U[0.35, 0.80]) and RE-ROLLED lower after each use, so the two Getaway Grenades
  spread across her HP bar and across matches -- the policy cannot learn a constant. VERIFIED:
  6 seeds -> 6 different thresholds, still fires when crossed. (Spec confirmed vs Fandom: 1
  elixir, 1s invis, 6-tile back teleport, 2 uses, 3s cd; dash band 3.5-6, 2x dmg.)
- MEGA KNIGHT: the leap IS modeled (user right) -- and testing the king-activation chain exposed
  a LATENT BUG: the landing-splash gate tested the generic `splash` flag, which is FALSE for MK
  (his jump splash is its own leap_splash field). His jump has been silently SINGLE-TARGET in
  every training run to date. FIXED (gate = leap_splash > 0 or splash) + the tower coupling
  added: the slam now damages CROWN TOWERS near the landing (both landing kinds; via
  _damage_tower so activation-on-damage fires). VERIFIED: slam beside the king -> 537 chip +
  ACTIVE=True; mid-board landing touches no tower; group landing splashes units again.
  -> #59 MK king activation is now S: sim-learnable, and the doctrine bait spots teach it.
- GEOMETRY DISCOVERY (the reason every activation test failed at first): the ENGINE's towers sit
  DEEPER than the live screen anchors (engine king (0.50, 0.906) vs screen 0.72; princesses
  0.797 vs 0.615). Bridge-relative doctrine spots are unaffected (river = 0.5 both spaces), but
  KING-RELATIVE spots were ~7 tiles short -- doctrine.py now reads eng.towers[0][2] at runtime
  for the MK baits and the nado-king destination (VERIFIED: spots land 1.5-4.2 tiles off the
  real king). FLAGGED, not fixed: the same mismatch warps SIM-to-LIVE placement transfer on our
  half (a cell learned as "4 tiles from the engine king" taps BEHIND the live screen king) --
  candidate future fix: align engine tower positions with the action-frame geometry, a
  significant engine change needing its own gated run.
- ROLLOUT NOTE: the resumed run (11:16) carries the bank waiver but PREDATES tranche-2 doctrine
  + these engine fixes. Plan: read the waiver's effect over a few hourly checks, then restart as
  round 5 to bundle tranche-2 + the MK/BB engine fidelity fixes (they change opponent difficulty
  -- MK actually splashes now -- so they deserve a marked run boundary anyway).


------------------------------------------------------------------------------
2026-08-14 (night)  —  Fidelity audit (user questions) + EVOLUTIONS.md research/plan
------------------------------------------------------------------------------
- AUDITED per user: (1) LOG vs buildings: already correct (corridor damages buildings -- pump
  1070->695 verified -- plus the tower chip). (2) E-GIANT reflect: NOT implemented (KB has only
  tank/building_targeting; sim E-Giant is a plain tank -- the ranged-only doctrine is
  unlearnable in sim until the Zap Pack reflect lands). (3) SPAWN DAMAGE: modeled
  (_deploy_blast) + curated for MK 430 / Goblin Drill 84 / Ice Wizard 84; full Fandom category
  sweep queued. (4) MELEE SPLASH: systemic problems found -- dark_prince/executioner/witch/
  skeleton_king (+ MK's normal swings) are flagged splash=False and fight SINGLE-TARGET in the
  sim (same bug family as the leap gate), and there is NO per-card splash radius (flat 1.9-tile
  fallback for everyone). Fix plan in EVOLUTIONS.md §0, incl. the user's sim-view AOE-circle
  visualisation idea.
- EVOLUTIONS.md written: all 42 evolutions (41 already stat-imported + the new Season-86 Evo
  Elite Barbarians: rage javelins at range + a lingering rage TRAIL, cycle 1), each mapped to an
  engine tier (T0 stat-only / T1 existing primitive / T2 new primitive; icebow's own Knight+Tesla
  evos already fully modeled), with the phased plan: A = opponents field evos at all (meta_decks
  evo slot + ScriptedBot evo-cycle via the existing build_spec overlay -- T0 evos work
  end-to-end), B = mechanics by meta priority behind the §0 fidelity fixes, C = per-evo unit
  tests + doctrine rows. [verify] tags mark evos whose Fandom pages must be read before coding.


------------------------------------------------------------------------------
2026-08-15  —  Phase A shipped: opponents field evolutions; fidelity fixes (reflect, splash, flash)
------------------------------------------------------------------------------
- PHASE A (EVOLUTIONS.md) IMPLEMENTED + VERIFIED: ScriptedBot now fields its deck's ONE evolution
  (2026 slot rules) -- heuristic pick (first deck card with a loadable `_evo` stat block; KB
  evolution.cycles, default 2), charge counted per base play, the charged play fields the `_evo`
  spec through the ordinary build_spec overlay. VERIFIED: a knight deck charges in 2 plays and
  fields knight_evo on the 3rd (carrying its modeled damage-reduction). SelfPlayOpponent already
  had full evo machinery (mirrors our deck). T0 stat-evos now work end-to-end vs opponents.
- E-GIANT ZAP PACK IMPLEMENTED (engine + KB): reflect_damage 120 / radius 3.0 / stun 0.5s (wiki),
  applied in _land_hit when the ATTACKER (new param, threaded through all 4 direct-attack call
  sites) damages a reflect card from inside the radius; off while stunned/frozen; spell damage
  excluded (no attacker position); projectile attackers effectively safe outside the radius --
  which is the point. VERIFIED: adjacent knight 1766 -> 1286 (4 zaps), ice wizard at 5.8 tiles
  untouched. The ranged-only doctrine vs E-Giant is now LEARNABLE in sim.
- SPLASH SYSTEM FIXED: build_spec's splash gate now honours the curated `splash` FLAG (it only
  read db.has_splash -- witch's curated flag was silently unread, and dark_prince/skeleton_king
  had none: three real splash troops fought single-target; MK's swings fixed by the same line).
  NEW per-card `splash_radius_tiles` -> CardSpec.splash_r (flat 1.9 fallback retained): curated
  MK 1.3 (published), dark_prince 1.1 [verify], skeleton_king 1.5 [verify].
- SPLASH FLASH (user request): engine records (x, y, radius, t) per splash HIT (capped 40);
  sim-view draws each as its true AOE ellipse for ~0.15 s at the moment of attack -- radius
  errors are now visible instead of latent. VERIFIED events record on a valkyrie engagement.
- cards-import re-run: 41 evolutions re-imported; **elite_barbarians_evo NOT yet on the wiki's
  scrapeable pages** (Season 86 too new) -- re-run cards-import in a few days; until then Evo
  E-Barbs stays a [pending-import] row in EVOLUTIONS.md.
- NOTE: all of today's engine changes (incl. yesterday's MK leap-splash + BB variability) land
  on the NEXT trainer restart -- the running round-4 resume predates them. Restart as round 5
  when ready; expect a difficulty step (opponents now field evos AND splash properly).

2026-08-14 -- E-Wiz spawn zap + deploy-blast status + ranged-AOE audit (user request)
- ELECTRO_WIZARD SPAWN ZAP: curated spawn_damage 115 [verify] + spawn_radius_tiles 3.0 [verify]
  in cards.yaml (dmg = one zap at L11; import carries NO spawn_damage for him). Rides the
  existing _deploy_blast path.
- _deploy_blast now calls _apply_status on every unit it hits: E-Wiz's spawn zap STUNS,
  ice_wizard's spawn nova now applies his SLOW (both were damage-only before). MK/drill/
  delivery unaffected (no status flags). VERIFIED: knight next to deploying E-Wiz takes
  1766->1249 and is stunned when the deploy finishes.
- RANGED-AOE AUDIT (user asked to integrate ranged splash "the bomber way"): already done --
  build_spec gives bomber/wizard/princess/sparky/firecracker real projectiles (imported speed)
  whose impact blast radius = published splash radius (1.5/1.5/2.0/1.8/0.4). No change needed;
  magic_archer's piercing line remains the T2 exception. Earlier flat-vs-nested projectile-key
  worry was unfounded.
- EVOLUTIONS.md 0.1 Table 2 updated (e-wiz row + status note + ranged-AOE audit note). Sweep
  tables COMPLETE -- awaiting user verification, then round-5 restart.

2026-08-14 -- Dark Prince charged splash (user-verified radii)
- USER CORRECTION: dark_prince splash is 1.25 tiles base (import said 1.1), widened to 2.2 on a
  CHARGED strike. Curated splash_radius_tiles 1.25 + charge_splash_radius_tiles 2.2 in
  cards.yaml (verified: true).
- ENGINE: new CardSpec.charge_splash_r; a completed run-up now replaces the hit's damage AND
  its blast radius (_attack passes splash_r override into _land_hit; splash rad resolution is
  charged-override -> per-card -> 1.9 fallback). Only the direct melee tail widens -- no
  charging card fires projectiles/split/shotgun.
- VERIFIED (bare engine, stationary buildings so geometry can't drift): bystander x-bow 2.0
  tiles from the target takes 0 from a base swing (266) and the full 532 from a charged swing.
  Status-effects suite still green (4/4).
- Repo now COMMITTED + PUSHED to main after every change batch (user request, standing rule).

2026-08-14 -- Firecracker cone shards + lane-aware tower targeting (user bug reports)
- FIRECRACKER (user: secondary projectiles render wrong): root cause was WORSE than rendering
  -- the old _spark_burst sprayed 5 bolts in a 360-degree RING with pierce=True but never set
  dirx/diry, and piercing shots move along dirx/diry... so all 5 bolts sat MOTIONLESS on the
  impact point until expiry. Rebuilt per wiki/guides: 5 bolts spray FORWARD in a 70-degree cone
  (17.5 deg apart) centred on the rocket's heading; each bolt PIERCES with the published
  0.4-tile corridor; damage is PER BOLT (5 x 64 = 320 max on one body) and the carrier rocket
  deals NOTHING (was double-dipping 64 blast + shards); bolts fly the remainder of her 11-tile
  projectile budget past impact (bridge tower-chip now reachable, like Magic Archer).
  sim_view: sparks draw as small corridor circles, no label spam. VERIFIED: primary target
  takes all 5 bolts (318), body 3 tiles behind takes the centre bolt (66), 90-degree flanker
  untouched.
- LAVA HOUND (user: empty-lane back spawn cross-maps to the far princess instead of king):
  building-targeters + the generic march-at-tower fallback picked nearest tower by EUCLIDEAN
  distance -- from a deep back spawn in a towerless lane the far princess (~25t) beats the king
  (~26.5t), and heading diagonally keeps it self-consistently closer. New _march_gap: when the
  river is in between, distance routes through the unit's OWN lane bridge (same bridge the
  movement code picks), so targeting and walking can never disagree; air uses the same lane
  rule for SELECTION (real CR: back-lane Balloon/Hound in an empty lane heads for the King).
  VERIFIED: both towers up -> near princess; lane tower down -> KING. Applies to all
  building-only troops AND generic troops with nothing in sight.
- Regression: status effects 4/4, death splits OK, 3 full random-policy matches no hangs.
- NOTE: round 5 restarted 12:51 predates these two fixes -- needs ANOTHER restart to pick
  them up (run is minutes old, cheap).

2026-08-14 -- T1 EVO MECHANICS BATCH: all eight, wiki-swept + unit-tested (user request)
- New suite tests/test_evo_t1.py (8/8 green) pins every number below; stats flow from the
  imported _evo rows via the overlay fix. Mechanics land on the NEXT sim restart (round 5
  currently running was restarted by the user before this batch).
- EVO ROYAL GIANT: every shot blasts 2.5 tiles AROUND HIMSELF, ground only, 1-tile shove
  (air immune; heavies shrug via knockback_immune). Recoil damage NOT published anywhere
  scrapeable -- curated 154 (= half his 307 hit) tagged [verify] for the user.
- EVO SKELETONS: +1 evo skeleton per landed swing, hard cap 8 alive per team (wiki: "maximum
  total of 8"). VERIFIED population 3->8, never exceeds cap.
- EVO RECRUITS: charge arms only AFTER the shield breaks (2.5-tile run-up, 2x = 266 at L11).
  VERIFIED 133 with shield, 266 without. Generic charge_after_shield gate on the charge block.
- EVO BARBARIANS: self-rage on every swing: +30% move+attack for 3 s (3 s since 14/5/2024,
  30% since the 6/10/2025 global rage nerf). Shares the no-stacking rage rule with zones.
- EVO VALKYRIE: every swing spawns a 0.5 s vortex -- 5.5 tiles, pulls ground AND air toward
  her, 76 damage spread over the spin (reuses the tornado _Vortex machinery via replace()).
- EVO ZAP: growing triple pulse -- 2.5 -> 3.0 -> 3.5 tiles, ~1 s apart, each a FULL zap
  (192 dmg / 58 crown / 0.5 stun). _Spell gained r_override + echoes; echo re-queues itself.
- EVO PEKKA: imported hp 5640 was the OVERHEAL CAP (3760 x 1.5 exactly), not deploy hp, and
  the damage row was missing -- re-curated 3760/842. Post-rework heal: flat 470 per troop/
  building kill (12.5% of core hp), overheal to 150%. Hooked at the melee kill site.
- EVO SKELETON BARREL: KB count 2 = BARRELS CARRIED, not bodies (would have fielded two
  units) -- re-curated one body, contact damage zeroed like the base. First barrel drops at
  75% hp mid-flight (238/2.0 blast + 7 skels), second on death, BOTH at once if it reaches
  the tower unspent. VERIFIED 7 mid + 7 death, and 14-at-once on an untouched arrival.
- Regression: evo suite 8/8, status effects, death splits, wincon bank, 3 random matches OK.

2026-08-14 -- CRITICAL evo-stats fix + Evo Bomber bounces + Suspicious Bush rebuild (user bugs)
- SYSTEMIC BUG (user noticed via Evo Bomber): build_spec stripped "_evo" and read only the
  BASE row + curated evolution dict -- the imported `<base>_evo` KB rows (which carry the evo's
  own stats top-level) were NEVER merged. ALL 41 imported evos fielded base stats in Phase A.
  Fix: evo row overlays base before parsing (evolution/base/display/rarity excluded so curated
  mechanic dicts -- our Knight DR / Tesla pulse -- still apply). VERIFIED bomber_evo 304->332,
  knight_evo damage_reduction 0.60 intact.
- EVO BOMBER (user: "coded as single-target melee" -- it was ranged splash all along, but with
  base hp and no evo mechanic): BOUNCING BOMBS implemented -- after the initial blast the bomb
  bounces TWICE, 2.5 tiles apart along the flight heading (5/3/2024 balance, from 3), same
  damage/area each blast; enemies hit only ONCE per attack (16/12/2024) via a hit set shared
  across the chain. This is the T2 "projectile continuation" primitive from EVOLUTIONS 2.
  VERIFIED: bodies at 0/+2.5/+5 tiles each take exactly one 225 blast.
- SUSPICIOUS BUSH (user: no goblins on defeat, should be invisible + tower-targeting + never
  attack, stats completely off): the imported row had the BUSH GOBLINS' stats (304/256) pinned
  on the parent. Rebuilt: bush = 81 hp (skeleton-tier), damage 0, medium speed,
  building_targeting, ghost-invisible for LIFE (reuses Royal Ghost machinery -- refade keys on
  fighting and it never fights; troops AND towers ignore it, splash/spells still hit),
  kamikaze on reaching a building; on arrival OR defeat spawns 2 BUSH GOBLINS (new pseudo-card:
  304 hp / 227 dmg / 1.4s / melee, from wiki + import spawn_unit_stats). VERIFIED: enemy knight
  never targets it, it never targets troops, crosses the board untouched, breaks at the tower
  into exactly 2 goblins, zero tower damage from the bush itself.
- Regression: all 3 suites OK, 3 random matches clean. Lands on next sim restart (still pending).

2026-08-14 -- Death mechanics: Lumberjack rage, Phoenix egg, Skeleton Barrel dive, rascal
             hitboxes (user requests)
- LUMBERJACK: drops a Rage zone where he dies -- radius 3 tiles, 4.5 s, +30% move AND attack
  speed for friendlies, 0.5 s arm delay (Aug/Oct 2025 balance values). New engine rage_zones
  (x, y, r, team, t_on, t_off, boost) + _rage_mult hook at the one point spd feeds both
  movement and cooldown; zones don't stack (strongest wins); magenta ring in sim_view.
  Curated as cards.yaml drops_rage (generic -- any future card can drop one).
- PHOENIX: death blast (already imported 163/1.5) + drops an EGG once: phoenix_egg pseudo-card
  (239 hp @11, troop so Log/Tornado move it, stationary, harmless). Egg that SURVIVES 3.8 s
  hatches a REBORN phoenix at 80% hp/damage (7/2/2023 balance) via hatch_left/hatch_spec;
  hatching removes the egg OUTSIDE the death path. Reborn (from_egg) deals no death damage and
  drops no second egg; a killed egg never hatches. VERIFIED both branches.
- SKELETON BARREL: "reaches a building or is destroyed -> barrel drops": kamikaze flag + zeroed
  contact damage (damage AND dps -- dps fallback would have resurrected it), spawns
  {unit: skeletons, on_death: 7}. Death blast 145/2.0 (imported) + knockback + 7 skels fire
  identically on arrival-dive and on being shot down. VERIFIED: tower takes exactly 145.
- RASCALS (user: girls have much smaller hitbox): new component_collision_tiles cards.yaml key,
  index-aligned with KB component rows -- [boy 0.75, girls 0.45]. Component builder previously
  inherited the card-level radius (the boy's) for every member.
- Regression: status effects, death splits, wincon bank all OK; 3 full random matches clean.
- These land on the NEXT sim restart (round-5-restart still pending from the firecracker/hound
  batch).

2026-08-14 -- E-Wiz spawn zap values user-verified
- USER CONFIRMED: electro_wizard spawn zap 115 dmg / 3.0-tile radius -- both correct as
  curated. [verify] tags cleared from cards.yaml comment + EVOLUTIONS.md Table 2.
- Sweep tables 0.1 are now FULLY verified (dark_prince settled yesterday) -> round-5 restart
  is clear to launch: run.py train-sim-ppo --matches 100000 --envs 32 --resume --size 432.


------------------------------------------------------------------------------
Notes / caveats
------------------------------------------------------------------------------
- Reward-shaping changes only take effect after an RL run (`train-rl`) — they
  steer learning; they are not hard-coded behaviour.
- Card recognition is template matching (no learned classifier), so it needs
  per-deck templates built from your own recordings (`hand-templates`).
- Model weights (data/*.pt) and recordings (data/sessions/) are intentionally
  NOT committed — they are large and regenerable. Retrain to reproduce.
